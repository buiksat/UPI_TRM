%%%%%%%% ICML 2026 SUBMISSION FILE %%%%%%%%%%%%%%%%%

\documentclass{article}

% Recommended packages:
\IfFileExists{microtype.sty}{\usepackage{microtype}}{}
\usepackage{graphicx}
% \usepackage{subfigure} % deprecated; avoid if possible
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage[dvipsnames]{xcolor}
\usepackage{hyperref}
\hypersetup{
  pdftitle={Unrolled Policy Iteration for Tiny Recursive Models},
  pdfauthor={},
  pdfcreator={},
  pdfproducer={}
}

\providecommand{\theHalgorithm}{\arabic{algorithm}}

% Use this for the initial blind version:
\usepackage{icml2026}
% For camera-ready: \usepackage[accepted]{icml2026}

% Math and theorems
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}
\usepackage{amsthm}

% Algorithms
\usepackage{algorithm}
\usepackage{algorithmic}

% Clever references
\usepackage[capitalize,noabbrev]{cleveref}

% For itemize options
\usepackage{enumitem}
% \overfullrule=2cm % debug only; uncomment for dr

% Emergency stretch and tolerance for better line breaking
\emergencystretch=3em
\tolerance=1000


%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% THEOREMS
%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{assumption}[theorem]{Assumption}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\newtheorem*{theorem*}{Theorem}
\newtheorem*{proposition*}{Proposition}
\newtheorem*{lemma*}{Lemma}
\newtheorem{example}[theorem]{Example}


% ========================================
% Collaborative comments (OFF by default for anonymous submission)
% ========================================
\newif\ifcomments
\commentsfalse % SUBMISSION: keep OFF (set \commentstrue only for internal drafts)
\ifcomments
  \PackageWarningNoLine{main}{COMMENTS ARE ENABLED. Set commentsfalse before submission.}
  \newcommand{\gopesh}[1]{{\color{red}\small[Gopesh: #1]}}
  \newcommand{\bahram}[1]{{\color{blue}\small[Bahram: #1]}}
  \newcommand{\houssam}[1]{{\color{green}\small[Houssam: #1]}}
  \newcommand{\brett}[1]{{\color{Mulberry}\small[Brett: #1]}}
\else
  \newcommand{\gopesh}[1]{}
  \newcommand{\bahram}[1]{}
  \newcommand{\houssam}[1]{}
  \newcommand{\brett}[1]{}
\fi

% ========================================
% Length toggle: short (ICML submission) vs long (draft)
% ========================================
\newif\ifshort
\shorttrue % <-- set \shortfalse for the long draft

\ifshort
  \newcommand{\shortonly}[1]{#1}
  \newcommand{\longonly}[1]{}
\else
  \newcommand{\shortonly}[1]{}
  \newcommand{\longonly}[1]{#1}
\fi

%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% CUSTOM MACROS
%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\newcommand{\R}{\mathbb{R}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\XX}{\mathcal{X}}
\newcommand{\YY}{\mathcal{Y}}
\newcommand{\ZZ}{\mathcal{Z}}
\newcommand{\SSS}{\mathcal{S}}
\newcommand{\AAcal}{\mathcal{A}}
\newcommand{\norm}[1]{\left\lVert #1 \right\rVert}
\newcommand{\Lip}{\mathrm{Lip}}
\newcommand{\KL}{\mathrm{KL}}
\newcommand{\sg}{\operatorname{sg}}
\newcommand{\Cdrift}{C_{\mathrm{drift}}}
% NEW: Notation for policy-specific sup-norms
\newcommand{\supnorm}[2]{\left\| #1 \right\|_{\infty,#2}}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% TITLE & AUTHOR
%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\icmltitlerunning{UPI--TRM}

\begin{document}

% Suppress underfull box warnings
\hbadness=10000
\vbadness=10000

\twocolumn[
\icmltitle{Unrolled Policy Iteration for Tiny Recursive Models}

\begin{icmlauthorlist}
\icmlauthor{Anonymous Authors}{anon}
\end{icmlauthorlist}

\icmlaffiliation{anon}{Anonymous Institution}
\icmlcorrespondingauthor{Anonymous}{anonymous@example.com}

\icmlkeywords{Reinforcement Learning, Policy Iteration, Tiny Recursive Models, Value Function Approximation, Plan-Space MDP, Plan Editing}

\vskip 0.3in
]

\printAffiliationsAndNotice{} % leave blank for anonymous submission
\begin{abstract}
We study reinforcement learning in architectures with \textbf{compute-truncated internal evaluators}, where policy improvement depends on value estimates produced by unrolling a recurrent evaluator for $n$ steps. This arises in plan-editing models such as Tiny Recursive Models trained from checker feedback, where evaluator depth is an architectural compute budget rather than an outer-loop algorithmic choice. We formalize plan editing as a plan-space MDP and analyze approximate policy iteration with truncated internal evaluation. Under a contraction assumption on the latent recursion, we decompose value error into (i) an architectural Bellman-residual term and (ii) a finite-unrolling bias that decays geometrically with the contraction modulus and unroll depth, making explicit how evaluator design parameters govern evaluation accuracy. For conservative mixture updates, we show that with statewise-centered advantages, evaluation bias enters the improvement bound with an $O(\alpha)$ dependence on the mixture weight $\alpha$, reducing sensitivity to imperfect evaluation. Experiments on a controlled 4$\times$4 Sudoku suite trained from checker feedback validate feasibility and isolate how contraction, projection, and unroll depth affect stability under evaluation-time depth mismatch. Overall, the analysis identifies contraction and unroll depth as practical stability controls for policy improvement under truncated internal computation.
\end{abstract}

\section{Introduction}
\label{sec:intro}

Tiny Recursive Models (TRMs)~\citep{TRM2025} solve verifiable reasoning tasks by iteratively editing a candidate plan using a small recurrent evaluator.
Training TRMs typically requires ground-truth outputs with deep supervision~\citep{TRM2025}.
We investigate whether TRM-style architectures can be trained \emph{solely from checker feedback}---scalar signals from an automatic verifier indicating correctness or constraint satisfaction---without access to demonstrations.
This setting is appealing when labeled solutions are expensive but verifiers are cheap (e.g., Sudoku constraints, unit tests, exact-match rules).

Training from checker feedback alone is challenging: the evaluator's value estimates are both \emph{approximate} (limited expressivity) and \emph{computationally truncated} (finite unrolling of the inner recursion).
Standard approximate policy iteration (API) and conservative policy iteration (CPI) bounds treat evaluation error as an external quantity~\citep{BertsekasTsitsiklis1996,KakadeLangford2002}.
In TRMs, however, evaluation is performed by an internal dynamical system whose truncation depth $n$ is a design knob.
Understanding how compute truncation interacts with policy improvement requires making this dependency explicit.

\textbf{Plans, edits, and checker feedback.}
We call the structured candidate output $y \in \YY$ a \emph{plan} (e.g., a filled Sudoku grid or symbolic structure).
An automatic \emph{checker} maps an instance--plan pair $(x,y)$ to a scalar score $c(x,y)$.
We treat the outer edit loop of a TRM as a discounted MDP over plans: states are instance--plan pairs $(x,y)$, actions are edits that modify the plan, and rewards derive from the checker.

\textbf{TRM as plan editing.}
At edit step $t$, the model holds a current plan $y_t$.
An \emph{inner evaluator} runs $n$ recurrent updates to produce a latent state $z^{(n)}(x,y_t)$, and an \emph{edit policy} proposes an action $a_t$, producing the next plan $y_{t+1}=\mathrm{edit}(y_t,a_t;x)$.
This defines a sequential decision process over plans---the \emph{plan-space MDP}.

\begin{table}[t]
\caption{TRM edit loop as plan-space MDP. TRM evaluator dials: $L_z$ (contraction), $n$ (unroll depth). Standard algorithmic knobs: $K$ (bootstrap horizon), $\alpha$ (mixture weight).}
\label{table:overview}
\centering
\footnotesize
\begin{tabular}{@{}lp{0.62\columnwidth}@{}}
\toprule
\multicolumn{2}{@{}l@{}}{\textbf{Inner evaluator (TRM)}} \\
\midrule
Latent unroll & $z^{(t+1)}\!=\!(\Pi_R\!\circ\!f_\theta)(z^{(t)},y,x)$ \\
Unrolled value & $U_n(s)=V_\psi(z^{(n)}(s),x)$ \\
Bias & Dial $L_z<1$; truncation error $\propto L_z^n$ \\
\midrule
\multicolumn{2}{@{}l@{}}{\textbf{Outer plan-space MDP}} \\
\midrule
State & $s=(x,y)$; instance $x$ fixed per episode \\
Action & $a\in\mathcal{A}(y)$; edit $y'=\mathrm{edit}(y,a;x)$ \\
Reward & Checker + shaping; discount $\gamma$ \\
Update & $\pi_{\mathrm{new}}=(1-\alpha)\pi+\alpha\pi_{\mathrm{cand}}$ \\
\bottomrule
\end{tabular}
\end{table}

\Cref{table:overview} summarizes this mapping and \Cref{fig:trm_architecture} illustrates the architecture.
The table highlights two TRM-specific evaluator dials (Lipschitz constant $L_z$ of the recurrent update network, and number $n$ of recursive updates), plus two standard algorithmic knobs ($K$-step bootstrapped estimate, and mixture weight $\alpha$ for conservative update) that appear in our bounds.
We call $(L_z, n)$ \emph{dials} because they control evaluator computation, and $(K, \alpha)$ \emph{knobs} because they are standard API/CPI hyperparameters. This distinction highlights that our bounds expose TRM-specific structure beyond standard approximate DP.

\textbf{From an edit loop to an MDP.}
An episode begins by sampling an instance $x\sim\mathcal{D}$ and initial plan $y_0\sim\iota(\cdot\mid x)$; the agent then repeatedly applies edits.
This is an ordinary discounted MDP with state $s=(x,y)$, action $a$ an edit, and deterministic transition $y'=\mathrm{edit}(y,a;x)$.
Crucially, the TRM inner recursion does \emph{not} define environment dynamics; it defines a \emph{compute-truncated value estimate} $U_n(s)$ used for policy evaluation and improvement.
The TRM-specific architectural parameters, which we call ``dials'' $(L_z, n)$, control evaluator compute and inner-loop stability; the standard algorithmic hyperparameters, or ``knobs'' $(K, \alpha)$ from API/CPI---multi-step targets and mixture conservativeness---provide a clean analysis vehicle.

This formulation raises two questions that we answer with explicit bounds: (i) How do inner-loop contraction and unrolling depth $n$ control value-estimation accuracy? (ii) How do conservative updates mitigate the resulting evaluation error?

\textbf{Contributions.}
\begin{enumerate}
\item \textbf{Algorithm:} We propose UPI--TRM, an unrolled generalized policy-iteration template for TRMs trained from checker-based reward, combining $K$-step bootstrapped value regression, advantage-based improvement, and conservative mixture updates.
\item \textbf{Value decomposition:} Under a contraction assumption on the latent recursion, we decompose value error into an \emph{Architectural $K$-step Bellman Residual} plus a \emph{Truncation Bias} term that decays as $L_z^n$ (Proposition~\ref{prop:error_bounds}).
\item \textbf{Conservative improvement:} For mixture updates with statewise-centered advantages, we obtain a CPI bound where evaluation error contributes as $O(\alpha\,\varepsilon_{A,\mathrm{cand}})$ rather than $O(\varepsilon_A)$ (Theorem~\ref{thm:monotone_with_error}). This guarantee relies on exact centering; our experiments use approximate centering, so the bound degrades according to the centering defect $\varepsilon_{\mathrm{cent}}:=\sup_s|\E_{a\sim\pi}[\widehat A(s,a)]|$ (see Remark~\ref{rem:centering_practice}).
\end{enumerate}

%\noindent We summarize the algorithmic template in Section~\ref{sec:algorithm} before presenting the analysis.

\longonly{
\textbf{Relation to API/CPI.}
Standard API/CPI bounds treat evaluation error as an external quantity~\citep{BertsekasTsitsiklis1996,Scherrer2014,KakadeLangford2002}.
Our contribution is to make \emph{compute truncation} explicit: the error decomposition (architectural residual $+$ unrolling bias $\propto L_z^n$) connects TRM-specific evaluator design choices $(L_z, n)$ to safe-improvement thresholds.
The Bellman contraction and Banach fixed-point results we invoke are standard (included for completeness), as are the mixture-update knobs $(K, \alpha)$ from API/CPI; our novelty lies in (i) exposing TRM evaluator truncation depth $n$ in the value-error bound, and (ii) showing how TRM evaluation error interacts with conservative mixture updates---centered advantages yield an $O(\alpha)$ rather than $O(1)$ penalty.

\textbf{Positioning.}
Our plan-space MDP framing connects to learning-to-search~\citep{Daume2009SEARN,Ross2011DAgger,Chang2015LOLS} and RL with verifiable rewards, while our stability analysis builds on CPI and trust-region ideas~\citep{KakadeLangford2002,Schulman2015TRPO}.}
\textbf{Scope of theory.}
Our analysis is API/CPI-style: we bound error propagation in a single evaluation$\rightarrow$improvement step, \emph{conditional} on bounded residual/advantage error.
We do \emph{not} prove end-to-end convergence of SGD or TD learning.
The bounds provide structural guidance: they show how $(L_z,n)$ (TRM evaluator) and $(K,\alpha)$ (standard) influence safe improvement, and what to monitor (empirical residuals, centering defect).
Implementation details (replay, target networks, distillation) are in \Cref{app:implementation}.
\longonly{A condensed positioning relative to environment-state RL (e.g., HRM-Agent) and token-space self-improvement methods appears in Section~\ref{sec:related_work}.}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/diagram.pdf}
\caption{TRM architecture for plan-space policy iteration. The \textbf{State} $(x, y_t)$ feeds the \textbf{Inner Evaluator}, which unrolls $n$ steps to produce latent $z^{(n)}$. The \textbf{Value Head} computes $U_n(s_t)$ from $z^{(n)}$, while the \textbf{Checker} provides reward $r_t$. Together these form the \textbf{Advantage} $\hat{A}$, which drives policy gradients to the \textbf{Policy}. Dashed arrow: action $a_t$ updates the plan. Annotation boxes show TRM dials ($L_z$, $n$, $R$) and standard knobs ($K$-step bootstrap, mixture $\alpha$).}
\label{fig:trm_architecture}
\end{figure}

\section{Background and Setup}
\label{sec:setup}

\shortonly{
% Compressed setup for ICML short version
\textbf{Notation.}
$s = (x, y)$ denotes a state (instance--plan pair); $z \in \mathbb{R}^d$ is the latent; $z^{(n)}$ is the latent after $n$ inner steps; $z^*$ is the fixed point. $L_z$ is the contraction modulus of the inner update; $L_V$ is the Lipschitz constant of the value head. $\varepsilon_{\mathrm{res}}^*$, $\varepsilon_A$, $\varepsilon_{A,\mathrm{cand}}$, $\varepsilon_{\mathrm{cent}}$ denote various error quantities defined in Section~\ref{sec:error_bounds}. See Table~\ref{tab:notation} (Appendix~\ref{app:notation}) for a complete list.

We formalize plan editing as an MDP and introduce the TRM evaluator structure.

\textbf{Plan-space MDP.}
States $s=(x,y)$ pair an instance $x\in\XX$ (e.g., a puzzle) with a candidate plan $y\in\YY$.
Actions edit the plan: $y'=\mathrm{edit}(y,a;x)$.
A deterministic checker provides reward $c(x,y)\in[0,C_{\max}]$.
We use potential-based shaping $\Phi(s)=c(x,y)$ with discount $\gamma$.
Standard MDP quantities (value $V^\pi$, advantage $A^\pi$) are defined as usual; see Appendix~\ref{app:notation}.

\textbf{TRM evaluator.}
A Tiny Recursive Model maintains a latent $z\in\ZZ=\R^d$ updated via $z^{(t+1)}=f_\theta(z^{(t)},y,x)$.
After $n$ inner steps (with optional projection $\Pi_R$), we define the unrolled value as $U_n(s)=V_\psi(z^{(n)}(s),x)$.
The ideal (fixed-point) value is $U_*(s)=V_\psi(z^*(s),x)$ where $z^*$ is the unique fixed point of the $L_z$-contractive map $z\mapsto (\Pi_R\circ f_\theta)(z,y,x)$.

\textbf{Episodic-$z$ vs.\ persistent-$z$.}
Our primary analysis uses \emph{episodic-$z$}: the latent is reinitialized each edit step, giving Markov dynamics on $(x,y)$.
The persistent-$z$ extension (Appendix~\ref{app:persistent_latent}) carries $z$ across edits and requires a slow-drift assumption.

\textbf{Advantage-evaluation closure.}
Conservative improvement requires value accuracy on on-policy states and their one-step successors under candidate actions.
We write $\|\cdot\|_{\infty,\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}$ for sup-norms over this closure (Appendix~\ref{app:adv_closure_full}).}

\longonly{
We establish notation, define the plan-space MDP, and introduce key conventions that will be used throughout.

\textbf{Roadmap for the setup.}
Our analysis needs two simple technical devices beyond standard RL notation.
First, conservative improvement evaluates candidate actions from on-policy states, so to bound advantage error we require value accuracy not only on states visited by $\pi$ but also on their one-step successors under actions from $\pi$ and a candidate policy. This motivates the \emph{advantage-evaluation closure} in Definition~\ref{def:adv_closure}.
Second, because we use potential-based shaping, we explicitly fix a terminal boundary value (equivalently, a constant shift of the usual $V(s_{\mathrm{abs}})=0$ convention). This lets us write $K$-step operators without special-casing early termination while keeping advantages and policy improvement unchanged.

\subsection{Notation and Conventions}
\label{sec:notation}

We use $\|\cdot\|_\infty$ for sup-norms over state spaces and $$\|\cdot\| \coloneqq \|\cdot\|_2$$ for the Euclidean norm on the latent space $\ZZ=\R^d$.
All Lipschitz constants ($L_z$, $L_V$, etc.) and the projection $\Pi_R$ are taken with respect to this Euclidean norm.
We work in the Banach space of bounded real-valued functions equipped with $\|\cdot\|_\infty$.
When we need to emphasize the domain of a sup-norm, we write $\supnorm{f}{\Pi}$ for the sup-norm over the reachable set $\mathcal{R}_\Pi$ (defined below).
Key notation is summarized in \Cref{app:notation}.}

\longonly{
\begin{table}[t]
\caption{Summary of key notation.}
\label{tab:notation_key}
\centering
\scriptsize
\begin{tabular}{@{}ll@{}}
\toprule
Symbol & Meaning \\
\midrule
$\XX, \YY, \ZZ$ & Input, plan, latent spaces \\
$\SSS_{\mathrm{plan}}$ & Plan-space state (episodic-$z$) \\
$\tilde{\SSS}$ & Persistent-$z$ state space \\
$s\!=\!(x,y)$, $\tilde s\!=\!(x,y,z)$ & Plan-space states \\
$s_{\mathrm{abs}}$ & Absorbing terminal state \\
$\mathcal{Z}_{\mathrm{inv}}$ & Forward-invariant region \\
$f_\theta$, $\tilde f_\theta$ & Recursion map, projected map \\
$z^*(x,y)$ & Fixed point \\
$V_\psi(z,x)$ & Latent value head \\
$L_z \!\in\! (0,1)$ & Contraction modulus \\
$C_z$, $R$ & First-update bound, projection radius \\
$L_V$ & Lipschitz constant of $V_\psi$ \\
$\Cdrift(n)$ & Drift constant (persistent-$z$) \\
$\|\cdot\|_{\infty,\Pi}$ & Sup-norm over $\mathcal{R}_\Pi$ \\
\bottomrule
\end{tabular}
\end{table}}

\longonly{
\begin{definition}[Reachable set]
\label{def:reachable}
For a policy $\pi$, let $\mathcal{R}_\pi$ be the set of states $s$ such that
$\Pr_\pi(\exists\, t\ge 0:\; s_t=s \mid s_0\sim\rho)>0$. Let $\rho$ denote a fixed initial-state distribution over $\SSS_{\mathrm{plan}}$ (defined in \Cref{sec:meta_mdp}).

\emph{Convention (absorbing state).}
We include the absorbing terminal state $s_{\mathrm{abs}}$ in function domains purely for closure under transitions during $K$-step rollouts.
We set the boundary value exactly (Appendix~\ref{app:shaping_convention}), so Bellman residuals and value errors at $s_{\mathrm{abs}}$ are identically zero: including or excluding $s_{\mathrm{abs}}$ from sup-norms does not change any bound.
Readers who prefer the standard $V(s_{\mathrm{abs}})=0$ convention can shift the shaping potential by a constant; this leaves advantages and policy improvement unchanged.
\end{definition}

\begin{definition}[Advantage-evaluation closure]
\label{def:adv_closure}
Fix a current policy $\pi$ and a candidate policy $\pi_{\mathrm{cand}}$.
Let $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}$ be any state set that contains $\mathcal{R}_\pi$ and all one-step successors of states in $\mathcal{R}_\pi$ obtained by actions in $\mathrm{supp}(\pi(\cdot|s))\cup\mathrm{supp}(\pi_{\mathrm{cand}}(\cdot|s))$. We further require that $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}$ is closed under forward dynamics of $\pi$.
A constructive minimal choice is given in Appendix~\ref{app:adv_closure_full}.
We write $\|f\|_{\infty,\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}} := \sup_{s\in \mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}} |f(s)|$.
As with $\mathcal{R}_\pi$, all norms implicitly include $s_{\mathrm{abs}}$: $\|f\|_{\infty,\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}} = \sup_{s\in \mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}\cup\{s_{\mathrm{abs}}\}} |f(s)|$.
\end{definition}

\textbf{Standard RL quantities.}
We adopt standard discounted MDP notation~\citep{SuttonBarto2018,Puterman1994}.
For any policy $\pi$, the value function is
$V^\pi(s)=\E_\pi[\sum_{t\ge0}\gamma^t r(s_t,a_t,s_{t+1}) \mid s_0=s]$,
the action-value function is
$Q^\pi(s,a)=\E_\pi[r(s,a,s_1)+\sum_{t\ge1}\gamma^t r(s_t,a_t,s_{t+1}) \mid s_0=s,a_0=a]$,
and the advantage is $A^\pi(s,a)=Q^\pi(s,a)-V^\pi(s)$, satisfying $\E_{a\sim\pi(\cdot|s)}[A^\pi(s,a)]=0$.
Full definitions are in \Cref{app:notation}.

\subsection{Tiny Recursive Models}
\label{sec:trm_brief}

We consider tasks with inputs $x\in\XX$ and structured outputs $y\in\YY$ (grids, sequences, or symbolic structures).
TRM maintains
(i)~an internal latent reasoning state $z\in\ZZ$;
(ii)~a plan $y$; and
(iii)~a small update network $f_\theta:\ZZ\times\YY\times\XX\to\ZZ$.
On each inner step $t$, the latent reasoning state is updated via
\begin{equation}
    z^{(t+1)} = f_\theta\bigl(z^{(t)},y,x\bigr),
    \qquad
    t = 0,1,\dots.
    \label{eq:inner}
\end{equation}
After $n$ inner steps, an edit policy modifies the plan using $z^{(n)}$ and input $x$.



We introduce a parametric edit policy $\pi_\phi(a\mid y,z,x)$ over edit actions $a$, and a deterministic edit operator $y' = \mathrm{edit}(y,a;x)$. This makes the outer edit loop an explicit decision process, allowing us to (i) train the edit policy directly from checker rewards, and (ii) analyze the stability of policy updates as a function of evaluator design and compute (the unrolling depth $n$).

\textbf{Relation to the original TRM edit rule.}
In the original TRM pseudocode, the model repeatedly refines its answer while updating latent features, and training uses deep supervision with an early-stopping signal~\citep{TRM2025}.
Our formulation separates \emph{evaluation} (the $n$-step latent unroll producing $z^{(n)}$) from \emph{editing} (an explicit policy $\pi_\phi$ over plan edits).
This separation is convenient for reinforcement learning from checker rewards: the environment state is the current plan, while the evaluator is a compute-truncated internal dynamical system.

\longonly{
\begin{remark}[Mapping to original TRM]
\label{rem:trm_mapping}
In the original TRM implementation~\citep{TRM2025}: (i) the latent $z$ is persistent across edit steps (our persistent-$z$ variant); (ii) deep supervision provides intermediate targets during training; (iii) an ACT-style halting head determines when to stop inner iterations.
Our episodic-$z$ variant simplifies analysis by reinitializing $z$ each step, yielding Markov dynamics on $\SSS_{\mathrm{plan}}$.
Our RL formulation replaces deep supervision with checker-based rewards.
\end{remark}}

\subsection{Plan-space MDP}
\label{sec:meta_mdp}
We formalize a plan-space MDP over instance--plan pairs $(x,y)$, which is closely related to ``learning to search'' and structured prediction as sequential decision making~\citep{Daume2009SEARN,Ross2011DAgger,Chang2015LOLS}, but uses checker rewards rather than expert demonstrations.
This is an ordinary discounted MDP over $\SSS_{\mathrm{plan}}=\XX\times\YY$; we do not rely on any meta-learning structure.

\textbf{State space and absorbing state.}
We draw an instance $x \sim \mathcal{D}$ and an initial plan $y_0 \sim \iota(\cdot \mid x)$, which together induce the initial-state distribution $\rho(x,y_0)=\mathcal{D}(x)\,\iota(y_0\mid x)$.
Here $\mathcal{D}$ denotes the task/data distribution over inputs (e.g., a dataset of Sudoku puzzles), and $\iota(\cdot \mid x)$ is the initialization distribution over plans given instance $x$ (e.g., in Sudoku we take $y_0=x$, the plan initially equals the clue grid, or $y_0$ is the empty grid with clues filled).
We augment $\SSS_{\mathrm{plan}}$ with an absorbing terminal state $s_{\mathrm{abs}}$ representing solved episodes or termination.
Once $s_t=s_{\mathrm{abs}}$, we have $s_{t+k}=s_{\mathrm{abs}}$ for all $k\ge 1$ (i.e., $P(s_{\mathrm{abs}}\mid s_{\mathrm{abs}},a)=1$ for all $a$).

\textbf{Rewards (checker + potential shaping).}
A task-specific checker provides a bounded score $c(x,y)\in[0,C_{\max}]$.
Transitions are deterministic: $y'=\mathrm{edit}(y,a;x)$.
\begin{remark}[Stochasticity]
For clarity we present deterministic edits and a deterministic checker, as in Sudoku.
Our analysis extends to stochastic transitions and noisy checkers by interpreting $r(s,a,s')$ and Bellman operators under the usual expectation over $P(\cdot\mid s,a)$.
\end{remark}


We use potential-based shaping with $\Phi(x,y)=c(x,y)$:
\begin{equation}
r(s,a,s') = r_0(s,a,s') + \gamma \Phi(s') - \Phi(s),
\label{eq:shaping}
\end{equation}
where base reward $r_0$ is nonzero only on transitions entering the absorbing state.
This preserves optimal policies~\citep{Ng1999Shaping}.
We adopt an absorbing-state normalization with $V^\pi(s_{\mathrm{abs}})=-C_{\max}$ for all $\pi$; details in Appendix~\ref{app:shaping_convention}.

\begin{assumption}[Bounded rewards]
\label{ass:bounded}
We use potential-based shaping~\eqref{eq:shaping}, yielding a \emph{shaped reward} $r(s,a,s')$.
We assume these shaped rewards are uniformly bounded: $|r(s,a,s')|\le R_{\max}$ for all transitions.

%Shaped  rewards are uniformly bounded: $|r(s,a,s')|\le R_{\max}$ for all transitions (including the absorbing self-loop).
Consequently, by the geometric series bound $|\sum_{t\ge 0} \gamma^t r_t| \le \sum_{t\ge 0} \gamma^t R_{\max} = R_{\max}/(1-\gamma)$, for any policy $\pi$ we have
$|V^\pi(s)|\le \frac{R_{\max}}{1-\gamma}$ and $|A^\pi(s,a)|\le \frac{2R_{\max}}{1-\gamma}$~\citep[Ch.~3]{Puterman1994}.
\end{assumption}

Consequently, for any policy $\pi$ and any state $s$ (including $s_{\mathrm{abs}}$),
the discounted return is uniformly bounded:
\begin{equation}
\begin{aligned}
|V^\pi(s)| &\le \frac{R_{\max}}{1-\gamma}, \\
\sup_{a} |Q^\pi(s,a)| &\le \frac{R_{\max}}{1-\gamma}, \\
\sup_{a} |A^\pi(s,a)| &\le \frac{2R_{\max}}{1-\gamma}.
\end{aligned}
\label{eq:value_adv_bounds_from_Rmax}
\end{equation}
In particular, the CPI constant satisfies
$\varepsilon_{\mathrm{CPI}}=\max_s\bigl|\E_{a\sim\pi_{\mathrm{cand}}(\cdot|s)}A^\pi(s,a)\bigr|
\le 2R_{\max}/(1-\gamma)$.

\textbf{Discounted occupancy and performance.}
We fix discount $\gamma\in[0,1)$. The case $\gamma=0$ is degenerate (a one-step greedy objective) but allowed; all bounds hold and simplify. The interesting regime for multi-step plan editing is $\gamma\approx 1$.
A policy $\pi(a\mid s)$ induces the normalized discounted occupancy:
\begin{equation}
    d_\pi(s)
    =
    (1-\gamma)
    \sum_{t\ge 0}
    \gamma^t \Pr(s_t = s \mid s_0\sim\rho,\pi).
\end{equation}
This definition makes $d_\pi$ a probability distribution, as factor $(1-\gamma)$ normalizes the discounted visitation counts.
Using the standard occupancy-measure identity, a direct rearrangement of the discounted return gives
\begin{align}
\eta(\pi)
&=
\E_{s_0\sim\rho}\bigl[V^\pi(s_0)\bigr]
\notag\\
&=
\frac{1}{1-\gamma}\,
\E_{\substack{s\sim d_\pi,\, a\sim\pi(\cdot\mid s) \\ s'\sim P(\cdot\mid s,a)}}[r(s,a,s')];
\end{align}
see~\citet{KakadeLangford2002,Puterman1994}.

\subsection{Episodic-$z$ vs.\ persistent-$z$ variants}
\label{sec:reset_persistent}

The inner evaluator maintains a latent state $z$.
A key design choice is whether $z$ is reinitialized at each edit step or carried across steps:
\begin{itemize}
\item \textbf{Episodic-$z$ (primary setting):} The latent is reinitialized to $z_{\mathrm{init}}(x,y_t)$ at each edit step $t$.
The MDP state is $s=(x,y)\in\SSS_{\mathrm{plan}}=\XX\times\YY$, yielding Markov dynamics.
This variant admits cleaner analysis and is our main focus.
\item \textbf{Persistent-$z$:} The latent is carried across edits: the state is $\tilde s=(x,y,z)\in\tilde{\SSS}=\XX\times\YY\times\ZZ$.
This matches the original TRM architecture but complicates analysis because the latent's fixed point drifts as $y$ changes.
We provide an extension under a slow-drift condition in Appendix~\ref{app:persistent_latent}.
\end{itemize}

\longonly{
\begin{table}[t]
\caption{Comparison of episodic-$z$ and persistent-$z$ variants.}
\label{tab:variants}
\centering
\scriptsize
\resizebox{\linewidth}{!}{%
\begin{tabular}{@{}lll@{}}
\toprule
Property & Episodic-$z$ & Persistent-$z$ \\
\midrule
State space & $\SSS_{\mathrm{plan}} = \XX \times \YY$ & $\tilde{\SSS} = \XX \times \YY \times \ZZ$ \\
Latent init & $z^{(0)} = z_{\mathrm{init}}(x,y)$ & Carried: $z_0 \to z_1 \to \cdots$ \\
Markov? & Yes (in $(x,y)$) & Yes (in $(x,y,z)$) \\
Theory & Primary (simpler) & Via slow-drift \\
TRM match & Simplified & Original architecture \\
\bottomrule
\end{tabular}%
}
\end{table}
}

\subsection{TRM as unrolled generalized policy iteration}
\label{sec:gpi_view}

We attach a value head $V_\psi:\ZZ\times\XX\to\R$ on top of the latent.
In the episodic-$z$ setting, for $s=(x,y)$ we define
\begin{equation}
    z^{(n)}(s)
    =
    (\Pi_R \circ f_\theta)^{\circ n}\bigl(z_{\mathrm{init}}(x,y),y,x\bigr).
\end{equation}
Here $\Pi_R$ is the projection defined in~\eqref{eq:projection} and is applied at \emph{each} inner step to ensure forward-invariance:
\begin{equation}
\begin{split}
\Pi_R(z) &= \arg\min_{u:\|u\|_2\le R}\|u-z\|_2 \\
&=
\begin{cases}
z, & \|z\|_2 \le R,\\[2pt]
R\,\dfrac{z}{\|z\|_2}, & \|z\|_2 > R.
\end{cases}
\end{split}
\label{eq:projection}
\end{equation}
In particular, $\Pi_R(0)=0$.
If the projection is disabled, take $\Pi_R$ to be the identity map.

\noindent\emph{Convention (projection off).} Equation~\eqref{eq:projection} defines Euclidean projection for $R>0$; when projection is disabled we set $\Pi_R$ to the identity map (in experiments we denote this configuration by ``$R{=}0$ / proj.\ off'' as a sentinel, not as projection onto a radius-$0$ ball).

\emph{Interior projection fixed points.}
If a fixed point $z^*$ of the projected map $\tilde f_\theta = \Pi_R \circ f_\theta$ lies strictly inside the ball ($\|z^*\|_2 < R$), then the projection is inactive and $z^*$ also satisfies the unprojected fixed-point equation $f_\theta(z^*,y,x) = z^*$.
See Lemma~\ref{lem:interior_proj} in Appendix~\ref{app:proofs} for the precise statement and proof.

\begin{remark}[Latent projection vs.\ projected TD]
\label{rem:projection_distinction}
Our projection $\Pi_R$ is a \emph{latent-space Euclidean projection} applied inside the evaluator recursion to enforce forward-invariance of iterates $z^{(t)}$.
This is fundamentally different from the \emph{projected Bellman operator} in function-approximation TD, which projects value functions onto a function class and can cause divergence~\citep{Baird1995Residual,TsitsiklisVanRoy1997}.
Under the Euclidean norm, $\Pi_R$ is non-expansive (1-Lipschitz): if $f_\theta$ is $L_z$-Lipschitz in $z$, then $\Pi_R\circ f_\theta$ remains $L_z$-Lipschitz.
The latent projection preserves contraction and does not introduce the divergence risks associated with projected TD.
Our analysis concerns error propagation in evaluation$\rightarrow$improvement steps, not the learning dynamics of TD itself.
\end{remark}

The \emph{unrolled value} is defined as
\begin{equation}
    U_n(s)
    \triangleq
    V_\psi\bigl(z^{(n)}(s),x\bigr).
    \label{eq:Un_def}
\end{equation}
If the inner recursion (with per-step projection) were run to convergence, it would reach the unique fixed point
$z^*(x,y)$ of the \emph{actual per-step update map} $\tilde f_\theta(\cdot,y,x) := (\Pi_R\circ f_\theta)(\cdot,y,x)$.
If the fixed point lies strictly inside the projection ball ($\|z^*(x,y)\|_2 < R$), then projection is locally inactive and $z^*$ also satisfies the unprojected fixed-point equation $f_\theta(z^*,y,x)=z^*$ (Lemma~\ref{lem:interior_proj}).
The corresponding \emph{ideal latent value} is:
\begin{equation}
    U_*(s) = V_\psi\bigl(z^*(s),x\bigr).
    \label{eq:Ustar_def}
\end{equation}

\textbf{Absorbing-state extension.}
The definitions~\eqref{eq:Un_def} and~\eqref{eq:Ustar_def} apply to plan-space states $s=(x,y)\in\SSS_{\mathrm{plan}}$.
For consistency with sup-norms over $\SSS_{\mathrm{plan}}\cup\{s_{\mathrm{abs}}\}$, we extend both functions to the absorbing state:
\begin{equation}
U_n(s_{\mathrm{abs}}) := -C_{\max}, \qquad U_*(s_{\mathrm{abs}}) := -C_{\max}.
\label{eq:U_sabs_extension}
\end{equation}
This is consistent with the shaping convention $V^\pi(s_{\mathrm{abs}})=-C_{\max}$ for all $\pi$ (Appendix~\ref{app:shaping_convention}).
Throughout, sup-norms in Bellman operators are taken over $\mathcal{R}_\pi \cup \{s_{\mathrm{abs}}\}$ (or $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}} \cup \{s_{\mathrm{abs}}\}$ for advantages).

\longonly{
\begin{sloppypar}
\begin{remark}[Conceptual vs.\ computable quantities]
\label{rem:conceptual}
$U_*$ equals $V^\pi$ only if $(f_\theta, V_\psi)$ exactly matches the true value function; in general $\|U_* - V^\pi\|_\infty$ is an architectural approximation error.
The quantity $U_*$ is a \emph{conceptual tool} for separating approximation error from unrolling bias---we cannot compute it directly.
In practice, one monitors the empirical $K$-step residual of $U_n$ as a proxy: if the residual is small and $n$ is large (so $\|U_n - U_*\|$ is small by Proposition~\ref{prop:banach}), Lemma~\ref{lem:residual_transfer} implies the architectural residual is also small.
\end{remark}

The resulting picture is generalized policy iteration in plan-space: train $(f_\theta,V_\psi)$ so $U_n\approx V^\pi$, then update $\pi_\phi$ via advantages.
\end{sloppypar}
}% end of longonly for conceptual remark
}% end of longonly for detailed Section 2

\shortonly{
\section{Algorithm Sketch}
\label{sec:algorithm}
We alternate three phases: (i)~$K$-step bootstrapped value regression for $U_n$, (ii)~a policy improvement step producing a candidate $\pi_{\mathrm{cand}}$, and (iii)~a conservative mixture update $\pi_{\mathrm{new}}\!=\!(1{-}\alpha)\pi+\alpha\pi_{\mathrm{cand}}$ (with optional distillation).

\textbf{Phase 1: Value regression.}
We form $K$-step rollouts and define a target with target network $V_{\bar\psi}$:
\begin{equation}
G^{(K)}(s_0)
=
\sum_{k=0}^{K-1}\gamma^k r_k
+
\gamma^K V_{\bar\psi}(z^{(n)}(s_K),x).
\label{eq:GK_def}
\end{equation}
We minimize the bootstrapped value loss
$\mathcal{L}_{\mathrm{val}}(\psi,\theta)
=
\E_{s_0\sim\mu}\bigl[
   (V_\psi(z^{(n)}(s_0),x_0)
   -
   \sg(G^{(K)}(s_0)))^2
\bigr]$.

\textbf{Phase 2: Advantage estimation.}
We define $\widehat Q_V(s,a) = \E_{s'}[r(s,a,s') + \gamma V(s')]$ and the centered advantage $\widehat A_V(s,a) = \widehat Q_V(s,a) - B_V(s)$, where $B_V(s)=\E_{b\sim\pi}[\widehat Q_V(s,b)]$ yields exact statewise centering (Theorem~\ref{thm:monotone_with_error}); see Appendix~\ref{app:implementation} for implementation details.

\textbf{Phase 3: Conservative update.}
Form the candidate $\pi_{\mathrm{cand}}$ via policy gradient on $\widehat A$, then mix:
$\pi_{\mathrm{new}}=(1-\alpha)\pi+\alpha\pi_{\mathrm{cand}}$.
Distill back to a single network: $\pi_\phi \gets \arg\min_\phi \KL(\pi_{\mathrm{new}} \| \pi_\phi)$.
The CPI guarantee applies to the explicit mixture; distillation introduces approximation error not covered by the bound (see Remark~\ref{rem:distillation_penalty}).

\begin{remark}[Distillation penalty]
\label{rem:distillation_penalty}
To recover a formal guarantee for the distilled policy $\pi_\phi$, one can upper-bound the performance gap via
$\eta(\pi_{\mathrm{new}}) - \eta(\pi_\phi) \le \tfrac{2\gamma}{(1-\gamma)^2}\,\sup_{s}\|\pi_{\mathrm{new}}(\cdot|s) - \pi_\phi(\cdot|s)\|_{\mathrm{TV}}$.
We empirically monitor $\KL(\pi_{\mathrm{new}}\|\pi_\phi)$ on replay states sampled from the buffer during training (the same states used for the distillation loss).
Practitioners can evaluate with the true mixture $\pi_{\mathrm{new}}$ (no distillation) if strict guarantees are needed, treating distillation as optional compression.
\end{remark}

\textbf{Dials and knobs.}
Our bounds expose two TRM evaluator dials---$L_z$ (contraction modulus) and $n$ (unroll depth)---controlling truncation bias, and two standard algorithmic knobs---$K$ (bootstrap horizon) and $\alpha$ (mixture weight)---controlling residual amplification and conservativeness.
Full pseudocode and a summary table appear in Appendix~\ref{app:implementation}.

The theory in Sections~\ref{sec:contraction}--\ref{sec:cpi} analyzes a single evaluation$\rightarrow$improvement step of this template under idealized assumptions.}

\longonly{
\section{Algorithmic Template: UPI--TRM}
\label{sec:algorithm}

\textbf{Algorithm presentation note.}
Our main theoretical results (Sections~\ref{sec:value_error_bounds}--\ref{sec:cpi}) analyze the \emph{episodic-$z$} variant, where the latent $z$ is reinitialized at each edit step.
\Cref{alg:upi} is presented in the \emph{persistent-$z$} form (carrying $z$ across steps) because it matches the original TRM design.
Remark~\ref{rem:episodic_z_algo} clarifies the episodic-$z$ special case.
Corollary~\ref{cor:persistent_value_adv} extends bounds to persistent-$z$ under a slow-drift assumption.

\begin{algorithm}[t]
\small
\caption{UPI--TRM: single interaction + update step}
\label{alg:upi}
\begin{algorithmic}[1]
\STATE \textbf{Input:} $x, y_0, z_0, \gamma, n, K, \alpha, \tau, R$
\STATE Initialize $\tilde{s}_0 \gets (x,y_0,z_0)$; $\mathit{done} \gets \mathrm{false}$
\FOR{$k = 0$ to $K-1$}
  \IF{$\mathit{done}$}
    \STATE $r_k \gets (\gamma-1)C_{\max}$; $\tilde{s}_{k+1} \gets s_{\mathrm{abs}}$ \hfill \emph{// Absorbing self-loop}
  \ELSE
    \STATE $z_k^{(n)} \gets (\Pi_R \circ f_\theta)^{\circ n}(z_k,y_k,x)$ \hfill \emph{// $n$ inner steps}
    \STATE Sample $a_k \sim \pi_{\phi}(a \mid y_k,z_k^{(n)},x)$
    \STATE $y_{k+1} \gets \mathrm{edit}(y_k,a_k;x)$; receive $r_k$
    \STATE $z_{k+1} \gets z_k^{(n)}$ \hfill \emph{// Carry latent}
    \STATE $\tilde{s}_{k+1} \gets (x,y_{k+1},z_{k+1})$
    \IF{terminated}
     \STATE $\mathit{done} \gets \mathrm{true}$; $\tilde{s}_{k+1} \gets s_{\mathrm{abs}}$
    \ENDIF
  \ENDIF
\ENDFOR
\STATE Compute target $G^{(K)}$ via~\eqref{eq:GK_def}
\STATE Update $(\theta,\psi)$ by minimizing $\mathcal{L}_{\mathrm{val}}$~\eqref{eq:KBR_loss}
\STATE Form advantages $\widehat A$\longonly{ via~\eqref{eq:Ahat_def}}\shortonly{ (see Appendix~\ref{app:implementation})}
\STATE Policy gradient step $\to$ candidate $\pi_{\mathrm{cand}}$
\STATE $\pi_{\mathrm{new}} \gets (1-\alpha)\pi_{\phi} + \alpha\pi_{\mathrm{cand}}$
\STATE Distill: $\pi_\phi \gets \arg\min_\phi \KL(\pi_{\mathrm{new}} \| \pi_\phi)$
\STATE $\bar\psi \gets \tau \bar\psi + (1-\tau)\psi$
\end{algorithmic}
\end{algorithm}

The theory in Sections~\ref{sec:bellman}--\ref{sec:cpi} analyzes a single evaluation--improvement step of this template under idealized assumptions.

\begin{remark}[Episodic-$z$ special case]
\label{rem:episodic_z_algo}
In the episodic-$z$ variant, we skip the ``carry latent'' step (line 10). Instead, at the start of each loop iteration, we reinitialize $z_k \gets z_{\mathrm{init}}(x,y_k)$.
Our theoretical bounds in Sections~\ref{sec:value_error_bounds}--\ref{sec:cpi} apply directly; Corollary~\ref{cor:persistent_value_adv} extends to the persistent-$z$ case.
\end{remark}

\begin{remark}[Mapping dials to algorithm]
Table~\ref{tab:dial_mapping} connects the theoretical dials to Algorithm~\ref{alg:upi} parameters.
Lines 1--19 correspond to the theoretical GPI-with-mixture setting; the distillation in line 20 is essential for updating $\phi$ but lies outside our formal guarantee.
Practitioners can evaluate with the true mixture $\pi_{\mathrm{new}}$ (no distillation) if strict guarantees are needed, treating distillation as optional compression.
\end{remark}

\begin{table}[t]
\caption{Mapping dials to algorithm and theory.}
\label{tab:dial_mapping}
\centering
\footnotesize
\begin{tabular*}{\columnwidth}{@{\extracolsep{\fill}}lll@{}}
\toprule
Dial & Algorithm Line & Theorem \\
\midrule
$n$ (inner steps) & Line 7 & Prop.~\ref{prop:banach}\longonly{, Cor.~\ref{cor:epsA_dials}} \\
$K$ (horizon) & Line 3 loop & Prop.~\ref{prop:error_bounds} \\
$\alpha$ (mixture) & Line 19 & Thm.~\ref{thm:monotone_with_error} \\
$L_z$ (contraction) & Norm clamp & Assump.~\ref{ass:Lip} \\
$R$ (projection) & Line 7 & Def.~\ref{def:forward_invariant} \\
\bottomrule
\end{tabular*}
\end{table}

\subsection{Practical Details}
\label{sec:practical}

In practice we use replay buffers and mini-batches.
For advantage estimation we use either
(i)~the centered estimator\longonly{~\eqref{eq:Ahat_def}} $\widehat A = \widehat Q - B_V$ with an \emph{exactly computed} baseline $B_V(s)=\E_{b\sim\pi}[\widehat Q_V(s,b)]$ (tractable for discrete actions), preserving statewise centering $\E_{a\sim\pi}[\widehat A(s,a)]=0$---the regime of Theorem~\ref{thm:monotone_with_error} with $O(\alpha\varepsilon_{A,\mathrm{cand}})$ penalty---or
(ii)~a GAE-style estimator, where centering holds approximately and the guarantee degrades via $\varepsilon_{\mathrm{cent}}$ (Remark~\ref{rem:approx_centering}).

We also use:
norm control on $z{\to}z$ pathways (operator-norm clamping, per-layer scaling) to target $L_z<1$;
optional $z{\to}V$ norm control to bound~$L_V$;
explicit projection~\eqref{eq:projection} for forward-invariance;
empirical monitoring of $\hat L_z$, $\hat C_z$, $\widehat{L}_{z^*}$;
and distillation to update the policy network.

Table~\ref{tab:diagnostics} in Appendix~\ref{app:diagnostics} summarizes the main theoretical quantities and the practical diagnostics we log as proxies.

The horizon $K$ trades bias against variance: larger $K$ shrinks the coefficient $(1-\gamma^K)^{-1}$ (tightening the bound \emph{for a fixed/comparable $K$-step residual}) but can change the $K$-step residual itself and increases target variance.
The mixture coefficient $\alpha$ or TRPO-style KL trust-region size should be chosen conservatively relative to estimated evaluation error.
Adaptive schemes that increase $\alpha$ when empirical TD errors are small can accelerate learning while maintaining safety.

\textbf{Exact baseline for discrete actions.}
For Sudoku-style tasks with discrete edits (e.g., ``set cell $(i,j)$ to~$d$''), exact baseline summation is feasible.
For larger spaces, use action-value estimation or sampling-based baselines.}

\section{Stability via Contraction of the Inner Evaluator}
\label{sec:contraction}

\shortonly{
% Compressed Bellman section for ICML short version
The Bellman operator $\mathcal{T}^\pi$ and its $K$-step version $\mathcal{T}^\pi_K$ are standard~\citep{SuttonBarto2018,Puterman1994}.
The $K$-step operator is a $\gamma^K$-contraction with unique fixed point $V^\pi$.
We train the unrolled value $U_n$ by $K$-step bootstrapped regression, minimizing $\mathcal{L}_{\mathrm{val}}(\psi,\theta)=\E_{s_0\sim\mu}[(V_\psi(z^{(n)}(s_0),x_0)-\mathrm{sg}(G^{(K)}))^2]$ where $G^{(K)}=\sum_{k<K}\gamma^k r_k + \gamma^K \bar V(s_K)$.
We do not derive a general $L^2(\mu)\to\|\cdot\|_\infty$ conversion; our bounds should be read as conditional on controlling the sup-norm residuals over the relevant closure set (see Appendix~\ref{app:implementation} for practical training details).}

\longonly{
We now introduce the Bellman operators that underpin our value-error analysis. The definitions below are standard~\citep{SuttonBarto2018,Puterman1994}; our novelty lies in connecting these operators to the inner-loop contraction structure of TRM.

For a fixed edit policy $\pi$, the Bellman operator is
\begin{align}
   (\mathcal{T}^\pi V)(s)
   &=
   \E_{a\sim\pi(\cdot\mid s),\, s'\sim P(\cdot\mid s,a)}\Big[
      r(s,a,s') + \gamma V(s')
   \Big].
   \label{eq:Bellman_operator}
\end{align}
Note that $r$ here includes the potential-based shaping term from~\eqref{eq:shaping}.
The $K$-step operator is $\mathcal{T}^\pi_K = (\mathcal{T}^\pi)^K$:
\begin{equation}
 (\mathcal{T}^\pi_K V)(s)
 =
 \E\Biggl[
   \sum_{k=0}^{K-1}\gamma^k r_k
   + \gamma^K V(s_K)
 \Biggr].
 \label{eq:K_step_T}
\end{equation}
Note that $K=1$ recovers the usual one-step Bellman operator.

\begin{lemma}[Contraction of $\mathcal{T}^\pi_K$ (textbook)]
\label{lem:Kstep_contraction}
For $\gamma\in(0,1)$ and $K\ge 1$,
$\|\mathcal{T}^\pi_K V-\mathcal{T}^\pi_K W\|_\infty \le \gamma^K \|V-W\|_\infty$,
where $\|\cdot\|_\infty$ is taken over any state set closed under forward dynamics of $\pi$ containing $s_{\mathrm{abs}}$.
The unique fixed point is $V^\pi$.
\end{lemma}
\emph{This is a standard result~\citep{Denardo1967,Puterman1994}; we include it for completeness and to distinguish it from inner-loop contraction (Section~\ref{sec:contraction}). Proof in Appendix~\ref{app:proofs}.}
We include the absorbing state $s_{\mathrm{abs}}$ in the domain of the operator.
Under our shaping convention (shaping applied on all transitions), the absorbing self-loop reward is
$r(s_{\mathrm{abs}},a,s_{\mathrm{abs}})=(\gamma-1)C_{\max}$, which implies the fixed boundary value
$V^\pi(s_{\mathrm{abs}})=-C_{\max}$ for every policy $\pi$.
By Definition~\ref{def:reachable}, $\mathcal{R}_\pi$ is closed under transitions, so states reached during $K$-step rollouts remain in the domain and the contraction property holds on $\mathcal{R}_\pi\cup\{s_{\mathrm{abs}}\}$.

\subsection{Bootstrapped $K$-step training}
\label{sec:bootstrapped_training}

Given policy $\pi_\phi$, we train $(f_\theta,V_\psi)$ so that $U_n$ approximates $V^\pi$.
We form $K$-step rollouts and define a target with target network $V_{\bar\psi}$:
\begin{equation}
G^{(K)}(s_0)
=
\sum_{k=0}^{K-1}\gamma^k r_k
+
\gamma^K \, \bar V(s_K),
\label{eq:GK_def}
\end{equation}
where we define the bootstrap as
\[
\bar V(s)
=
\begin{cases}
V_{\bar\psi}\bigl(z^{(n)}(s),x\bigr), & s=(x,y)\in\SSS_{\mathrm{plan}},\\
-C_{\max}, & s=s_{\mathrm{abs}}.
\end{cases}
\]

\emph{Why $-C_{\max}$?}
Because we apply potential-based shaping on \emph{all} transitions including the absorbing self-loop, the shaped self-loop reward becomes $(\gamma-1)C_{\max}$, which implies the fixed boundary value $V^\pi(s_{\mathrm{abs}})=-C_{\max}$ for every policy.
This differs from the standard $V(s_{\mathrm{abs}})=0$ convention only by a constant shift of the potential, which leaves advantages and optimal policies unchanged; see Appendix~\ref{app:shaping_convention}.


\textbf{Termination convention.}
Rollouts are treated as always having length $K$ in the augmented MDP: if $s_t$ reaches $s_{\mathrm{abs}}$ at time $T < K$, then $s_{T+1}=\cdots=s_K=s_{\mathrm{abs}}$ and the absorbing self-loop reward $r(s_{\mathrm{abs}},a,s_{\mathrm{abs}})=(\gamma-1)C_{\max}$ is collected for the remaining steps.
Under this convention,~\eqref{eq:GK_def} remains valid as written, with $\bar V(s_{\mathrm{abs}})=-C_{\max}$ ensuring consistency with the shaping convention in Appendix~\ref{app:shaping_convention}.

We minimize the bootstrapped value loss
\begin{multline}
\mathcal{L}_{\mathrm{val}}(\psi,\theta)
=\\
\E_{s_0\sim\mu}\Bigl[
   \bigl(V_\psi\bigl(z^{(n)}(s_0),x_0\bigr)
   -
   \sg\bigl(G^{(K)}(s_0)\bigr)\bigr)^2
\Bigr].
\label{eq:KBR_loss}
\end{multline}}
\longonly{
\begin{remark}[$L^2$ loss vs.\ sup-norm bounds]
\label{rem:bootstrapped_vs_residual}
The loss $\mathcal{L}_{\mathrm{val}}$ is a bootstrapped MSE under sampling distribution $\mu$, not the squared Bellman residual $\E[(V-\mathcal{T}^\pi V)^2]$.
Our theoretical bounds use sup-norm residuals $\|U_n-\mathcal{T}_K^\pi U_n\|_{\infty,\pi}$ as \emph{structural} quantities.

Small $L^2(\mu)$ error implies small sup-norm error under favorable coverage conditions, up to function-class complexity / covering-number factors (omitted here), via standard uniform convergence and union bound arguments~\citep{Chen2019Concentrability}.
Concretely, if the replay distribution satisfies $C_\mu := \sup_s d_\pi(s)/\mu(s) < \infty$ (standard in off-policy actor-critic theory~\citep{Fujimoto2018TD3}), then $L^2(\mu)$ convergence provides guarantees that scale with $C_\mu$.
For large discrete spaces or continuous domains, this coverage condition may be stringent; our bounds then serve as structural guidance, with empirical TD errors as practical proxies.
\end{remark}

\begin{remark}[Target network lag]
\label{rem:target_network}
The theory assumes the Bellman backup uses a fixed target for analysis; in practice, slow updates to $V_{\bar\psi}$ (via Polyak averaging) help stability but introduce additional approximation error not explicitly modeled in our bounds.
This is standard practice in deep RL~\citep{Mnih2015DQN}.
\end{remark}
}

\shortonly{
% Compressed contraction section for ICML short version
We distinguish \emph{Bellman-operator contraction} ($\mathcal{T}^\pi_K$ is a $\gamma^K$-contraction---a standard MDP fact) from \emph{inner evaluator contraction} (the latent update $z\mapsto \tilde f_\theta(z,y,x)$ is an $L_z$-contraction with $L_z<1$---an architectural constraint).
Contractive $z\!\to\!z$ layers do \emph{not} imply TD convergence; they control truncation bias within each forward pass.

\begin{assumption}[Forward-invariant contraction]
\label{ass:Lip}
There exists a closed set $\mathcal{Z}_{\mathrm{inv}}\subseteq\ZZ$ such that the projected update map $\tilde f_\theta(z,y,x):=(\Pi_R\circ f_\theta)(z,y,x)$ maps $\mathcal{Z}_{\mathrm{inv}}$ to itself and is an $L_z$-contraction in $z$ with $L_z\in(0,1)$.
Let $z^*(x,y)$ denote the unique fixed point and define $C_z:=\sup_{(x,y)}\|\tilde f_\theta(z_{\mathrm{init}},y,x)-z_{\mathrm{init}}\|$.
\end{assumption}

We enforce Assumption~\ref{ass:Lip} via operator-norm clamping on $z\!\to\!z$ layers plus per-layer scaling; projection $\Pi_R$ ensures forward-invariance.
Value-head spectral normalization is disabled in stability-dial experiments to avoid confounds (Appendix~\ref{app:latent_collapse_diagnostic}).

\textbf{Scope of contraction analysis.}
The contraction regime analyzed here is a \emph{sufficient condition} for the stability and value-decomposition guarantees in Proposition~\ref{prop:error_bounds}---it is not a claim that TRM requires contraction to function.
The original TRM work~\citep{TRM2025} emphasizes that fixed-point convergence is not required for the model to reason effectively.
Our analysis targets the contractive subset of parameter space where rigorous bounds hold, and treats contraction as a tunable stability dial that practitioners can enforce (or relax) based on their depth-mismatch tolerance.}

\longonly{
\textbf{Two notions of contraction (operator vs.\ architecture).}
Our analysis involves two distinct contraction concepts that should not be conflated:
\begin{enumerate}
\item \emph{Bellman-operator contraction} (Lemma~\ref{lem:Kstep_contraction}): The $K$-step Bellman operator $\mathcal{T}^\pi_K$ is a $\gamma^K$-contraction on bounded value functions under $\|\cdot\|_\infty$. This is a standard MDP fact for a \emph{fixed} policy $\pi$, holding in function space independent of any parameterization.
\item \emph{Inner evaluator contraction} (Assumption~\ref{ass:Lip}): For fixed network parameters $\theta$, we require the latent update map $z\mapsto \tilde f_\theta(z,y,x)$ to be an $L_z$-contraction with $L_z<1$. This is an \emph{architectural constraint} on the $z\!\to\!z$ pathway, used to bound finite-unrolling bias and stabilize the inner recursion.
\end{enumerate}
\emph{What we do NOT claim:} Contractive $z\!\to\!z$ layers do \emph{not} imply that TD/projected-Bellman updates are contractive under function approximation.
The Bellman-operator contraction (item 1) is an operator-level statement underpinning error-propagation bounds---not a training guarantee.
The inner-loop contraction (item 2) controls truncation bias within each forward pass, but since $\theta$ changes at every SGD step, the fixed points move and no end-to-end convergence claim follows.

\textbf{Scope of contraction analysis.}
The contraction regime analyzed here is a \emph{sufficient condition} for the stability and value-decomposition guarantees in Proposition~\ref{prop:error_bounds}---it is not a claim that TRM requires contraction to function.
The original TRM work~\citep{TRM2025} emphasizes that fixed-point convergence is not required for the model to reason effectively.
Our analysis targets the contractive subset of parameter space where rigorous bounds hold, and treats contraction as a tunable stability dial that practitioners can enforce (or relax) based on their depth-mismatch tolerance.

We now state our contraction assumption, carefully addressing the circularity concern raised about policy-dependent reachable sets.

\subsection{Forward-invariant contraction region}

The key insight is to require contraction on a \emph{forward-invariant superset} of reachable states, independent of the policy.

\begin{definition}[Forward-invariant region]
\label{def:forward_invariant}
Let $\Pi_R:\ZZ\to\ZZ$ be a projection operator used after each inner update
(in our implementation, Euclidean projection onto a ball; see~\eqref{eq:projection}).
Define the \emph{per-step update map actually used in the unrolling} as
\[
\tilde f_\theta(z,y,x) := (\Pi_R \circ f_\theta)(z,y,x).
\]
A set $\mathcal{Z}_{\mathrm{inv}} \subseteq \ZZ$ is \emph{forward-invariant} for the inner recursion if:
(i) for all $z \in \mathcal{Z}_{\mathrm{inv}}$ and all $(x,y)$, we have $\tilde f_\theta(z,y,x) \in \mathcal{Z}_{\mathrm{inv}}$; and
(ii) for all $(x,y)$, the initialization $z_{\mathrm{init}}(x,y) \in \mathcal{Z}_{\mathrm{inv}}$.
\end{definition}


\begin{assumption}[Forward-invariant contraction]
\label{ass:Lip}
There exists a closed, bounded set $\mathcal{Z}_{\mathrm{inv}}\subseteq\ZZ$ and (optional) projection $\Pi_R:\ZZ\to\ZZ$ such that, for all $(x,y)$, the update map
\[
\tilde f_\theta(z,y,x):=(\Pi_R\circ f_\theta)(z,y,x)
\]
maps $\mathcal{Z}_{\mathrm{inv}}$ to itself and is an $L_z$-contraction in $z$ on $\mathcal{Z}_{\mathrm{inv}}$, with $L_z\in(0,1)$:
\[
\|\tilde f_\theta(z,y,x)-\tilde f_\theta(z',y,x)\|\le L_z\|z-z'\|,\quad \forall z,z'\in\mathcal{Z}_{\mathrm{inv}}.
\]
Let $z^*(x,y)$ denote the unique fixed point of $z\mapsto \tilde f_\theta(z,y,x)$.
We assume a uniform first-step bound from the initialization used in the paper:
\begin{equation}
C_z \coloneqq \sup_{(x,y)}\bigl\|\tilde f_\theta(z_{\mathrm{init}}(x,y),y,x)-z_{\mathrm{init}}(x,y)\bigr\|<\infty.
\label{eq:Cz_def}
\end{equation}
\end{assumption}

\begin{remark}[Interior fixed point for IFT analysis]
\label{rem:interior_fp}
If a nontrivial projection $\Pi_R$ is used and the fixed point satisfies $\|z^*(x,y;\theta)\|_2 < R$, then by Lemma~\ref{lem:interior_proj}, $f_\theta(z^*(x,y),y,x)=z^*(x,y)$ (the unprojected fixed-point equation).
Moreover, $\Pi_R$ equals the identity in a neighborhood of $z^*$, so $\partial_z\tilde f_\theta$ at $z^*$ equals $\partial_z f_\theta$ at $z^*$.
This condition is used for implicit-differentiation arguments in the persistent-$z$ extension (Appendix~\ref{app:persistent_latent}); it is \emph{not} required for Proposition~\ref{prop:error_bounds}.
\textbf{Caveat:} This condition is not guaranteed to hold throughout training as $\theta$ evolves; we treat it as a regularity condition verified post-hoc or enforced via output norm constraints on $f_\theta$.
\end{remark}

\begin{remark}[Projection vs.\ operator-norm control]
\label{rem:projection_vs_contraction}
While $L_z < 1$ is the theoretical ideal for contraction, in practice a tight projection radius ($R{=}10$ in our experiments) often dominates stability dynamics. Projection clips extreme latent magnitudes regardless of whether the underlying operator contracts, providing a complementary and often stronger stabilization mechanism. At $R{=}10$, projection is active 100\% of the time and post-projection $\hat{L}_z^{\text{post}} \approx 0.01$-$0.02$ regardless of target $L^*_z$. See Appendix~\ref{app:exp2_sweep} for ablations disentangling these effects.
\end{remark}

\subsection{When is the evaluator a contraction?}
\label{sec:when_contraction}

Assumption~\ref{ass:Lip} treats contraction as a design constraint, not a consequence of training.
We now give sufficient conditions and describe practical enforcement.

\textbf{Sufficient condition (Jacobian bound).}
If $f_\theta$ is differentiable in $z$, a sufficient condition for $L_z$-contraction is:
\begin{equation}
\sup_{(x,y),\, z\in\mathcal{Z}_{\mathrm{inv}}}\|\partial_z f_\theta(z,y,x)\|_{\mathrm{op}} \le L_z < 1.
\label{eq:jacobian_sufficient}
\end{equation}
By the mean value theorem, this implies $\|f_\theta(z,y,x)-f_\theta(z',y,x)\|\le L_z\|z-z'\|$ for all $z,z'\in\mathcal{Z}_{\mathrm{inv}}$.
When per-step projection $\Pi_R$ is applied, non-expansiveness of $\Pi_R$ (1-Lipschitz under Euclidean norm) preserves the contraction modulus.

\textbf{Practical enforcement and monitoring.}
We enforce~\eqref{eq:jacobian_sufficient} approximately via:
\begin{enumerate}
\item \emph{Operator-norm control on $z\!\to\!z$ layers:} Explicit clamping of linear layer weights (rescaling when $\|W\|_{\mathrm{op}}$ exceeds a threshold, with periodic re-clamping during training).
\item \emph{Per-layer output scaling:} A strict scalar contraction factor $\lambda<1$ applied after each layer to target $L_z<1$.
\item \emph{Per-step projection:} Euclidean projection $\Pi_R$ onto a ball ensures forward-invariance.
\end{enumerate}
We monitor the empirical proxy $\hat L_z$ on replay batches via finite differences:
\begin{equation}
\hat L_z = \max_{i\in\text{batch}} \frac{\|\tilde f_\theta(z_i,y_i,x_i) - \tilde f_\theta(z_i',y_i,x_i)\|}{\|z_i - z_i'\|}.
\label{eq:Lz_empirical}
\end{equation}

\textbf{Interpretation.}
Inner $z\!\to\!z$ contraction is a stability/expressivity trade-off: enforcing $L_z<1$ limits the latent dynamics but ensures geometric decay of unrolling bias.
Our bounds say: \emph{if} you enforce and monitor these criteria, \emph{then} truncation bias and safe improvement scale predictably with $(L_z,n)$ (TRM evaluator) and standard $(K,\alpha)$ choices.
\longonly{
\begin{remark}[Enforcing contraction and forward-invariance (implementation)]
\label{rem:contraction_practice}
We enforce Assumption~\ref{ass:Lip} approximately using (i) norm control on the $z\!\to\!z$ pathways together with an additional scalar contraction factor to obtain $L_z<1$, and (ii) per-step projection $\Pi_R$ onto a ball to guarantee forward-invariance.
\emph{Implementation caveat:} in our codebase, applying standard power-iteration \emph{spectral normalization} to the \emph{inner recursion} can be numerically unstable (the empirical local Lipschitz proxy for the latent update map can explode under tiny perturbations).
Accordingly, our current implementation uses a safer alternative for the $z\!\to\!z$ reasoning layers: explicit operator-norm clamping (periodic rescaling of linear weights using power-iteration estimates) followed by per-layer output scaling to induce strict contraction.
Appendix~\ref{app:sn_instability} gives a component-level diagnostic comparing these mechanisms, and Appendix~\ref{app:implementation} summarizes the broader implementation details and diagnostics we log.
A controlled diagnostic isolating $z\!\to\!z$ contraction from value-head normalization (Appendix~\ref{app:latent_collapse_diagnostic}) suggests that contraction causes modest latent clustering but not collapse-to-zero, and that earlier $\mathrm{Var}(V)$ confounds arose from value-head normalization.
\end{remark}}

\shortonly{Requiring $L_z<1$ globally may limit expressivity; one can use moderate contraction ($L_z\in[0.7,0.9]$) and compensate with larger $n$.}
\longonly{
\begin{remark}[Trade-off: expressivity vs.\ contraction]
\label{rem:expressivity_tradeoff}
Requiring $L_z < 1$ globally may limit the expressivity of $f_\theta$: effective latent dynamics for reasoning may require local expansion in some directions.
This is a genuine limitation of our analysis.
In practice, one can use a less aggressive contraction constraint (e.g., $L_z = 0.95$) and increase unrolling depth $n$ to compensate.
Our preliminary experiments (Section~\ref{sec:experiments}) suggest that moderate contraction ($L_z \in [0.7, 0.9]$) suffices for Sudoku-style tasks without severe expressivity loss.
For tasks where even mild contraction degrades performance, our episodic-$z$ analysis may be more suitable than attempting to enforce persistent-$z$ contraction.
\end{remark}}

\begin{proposition}[Fixed point and finite-unrolling bias (Banach)]
\label{prop:banach}
Under Assumption~\ref{ass:Lip}, for each $(x,y)$ the map $T_{x,y}(z)=\tilde f_\theta(z,y,x)$ has a unique fixed point $z^*(x,y) \in \mathcal{Z}_{\mathrm{inv}}$, with linear convergence:
\begin{equation}
\norm{z^{(t)} - z^*(x,y)}
\;\le\;
L_z^{\,t}\,\norm{z^{(0)} - z^*(x,y)}.
\label{eq:linear_convergence}
\end{equation}
For our initialization $z^{(0)} = z_{\mathrm{init}}(x,y)$:
\begin{equation}
\norm{z^{(n)}-z^*(x,y)}
\;\le\;
\frac{L_z^{\,n}}{1-L_z}\,C_z.
\label{eq:finite_unrolling_bias}
\end{equation}
\end{proposition}
\emph{This follows from Banach's fixed-point theorem (textbook); the bound~\eqref{eq:finite_unrolling_bias} makes the dependence on our initialization $C_z$ explicit. Proof in Appendix~\ref{app:proofs}.}

\longonly{
\begin{remark}[Trade-off: contraction strength vs.\ convergence speed]
\label{rem:tradeoff}
If $L_z$ is close to 1, the bound $\frac{L_z^n}{1-L_z}C_z$ becomes large, requiring more unrolling steps $n$ to achieve small bias.
Stronger contraction (smaller $L_z$) yields faster convergence but may limit the expressivity of the latent dynamics.
% This trade-off is a key design consideration.
\end{remark}}

We also assume the value head is Lipschitz in $z$:
\begin{equation}
\bigl|V_\psi(z,x)-V_\psi(z',x)\bigr|
\;\le\;
L_V\,\norm{z-z'},
\label{eq:LV_def}
\end{equation}
for all $z,z'\in\mathcal{Z}_{\mathrm{inv}}$ and $x\in\XX$.
We ensure this bound via norm control on the $z \to V$ layers (e.g., operator-norm control / output scaling), tying theory and architectural design.
Note: value-head Lipschitz control is architecturally separate from $z\!\to\!z$ contraction enforcement and does not affect the latter; we verified this empirically by toggling spectral normalization on the value head and observing no change in the measured $\hat L_z$ proxy.
In our implementation, PyTorch spectral normalization on the value head was empirically unstable (see Appendix~\ref{app:sn_instability}) and is \emph{disabled} in all stability-dial/isolation experiments to avoid confounds (Appendix~\ref{app:latent_collapse_diagnostic}).
\begin{assumption}[Bounded value head on the invariant region]
\label{ass:bounded_value_head}
There exists $V_{\max}<\infty$ such that for all $z\in\mathcal{Z}_{\mathrm{inv}}$ and all
$x \in \mathrm{supp}(\mathcal{D})$,
\[
|V_\psi(z,x)| \le V_{\max}
\qquad\text{and}\qquad
|V_{\bar\psi}(z,x)| \le V_{\max}.
\]
Consequently, the induced functions $U_n(s)=V_\psi(z^{(n)}(s),x)$ and
$U_*(s)=V_\psi(z^*(s),x)$ are bounded on $\SSS_{\mathrm{plan}}$,
so the sup-norm residuals used in Section~\ref{sec:value_error_bounds} are well-defined.
This assumption holds, for example, if $V_\psi$ is clipped at the output layer, or if
$\mathcal{Z}_{\mathrm{inv}}\times \mathrm{supp}(\mathcal{D})$ is compact and $V_\psi$ is continuous on that set.
\end{assumption}

\textbf{Persistent-$z$ (deferred).}
We primarily analyze the episodic-$z$ setting in the main text.
An extension to persistent-$z$ under a slow-drift condition is provided in Appendix~\ref{app:persistent_latent}.
}% end longonly for detailed Section 5 (Contraction)

\subsection{Scope of guarantees}
\label{sec:scope}

\textbf{What we analyze.}
Our guarantees are \emph{API/CPI-style}: we bound error propagation in a single evaluation$\rightarrow$improvement step, \emph{conditional} on bounded residual/advantage error and (approximately) enforced/monitored $L_z<1$.
In particular, Proposition~\ref{prop:error_bounds} bounds value error in terms of architectural residual plus finite-unrolling bias, and Theorem~\ref{thm:monotone_with_error} bounds per-step policy improvement given advantage-estimation error.
These are \emph{structural bounds}: they show how $(L_z,n)$ (TRM evaluator) and $(K,\alpha)$ (standard) influence safe improvement and what quantities to monitor (empirical residuals, $\hat L_z$, centering defect), but they are \emph{not} end-to-end training guarantees.

\textbf{What we do NOT prove.}
We do \emph{not} prove end-to-end convergence of SGD/TD/BPTT.
Our bounds are single-step API/CPI error-propagation statements at a fixed parameter snapshot; replay, bootstrapping, and distillation add distribution-shift effects not modeled here (see Appendix~\ref{app:implementation}).

\textbf{Why Bellman contraction is still relevant.}
Despite the above caveats, the Bellman-operator contraction\longonly{ (Lemma~\ref{lem:Kstep_contraction})} underpins our error-propagation bounds: it allows relating value error to the Bellman residual at a single snapshot of parameters, which is the structural quantity we optimize.
This operator-level statement does not depend on how $\theta$ is updated, and provides meaningful guidance even when end-to-end convergence is not guaranteed.

See Appendix~\ref{app:implementation} for implementation caveats including distillation.

\section{Error Bounds and Conservative Improvement}
\label{sec:error_bounds}

The Bellman operators and CPI bounds are standard~\citep{KakadeLangford2002}; our contribution is (i) the explicit unrolling-bias decomposition connecting TRM evaluator dials $(L_z, n)$ to value error, and (ii) showing how this decomposition interacts with CPI mixture updates. The $O(\alpha)$ scaling itself follows from standard CPI with centering; our novelty is exposing the TRM-specific structure.
We connect latent recursion to approximate evaluation and conservative policy improvement.
\shortonly{We focus on episodic-$z$ analysis; the persistent-$z$ extension is in \Cref{app:persistent_latent}.}
\longonly{\textbf{Sections~\ref{sec:value_error_bounds}--\ref{sec:cpi} present the episodic-$z$ analysis; \Cref{sec:persistent_extension} extends to persistent-$z$ via $\Cdrift(n)$.}}

The results below build on the contraction framework of \Cref{sec:contraction}; each statement specifies its required conditions explicitly.
These conditions---bounded rewards (\Cref{ass:bounded}), bounded value head (\Cref{ass:bounded_value_head}), forward-invariant contraction (\Cref{ass:Lip}), Lipschitz value head, and uniform advantage error (\Cref{ass:Ahat_error_bound})---are idealized; in practice they are enforced approximately via norm control (clamping/scaling), target networks, projection, and monitoring.
The persistent-$z$ extension (\Cref{app:persistent_latent}) additionally requires the slow-drift condition (\Cref{ass:twotimescale}).

\subsection{Value error bounds (episodic-$z$)}
\label{sec:value_error_bounds}

We first define the architectural residual that will appear in our bounds:
\begin{equation}
\varepsilon_{\mathrm{res}}^* \triangleq \|U_* - \mathcal{T}^\pi_K U_*\|_{\infty,\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}.
\label{eq:eps_res_def}
\end{equation}
This measures how well the ideal fixed-point value $U_*$ satisfies the Bellman equation.
While $\varepsilon_{\mathrm{res}}^*$ depends on the ideal fixed-point $U_*$, it can be upper-bounded using the measurable residual of $U_n$ plus the truncation gap; see Lemma~\ref{lem:residual_transfer}.

\begin{proposition}[Value-error decomposition]
\label{prop:error_bounds}
Let $\gamma\in(0,1)$ and $K\ge 1$.
\begin{enumerate}
\item For any bounded $V:\mathcal{S}_{\mathrm{plan}}\cup\{s_{\mathrm{abs}}\}\to\R$, by the contraction property of $\mathcal{T}^\pi_K$\longonly{ (Lemma~\ref{lem:Kstep_contraction})}:
\begin{equation}
\|V - V^\pi\|_{\infty,\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}
\le
\frac{1}{1-\gamma^K}\,
\|V-\mathcal{T}^\pi_K V\|_{\infty,\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}\,.
\label{eq:boundA}
\end{equation}
\item Under Assumption~\ref{ass:Lip} and Lipschitz value head ($|V_\psi(z,x)-V_\psi(z',x)|\le L_V\|z-z'\|$), for the unrolled value $U_n(s)=V_\psi(z^{(n)}(s),x)$ with initialization $z^{(0)} = z_{\mathrm{init}}(x,y)$:
\begin{align}
\|U_n - V^\pi\|_{\infty,\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}
\le
\underbrace{\frac{\varepsilon_{\mathrm{res}}^*}{1-\gamma^K}}_{\text{arch.}}
+
\underbrace{L_V\,\frac{L_z^{\,n}}{1-L_z}\,C_z}_{\text{unroll.}}.
\label{eq:epsV_dials}
\end{align}
\end{enumerate}
\end{proposition}

\begin{remark}[Episodic-z fixed point]
\label{rem:episodic_z_fp}
In episodic-z, the latent $z$ is reinitialized to $z_{\text{init}}(x,y)$ at each edit step. The ``ideal'' value $U^*(s)$ represents the value obtained by unrolling from $z_{\text{init}}$ to convergence at each visited state $s=(x,y)$, i.e., $U^*(s) = V_\psi(\lim_{n\to\infty} (\Pi_R \circ f_\theta)^n(z_{\text{init}}, y, x), x)$. While actual trajectories reset $z$ before convergence, $U^*$ serves as an approximation target for $U_n$, and the Bellman residual $\varepsilon^*_{\text{res}} = \|U^* - \mathcal{T}^\pi_K U^*\|_\infty$ measures expressivity of the architecture under episodic dynamics.
\end{remark}

\emph{Part (i) is a standard consequence of Bellman-operator contraction on bounded functions~\citep{Puterman1994,daley2025multistep}. It does not assume linear function approximation. We state it in function space ($V$ is any bounded function), not in a projected-TD sense.}
The proof (Appendix~\ref{app:proofs}) decomposes $\|U_n - V^\pi\|_\infty \le \|U_* - V^\pi\|_\infty + \|U_n - U_*\|_\infty$, applies part (i) to $U_*$, and uses Lipschitzness with the unrolling bound from Proposition~\ref{prop:banach} to obtain the value-error bound~\eqref{eq:epsV_dials}.

\begin{remark}[Practical $L_V$ control]
\label{rem:LV_practical}
The Lipschitz bound $L_V$ is an idealized assumption. In our experiments, we disable value-head spectral normalization to avoid training instability (Appendix~\ref{app:latent_collapse_diagnostic}), so $L_V$ may be large in practice. 
\textbf{Implication:} When $L_V$ is uncontrolled, the bound~\eqref{eq:epsV_dials} provides only qualitative structural guidance (showing \emph{which} quantities matter), not quantitative guarantees. Practitioners should monitor empirical value instability $\Delta_V$ as a proxy.
\end{remark}
\longonly{The corresponding advantage-error bound is obtained by combining~\eqref{eq:epsV_dials} with Lemma~\ref{lem:A_error}, yielding Corollary~\ref{cor:epsA_dials}.}%
\shortonly{The corresponding advantage-error bound (deferred to the appendix) follows from a similar argument.}

This decomposition is central: the first term $\varepsilon_{\mathrm{res}}^*$ reflects how well the \emph{ideal} fixed-point value $U_*$ can approximate $V^\pi$ (an architectural/expressivity property), while the second term is finite-unrolling bias that decays geometrically with $n$.

\longonly{
The \emph{coefficient} $(1-\gamma^K)^{-1}$ decreases with $K$ and satisfies $(1-\gamma^K)^{-1} \le (1-\gamma)^{-1}$.
Thus, \emph{holding the $K$-step residual} $\|V-\mathcal{T}_K^\pi V\|_\infty$ fixed (or comparable across $K$), using a larger $K$ tightens the bound.
However, the residual itself depends on $K$ (e.g., $\varepsilon_{\mathrm{res}}^*(K)=\|U_*-\mathcal{T}_K^\pi U_*\|_\infty$) and need not decrease monotonically with $K$.
In practice, increasing $K$ also increases the \emph{variance} of the rollout return $G^{(K)}$, necessitating a bias-variance trade-off not captured by sup-norm analysis.}

\begin{lemma}[Residual transfer]
\label{lem:residual_transfer}
For any bounded $U_n,U_*$:
\begin{equation}
\begin{split}
\varepsilon_{\mathrm{res}}^*
&\le
\|U_n - \mathcal{T}^\pi_K U_n\|_{\infty,\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}} \\
&\quad +
(1+\gamma^K)\,\|U_n-U_*\|_{\infty,\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}.
\end{split}
\end{equation}
\end{lemma}
\emph{Proof: triangle inequality and contraction; see Appendix~\ref{app:proofs}.}
\longonly{This shows that if $U_n$ has small $K$-step residual and is close to $U_*$ on $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}$, then the architectural residual is also small.}

\shortonly{
\textbf{From value error to advantage error.}
The same value-error bound implies an advantage-error bound (up to a constant factor depending on the estimator); full statements are deferred to the appendix.}

\longonly{
\subsection{From Value Error to Advantage Error}
\label{sec:adv_error}

For an approximate value function $V$, define the \emph{one-step} action-value estimate and the corresponding centered \emph{one-step} advantage estimator:
\begin{align}
\widehat Q_V(s,a) &= \E_{s'\sim P(\cdot\mid s,a)}[r(s,a,s') + \gamma V(s')], \\
\widehat A_V(s,a) &= \widehat Q_V(s,a) - \underbrace{\E_{b\sim\pi(\cdot\mid s)}[\widehat Q_V(s,b)]}_{\text{baseline } B_V(s)}.
\label{eq:Ahat_def}
\end{align}
Note that $\E_{a \sim \pi}[\widehat A_V(s,a)] = 0$ by construction, mirroring the centering property of true advantages.

\begin{remark}[Centering and baseline computation]
\label{rem:baseline}
The $O(\alpha\,\varepsilon_{A,\mathrm{cand}})$ tightening in Theorem~\ref{thm:monotone_with_error}
relies on the \emph{statewise centering} property
\[
\E_{a\sim\pi(\cdot\mid s)}[\widehat A(s,a)] = 0 \qquad \text{for all } s.
\]
This holds identically when $\widehat A$ is defined as in~\eqref{eq:Ahat_def} with the baseline computed exactly as
$B_V(s)=\E_{b\sim\pi(\cdot\mid s)}[\widehat Q_V(s,b)]$ (e.g., by summation over a finite action space).

If the baseline is approximated, centering may fail. We quantify this by the \emph{centering defect}
\[
\varepsilon_{\mathrm{cent}}
:=
\sup_{s\in\mathcal{R}_\pi}\left|\E_{a\sim\pi(\cdot\mid s)}[\widehat A(s,a)]\right|.
\]
Theorem~\ref{thm:monotone_with_error} corresponds to the ideal case $\varepsilon_{\mathrm{cent}}=0$;
see Remark~\ref{rem:approx_centering} for the general bound with $\varepsilon_{\mathrm{cent}}>0$.
\end{remark}

\begin{lemma}[Value to one-step advantage error]
\label{lem:A_error}
Let $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}$ be the advantage-evaluation closure from Definition~\ref{def:adv_closure},
and let $\delta_V := \sup_{s\in \mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}} |V(s)-V^\pi(s)|$.
Then for all $(s,a)$ with $s\in\mathcal{R}_\pi$ and $a\in \mathrm{supp}(\pi(\cdot|s))\cup\mathrm{supp}(\pi_{\mathrm{cand}}(\cdot|s))$,
\[
|\widehat A_V(s,a) - A^\pi(s,a)| \le 2\gamma\,\delta_V.
\]
\end{lemma}

The factor $2\gamma$ is tight worst-case (error appears in both $Q$ and baseline).
In practice, errors in $\widehat Q_V(s,a)$ and the baseline $\E_\pi[\widehat Q_V]$ often have the same sign and partially cancel.
\begin{remark}[Extensions to $K$-step and GAE-style advantages]
\label{rem:A_error_kstep}
Lemma~\ref{lem:A_error} is stated for the \emph{one-step} estimator~\eqref{eq:Ahat_def}, hence the factor $2\gamma$.
If instead one defines a $K$-step action-value estimate
\begin{multline*}
\widehat Q_V^{(K)}(s,a) := \\
\E\biggl[\sum_{t=0}^{K-1}\gamma^t r_t + \gamma^K V(s_K) \biggm| s_0=s, a_0=a, \pi\biggr],
\end{multline*}
and the corresponding centered advantage
$\widehat A_V^{(K)}(s,a):=\widehat Q_V^{(K)}(s,a)-\E_{b\sim\pi(\cdot|s)}[\widehat Q_V^{(K)}(s,b)]$
(with the baseline computed exactly), then the same argument yields
\[
|\widehat A_V^{(K)}(s,a)-A^\pi(s,a)| \le 2\gamma^K\,\delta_V.
\]
In practice we may use $\lambda$-return / GAE-style estimators (weighted mixtures of multi-step returns), which admit analogous bounds with a coefficient depending on $(\gamma,\lambda)$; throughout, we absorb such estimator-dependent constants into the uniform advantage error parameter $\varepsilon_A$ in Assumption~\ref{ass:Ahat_error_bound}.
\end{remark}

\begin{corollary}[Advantage error in terms of dials]
\label{cor:epsA_dials}
Under Assumptions~\ref{ass:Lip} and~\eqref{eq:LV_def}, combining Proposition~\ref{prop:error_bounds} with Lemma~\ref{lem:A_error} yields:
\begin{align}
\varepsilon_A
&\le
\underbrace{\frac{2\gamma\,\varepsilon_{\mathrm{res}}^*}{1-\gamma^K}}_{\text{architectural}}
+
\underbrace{2\gamma\,L_V\,\frac{L_z^{\,n}}{1-L_z}\,C_z}_{\text{unrolling}}.
\label{eq:epsA_dials}
\end{align}
Here $\varepsilon_A$ bounds the advantage error on $\mathcal{R}_\pi$ for actions
$a\in \mathrm{supp}(\pi(\cdot|s))\cup\mathrm{supp}(\pi_{\mathrm{cand}}(\cdot|s))$,
and the intermediate value-error quantity in Lemma~\ref{lem:A_error} is taken over the advantage-evaluation closure $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}$ (Definition~\ref{def:adv_closure}).
\end{corollary}}

\longonly{
\begin{remark}[Concentrability-adjusted bound]
\label{rem:concentrability}
To make the distribution-shift cost explicit, suppose the replay distribution $\mu$ satisfies:
\begin{equation}
C_\mu^{\{\pi,\pi_{\mathrm{cand}}\}} := \!\sup_{s \in \mathcal{R}_{\{\pi, \pi_{\mathrm{cand}}\}}}\! \frac{\max(d_\pi(s), d_{\pi_{\mathrm{cand}}}(s))}{\mu(s)} < \infty.
\label{eq:concentrability}
\end{equation}
If the value function achieves $L^2(\mu)$ error $\delta_\mu^2 := \E_{s \sim \mu}[(U_n(s) - V^\pi(s))^2]$, then standard uniform convergence arguments~\citep{Chen2019Concentrability} yield, with high probability over training data:
\[
\varepsilon_A \lesssim \sqrt{C_\mu^{\{\pi,\pi_{\mathrm{cand}}\}}} \cdot \delta_\mu + \text{(covering-number terms)}.
\]
The concentrability coefficient $C_\mu$ quantifies coverage: if $\mu$ has poor support on states reachable under $\pi_{\mathrm{cand}}$, the bound degrades.
This is why off-policy replay with diverse exploration is essential.
We do not formalize the covering-number dependence, as it requires specifying the function class; the bound above provides structural intuition.
\end{remark}}

This makes explicit how $(L_z, n, K)$ and architectural factors shape advantage error.
The architectural residual $\varepsilon_{\mathrm{res}}^*$ depends on the expressivity of $(f_\theta, V_\psi)$, while the unrolling term is controlled by the dials $(L_z, n)$.

\shortonly{
\subsection{Conservative policy improvement}
\label{sec:cpi}

For conservative mixture updates $\pi_{\mathrm{new}}=(1-\alpha)\pi+\alpha\pi_{\mathrm{cand}}$, the standard CPI bound~\citep{KakadeLangford2002} guarantees monotonic improvement up to an $O(\alpha^2)$ penalty.
The $O(\alpha)$ scaling for mixture policies is standard in CPI~\citep{KakadeLangford2002}; our contribution is combining it with the value decomposition (Proposition~\ref{prop:error_bounds}) to make explicit how TRM evaluator dials $(L_z, n)$ affect the safe-improvement threshold.
With centered advantage estimators (which hold exactly for discrete action spaces with exact baseline computation), evaluation error contributes as $O(\alpha\,\varepsilon_{A,\mathrm{cand}})$ rather than $O(\varepsilon_A)$, and the value decomposition shows how $(L_z,n)$ and $(K,\alpha)$ together shape safe improvement.
Full theorem statement and derivation are in Appendix~\ref{app:cpi_derivation}.}

\longonly{
\subsection{Conservative policy improvement}
\label{sec:cpi}

Performance is $\eta(\pi) = \E_{s_0\sim\rho}[V^\pi(s_0)]$.
The local surrogate~\citep{KakadeLangford2002} is:
\begin{equation}
L_\pi(\pi') = \eta(\pi) + \frac{1}{1-\gamma}\,\E_{s\sim d_\pi, a\sim\pi'(\cdot\mid s)}[A^\pi(s,a)].
\end{equation}

We consider mixture updates:
\begin{equation}
\pi_{\mathrm{new}} = (1-\alpha)\pi + \alpha\pi_{\mathrm{cand}}, \quad \alpha\in[0,1].
\label{eq:pi_mixture}
\end{equation}

The classical CPI bound for such mixtures~\citep{KakadeLangford2002} gives:
\begin{equation}
\eta(\pi_{\mathrm{new}}) \ge L_\pi(\pi_{\mathrm{new}}) - \frac{2\varepsilon_{\mathrm{CPI}}\,\gamma}{(1-\gamma)^2}\,\alpha^2,
\label{eq:cpi_bound}
\end{equation}
where $\varepsilon_{\mathrm{CPI}} = \max_s|\E_{a\sim\pi_{\mathrm{cand}}}A^\pi(s,a)|$.
A simple uniform bound gives $\varepsilon_{\mathrm{CPI}} \le 2R_{\max}/(1-\gamma)$ (see~\eqref{eq:value_adv_bounds_from_Rmax}), which is loose but sufficient for our structural analysis.
In practice, $\varepsilon_{\mathrm{CPI}}$ can be much smaller if advantages are moderate.
A derivation specializing CPI to mixture policies, including a proof of the required occupancy-measure coupling bound, is provided in Appendix~\ref{app:cpi_derivation}.}
\longonly{
\begin{remark}[Norm convention and the factor $2$ in the CPI penalty]
\label{rem:cpi_norm_convention}
In Appendix~\ref{app:cpi_derivation} we bound distribution shift using the $\ell_1$ distance between action distributions, i.e., $\|\pi'(\cdot|s)-\pi(\cdot|s)\|_1$.
Since $\pi'(\cdot|s)$ and $\pi(\cdot|s)$ are probability vectors, $\|\pi'(\cdot|s)-\pi(\cdot|s)\|_1 \le 2$ for all $s$.
For the mixture $\pi_{\mathrm{new}}=(1-\alpha)\pi+\alpha\pi_{\mathrm{cand}}$, this yields $\|\pi_{\mathrm{new}}(\cdot|s)-\pi(\cdot|s)\|_1 \le 2\alpha$ and hence the $\alpha^2$ scaling in~\eqref{eq:cpi_bound}.
If one instead works with total-variation distance $D_{\mathrm{TV}}(p,q)=\tfrac12\|p-q\|_1$, the same derivation holds with corresponding constant changes.
\end{remark}}

\begin{assumption}[Uniform advantage error on states visited by $\pi$]
\label{ass:Ahat_error_bound}
For some $\varepsilon_A\ge 0$:
\begin{equation}
|\widehat{A}(s,a) - A^\pi(s,a)| \le \varepsilon_A
\end{equation}
for all $(s,a)$ with $s \in \mathcal{R}_{\pi}$ and
$a \in \mathrm{supp}(\pi_{\mathrm{cand}}(\cdot|s)) \cup \mathrm{supp}(\pi(\cdot|s))$.
\end{assumption}

\shortonly{The uniform advantage error $\varepsilon_A$ is a structural placeholder subsuming architectural residual, finite-sample error, and coverage factors. In practice, we monitor empirical TD errors; the guarantees provide structural guidance.

\textbf{Warning:} Converting $L^2(\mu)$ training loss to sup-norm bounds over $\mathcal{R}_\pi$ requires concentrability coefficients $C_\mu := \sup_s d_\pi(s)/\mu(s)$ that can be exponentially large in state dimension; our bounds should be interpreted as structural guidance rather than quantitative guarantees in high-dimensional settings.}
\longonly{
\begin{remark}[On the strength of this assumption]
\label{rem:assumption_strength}
A uniform sup-norm bound on $|\widehat{A}(s,a)-A^\pi(s,a)|$ for all $s \in \mathcal{R}_{\pi}$ and
$a \in \mathrm{supp}(\pi(\cdot|s)) \cup \mathrm{supp}(\pi_{\mathrm{cand}}(\cdot|s))$
is strong and not directly verifiable in high-dimensional spaces.
We treat $\varepsilon_A$ as a \emph{structural placeholder} that subsumes three sources of error:
\begin{enumerate}
\item \textbf{Architectural residual:} The terms in Corollary~\ref{cor:epsA_dials} controlled by dials $(L_z, n, K)$.
\item \textbf{Finite-sample / function-approximation error:} Converting $L^2(\mu)$ training loss to sup-norm bounds requires uniform convergence with covering-number factors (omitted here).
\item \textbf{One-step deviation coverage (the real scope issue):}
Corollary~\ref{cor:epsA_dials} bounds the advantage error at states $s\in\mathcal{R}_\pi$ for actions in
$\mathrm{supp}(\pi(\cdot|s))\cup\mathrm{supp}(\pi_{\mathrm{cand}}(\cdot|s))$, which is exactly the domain required in Theorem~\ref{thm:monotone_with_error} because the CPI surrogate is evaluated under $d_\pi$.
However, controlling $|\widehat{A}(s,a)-A^\pi(s,a)|$ for such pairs requires value-function accuracy at next states reached by taking candidate actions $a\sim\pi_{\mathrm{cand}}(\cdot|s)$ from $s\sim d_\pi$.
This is why our value-error bounds are taken over the advantage-evaluation closure $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}$ (Definition~\ref{def:adv_closure}).
When value learning is off-policy under a replay distribution $\mu$, extending $L^2(\mu)$ control to a sup-norm bound over $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}$ introduces a concentrability/coverage coefficient. This coefficient compares $\mu$ to the occupancy of this closure (see Remark~\ref{rem:concentrability}).
\end{enumerate}
In practice, we have $L^2(\mu)$ control under replay distribution $\mu$, not uniform bounds.
We monitor empirical TD errors and advantage magnitudes to gauge the scale of $\varepsilon_A$.
Our guarantees describe an idealized regime. Practitioners should interpret them as structural guidance showing \emph{how} the dials influence safe improvement, not as end-to-end certificates.

\textbf{High-probability variant:}
The same proof structure applies if the uniform bound in Assumption~\ref{ass:Ahat_error_bound} is replaced by a high-probability (or high-quantile) bound under the on-policy state distribution $s\sim d_\pi$ (and $a\sim \pi_{\mathrm{cand}}(\cdot|s)$).
This yields a probabilistic improvement guarantee, which is the appropriate interpretation in practice.
\end{remark}}

\begin{theorem}[CPI with centered evaluation error]
\label{thm:monotone_with_error}
Let $\pi_{\mathrm{new}}=(1-\alpha)\pi+\alpha\pi_{\mathrm{cand}}$ with $\alpha\in[0,1]$ and suppose Assumption~\ref{ass:Ahat_error_bound} holds.
Assume the updated policy is the \emph{exact mixture} $\pi_{\mathrm{new}}=(1-\alpha)\pi+\alpha\pi_{\mathrm{cand}}$ (no distillation error).
\textbf{Practical scope:} The exact centering condition~\eqref{eq:centering_condition} is satisfied when the baseline $B_V(s)$ is computed exactly via summation over discrete actions. Our experiments use GAE-style baselines with approximate centering; quantitative applicability requires measuring the centering defect $\varepsilon_{\mathrm{cent}}$.
Assume the advantage estimator is \emph{statewise-centered under $\pi$}:
\begin{equation}
\E_{a\sim\pi(\cdot\mid s)}[\widehat A(s,a)] = 0
\qquad\text{for all } s\in\mathcal{R}_\pi.
\label{eq:centering_condition}
\end{equation}
We define the candidate-policy bias in advantage estimation
\begin{equation}
\varepsilon_{A,\mathrm{cand}}
\triangleq
\sup_{s \in \mathcal{R}_{\pi}}
\left|
\E_{a\sim\pi_{\mathrm{cand}}(\cdot\mid s)}\!\left[\widehat{A}(s,a)-A^\pi(s,a)\right]
\right|.
\label{eq:epsA0_def}
\end{equation}
Then:
\begin{equation}
\eta(\pi_{\mathrm{new}})
\ge
\widehat{L}_\pi(\pi_{\mathrm{new}})
-
\frac{\alpha}{1-\gamma}\,\varepsilon_{A,\mathrm{cand}}
-
\frac{2\varepsilon_{\mathrm{CPI}}\,\gamma}{(1-\gamma)^2}\,\alpha^2,
\label{eq:main_theorem}
\end{equation}
where $\widehat{L}_\pi(\pi') = \eta(\pi) + \frac{1}{1-\gamma}\E_{s\sim d_\pi, a\sim\pi'(\cdot\mid s)}[\widehat{A}(s,a)]$.
\end{theorem}

\begin{remark}[Centering in practice]
\label{rem:centering_practice}
The $O(\alpha\,\varepsilon_{A,\mathrm{cand}})$ bound assumes exact statewise centering $\E_{a\sim\pi}[\widehat{A}(s,a)] = 0$ for all $s$. Using GAE or batch-level centering introduces a \emph{Centering Defect} $\varepsilon_{\mathrm{cent}} := \sup_s |\E_{a\sim\pi}[\widehat{A}(s,a)]|$, which degrades the guarantee to $O(\alpha\,\varepsilon_{A,\mathrm{cand}} + \varepsilon_{\mathrm{cent}})$. Our experiments use approximate centering, so the tighter bound serves as structural guidance rather than a strict guarantee.
\end{remark}

\emph{The backbone is CPI~\citep{KakadeLangford2002}; our observation is that for mixture updates with centered estimators, evaluation error contributes $O(\alpha\varepsilon_{A,\mathrm{cand}})$ rather than $O(\varepsilon_A)$.
Centering holds exactly when $\widehat A(s,a)=\widehat Q(s,a) - \E_{b\sim\pi}[\widehat Q(s,b)]$ with exact baseline computation (feasible for small discrete action spaces).}

\longonly{
\begin{remark}[Approximate centering]
\label{rem:approx_centering}
If the estimator is not exactly centered, define the centering defect
\[
\varepsilon_{\mathrm{cent}}
:=
\sup_{s\in\mathcal{R}_\pi}\left|\E_{a\sim\pi(\cdot\mid s)}[\widehat A(s,a)]\right|.
\]
The same proof as Theorem~\ref{thm:monotone_with_error} yields the more general bound
\begin{multline*}
\eta(\pi_{\mathrm{new}})
\ge
\widehat{L}_\pi(\pi_{\mathrm{new}})
- \frac{1}{1-\gamma}\Bigl((1-\alpha)\varepsilon_{\mathrm{cent}}+\alpha\varepsilon_{A,\mathrm{cand}}\Bigr) \\
- \frac{2\varepsilon_{\mathrm{CPI}}\,\gamma}{(1-\gamma)^2}\,\alpha^2.
\end{multline*}
In particular, Theorem~\ref{thm:monotone_with_error} corresponds to $\varepsilon_{\mathrm{cent}}=0$.
\end{remark}}

\textbf{The role of $\alpha$.}
The evaluation-error penalty scales as $\alpha \varepsilon_{A,\mathrm{cand}} / (1-\gamma)$, where $\varepsilon_{A,\mathrm{cand}}$ is the
candidate-policy expected advantage estimation bias from~\eqref{eq:epsA0_def}.
This $O(\alpha)$ reduction occurs because the estimator is statewise-centered under the current policy:
$\E_{a \sim \pi(\cdot\mid s)}[\widehat{A}(s,a)] = 0$, so for the mixture $\pi_{\mathrm{new}}=(1-\alpha)\pi+\alpha\pi_{\mathrm{cand}}$, only the $\alpha\pi_{\mathrm{cand}}$ component contributes nonzero expected estimated advantage.
\longonly{A sufficient condition for exact centering is exact computation of the baseline in~\eqref{eq:Ahat_def} (Remark~\ref{rem:baseline}).}%
\shortonly{A sufficient condition is exact baseline computation (e.g., summation over a discrete action space).}

\shortonly{
\textbf{Approximate centering (centering defect).}
When the baseline is approximated (e.g., via GAE), exact centering may fail.
Define the \emph{centering defect} $\varepsilon_{\mathrm{cent}} := \sup_{s\in\mathcal{R}_\pi}|\E_{a\sim\pi(\cdot\mid s)}[\widehat A(s,a)]|$.
The bound generalizes to:
\[
\begin{aligned}
\eta(\pi_{\mathrm{new}})
&\ge
\widehat{L}_\pi(\pi_{\mathrm{new}})
- \frac{(1-\alpha)\varepsilon_{\mathrm{cent}}+\alpha\varepsilon_{A,\mathrm{cand}}}{1-\gamma} \\
&\quad
- \frac{2\varepsilon_{\mathrm{CPI}}\gamma\,\alpha^2}{(1-\gamma)^2}.
\end{aligned}
\]
Theorem~\ref{thm:monotone_with_error} is the special case $\varepsilon_{\mathrm{cent}}=0$.
This shows that centered advantage estimation is important but not critical: the bound degrades gracefully with the centering defect.
}%
\longonly{When centering is only approximate, the bound degrades gracefully via the centering defect $\varepsilon_{\mathrm{cent}}$ (Remark~\ref{rem:approx_centering}).}

\longonly{
\begin{remark}[Why the $\alpha$ factor can matter in practice]
For $\gamma=0.99$, the prefactor $1/(1-\gamma)$ equals $100$.
If $\alpha=0.05$, the evaluation-error penalty in~\eqref{eq:main_theorem} contributes $\alpha/(1-\gamma)=5$ times $\varepsilon_{A,\mathrm{cand}}$, i.e., a $20\times$ reduction relative to a worst-case $\varepsilon_A/(1-\gamma)$ scaling when $\varepsilon_{A,\mathrm{cand}}\approx\varepsilon_A$.
\end{remark}}

The proof (Appendix~\ref{app:proofs}) combines the CPI mixture bound with the uniform advantage error and the centering property.

\longonly{
\begin{remark}[Sufficient condition for improvement]
\label{rem:sufficient_condition}
Rearranging~\eqref{eq:main_theorem}, the update $\pi \to \pi_{\mathrm{new}}$ is guaranteed to improve performance if
\begin{equation}
\widehat{L}_\pi(\pi_{\mathrm{new}}) - \eta(\pi)
\ge
\frac{\alpha}{1-\gamma}\varepsilon_{A,\mathrm{cand}}
+
\frac{2\varepsilon_{\mathrm{CPI}}\gamma}{(1-\gamma)^2}\alpha^2.
\label{eq:sufficient_condition}
\end{equation}
In words: the estimated surrogate improvement must exceed the noise threshold (from evaluation error) plus the distribution-shift penalty (from CPI).
\end{remark}

\begin{remark}[On Monte Carlo baselines]
\label{rem:mc_baseline}
If the baseline is estimated via Monte Carlo sampling from $\pi$, the centering property $\E_{a \sim \pi}[\widehat{A}(s,a)] = 0$ holds in expectation over sampling noise, but our theorem is a deterministic inequality.
One could extend the result to a high-probability bound with additional sampling assumptions (e.g., concentration inequalities on the baseline estimate), but we do not pursue this here.
\end{remark}}

\shortonly{A concrete numerical illustration of the bound appears in Appendix~\ref{app:cpi_derivation}.}

\textbf{Summary: TRM evaluator dials and standard knobs.}
\longonly{Corollary~\ref{cor:epsA_dials} and Theorem~\ref{thm:monotone_with_error}}%
\shortonly{The value decomposition (Proposition~\ref{prop:error_bounds}) and Theorem~\ref{thm:monotone_with_error}} together show how the TRM-specific evaluator dials $(L_z, n)$ and standard algorithmic knobs $(K, \alpha)$ shape safe improvement.
\shortonly{Smaller $L_z$ or larger $n$ reduce unrolling bias ($L_z^n$); larger $K$ tightens the residual coefficient; smaller $\alpha$ yields safer updates.}
\longonly{Table~\ref{tab:dials} summarizes the connections.

\begin{table}[t]
\caption{Summary of TRM evaluator dials ($L_z$, $n$) and standard algorithmic knobs ($K$, $\alpha$).}
\label{tab:dials}
\centering
\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}llll@{}}
\toprule
Dial & Controls & Reference & Effect \\
\midrule
$L_z$ & Contraction & Asm.~\ref{ass:Lip} & Smaller $\Rightarrow$ faster decay \\
$n$ & Unrolling & Prop.~\ref{prop:banach} & Larger $\Rightarrow$ smaller $L_z^n$ \\
$K$ & Horizon & Prop.~\ref{prop:error_bounds} & Larger $\Rightarrow$ tighter coeff.\ \\
$\alpha$ & Mixture & Thm.~\ref{thm:monotone_with_error} & Smaller $\Rightarrow$ safer update \\
\bottomrule
\end{tabular}%
}\end{table}}

\longonly{
\begin{table}[t]
\caption{Practical guidance for tuning dials.}
\label{tab:dials_practical}
\centering
\small
\begin{tabular}{@{}llll@{}}
\toprule
Dial & Typical & Tighten Bound & Trade-off \\
\midrule
$L_z$ & 0.7--0.9 & Decrease (spec.\ norm) & Expressivity \\
$n$ & 5--20 & Increase & Wall-clock \\
$K$ & 3--10 & Increase & Variance \\
$\alpha$ & 0.01--0.1 & Decrease & Learn speed \\
\bottomrule
\end{tabular}
\end{table}

\begin{example}[Numerical illustration]
\label{ex:numerical}
For $\gamma=0.99$, $L_z=0.8$, $n=5$, $K=5$:
we have $L_z^5 = 0.8^5 \approx 0.32768$ and $1-L_z = 0.2$.
Thus the finite-unrolling term in the \emph{value-error} bound (Proposition~\ref{prop:error_bounds}) is
\[
L_V\,\frac{L_z^{\,n}}{1-L_z}\,C_z
\approx
L_V\,\frac{0.32768}{0.2}\,C_z
\approx
1.64\,L_V\,C_z.
\]
For the corresponding \emph{advantage-error} bound (Corollary~\ref{cor:epsA_dials}), this unrolling term is multiplied by $2\gamma$:
\[
2\gamma\,L_V\,\frac{L_z^{\,n}}{1-L_z}\,C_z
\approx
2(0.99)\cdot 1.64\,L_V\,C_z
\approx
3.24\,L_V\,C_z.
\]
Also $(1-\gamma^K)^{-1} = (1-0.99^5)^{-1} \approx 20.4$, so the architectural residual term is scaled by $\approx 20.4$ in the value-error bound and by $\approx 2\gamma\cdot 20.4 \approx 40.4$ in the advantage-error bound.
Increasing $n$ to 10 gives $L_z^{10} \approx 0.107$, reducing the unrolling term by a factor of $\approx 3.05$.
Increasing $K$ to 10 gives $(1-\gamma^{10})^{-1} = (1-0.99^{10})^{-1} \approx 10.46$, which shrinks the \emph{coefficient} multiplying the $K$-step residual by about $20.4/10.46 \approx 1.95\times$ (roughly~$2\times$); the overall bound tightens only if the corresponding $K$-step residual $\|U_*-\mathcal{T}_K^\pi U_*\|_\infty$ remains comparable across~$K$.
\end{example}}

\shortonly{
\textbf{Persistent-$z$ (sketch).}
The original TRM carries the latent across edits.
We extend our bounds to this setting under a slow-drift assumption on the moving fixed point $z^*(x,y_t)$: the resulting value/advantage error has the same form as in the episodic-$z$ case, but with the unrolling term $L_z^n C_z/(1-L_z)$ replaced by a drift term $C_{\mathrm{drift}}(n)$ that captures tracking error of the moving fixed point.
This highlights a trade-off: persistent-$z$ variants reuse computation but incur an additional ``value-of-memory'' / tracking penalty when history matters; see Appendix~\ref{app:persistent_latent}.}

\longonly{
\subsection{Extension to persistent-$z$ TRM}
\label{sec:persistent_extension}

All previous results were stated for the episodic-$z$ plan-space MDP.
We now extend them to the persistent-$z$ variant using the slow-drift Assumption~\ref{ass:twotimescale}.

In the persistent-$z$ plan-space MDP over $(x,y,z)$, define:
\begin{equation}
\tilde{U}_n(\tilde{s}) = V_\psi(z^{(n)}(\tilde{s}),x),
\end{equation}
and the \emph{memoryless value function}:
\begin{equation}
V_{\mathrm{memless}}(x,y,z) \triangleq U_*(x,y).
\label{eq:Vmemless_def}
\end{equation}
Note that $V_{\mathrm{memless}}$ deliberately ignores $z$---it uses only the plan-space fixed-point value.
This represents the best value function achievable by an architecture that ignores the latent memory and instead fully solves the static instance $(x,y)$ at every step.
The Bellman operator $\mathcal{T}_K^{\tilde{\pi}}$ acts on functions of $\tilde{s} = (x,y,z)$, so $({\mathcal{T}_K^{\tilde{\pi}} V_{\mathrm{memless}}})(x,y,z)$ depends on $z$ through the policy and transitions even though $V_{\mathrm{memless}}$ itself does not.

\begin{corollary}[Persistent-$z$ value and advantage error]
\label{cor:persistent_value_adv}
Under Assumptions~\ref{ass:Lip}, \ref{ass:twotimescale}, and~\eqref{eq:LV_def}:
\begin{align}
\|\tilde{U}_n - V^{\tilde{\pi}}\|_{\infty,\tilde{\pi}}
&\le
\frac{\|V_{\mathrm{memless}} - \mathcal{T}_K^{\tilde{\pi}} V_{\mathrm{memless}}\|_{\infty,\tilde{\pi}}}{1-\gamma^K}
\notag \\
&\quad +
L_V\,\Cdrift(n),
\label{eq:persistent_value_bound}
\end{align}
and the advantage error satisfies:
\begin{align}
\varepsilon_A^{\mathrm{pers}}
&\le
\frac{2\gamma}{1-\gamma^K}
\|V_{\mathrm{memless}} - \mathcal{T}_K^{\tilde{\pi}} V_{\mathrm{memless}}\|_{\infty,\tilde{\pi}}
\notag \\
&\quad +
2\gamma\,L_V\,\Cdrift(n).
\end{align}
This parallels Corollary~\ref{cor:epsA_dials} but with $\Cdrift(n)$ replacing $L_z^n C_z / (1-L_z)$.
\end{corollary}

\begin{remark}[Value of memory interpretation]
\label{rem:value_of_memory}
The residual $\|V_{\mathrm{memless}} - \mathcal{T}_K^{\tilde{\pi}} V_{\mathrm{memless}}\|_{\infty,\tilde{\pi}}$ captures the \emph{value of memory}---the penalty for approximating a history-dependent policy $\tilde{\pi}(a\mid x,y,z)$ with a memoryless value function $V_{\mathrm{memless}}(x,y,z) = U_*(x,y)$.
This is a form of POMDP approximation error: if the task requires remembering past failures (not encoded in $y$), this residual is large.

Importantly, $V_{\mathrm{memless}}$ is the best \emph{architecture-level} approximation we can achieve for $V^{\tilde{\pi}}$ using only $(x,y)$---i.e., ignoring the latent state $z$.
Thus the residual measures the combined effect of (i) ignoring memory and (ii) the approximation limits of $(f_\theta, V_\psi)$ on $(x,y)$ alone.
If the true optimal value function depends critically on history not encoded in $y$, this residual can be interpreted as a value-of-memory penalty.

The connection to belief-state value function approximation in POMDPs~\citep{Kaelbling1998POMDP} is suggestive but not exact: in a true POMDP, the belief state encodes uncertainty, while here $z$ encodes past computation on a deterministic task.
Nonetheless, the structure is similar: $z$ plays the role of a sufficient statistic for history, and ignoring it incurs the ``cost of amnesia.''

The drift term $L_V \Cdrift(n)$ captures how well the latent tracks the fixed point.
Thus the persistent-$z$ bounds exhibit a tradeoff: the persistent variant saves computation (reusing $z$ across steps) but potentially loses information if the value of memory is high.
The episodic-$z$ analysis is tighter by design, which is why we use it as our primary setting.
\end{remark}}

\shortonly{
\section{Experiments}
\label{sec:experiments}

We validate that inner $z\!\to\!z$ contraction controls unroll sensitivity in practice.

\textbf{Benchmark purpose.} We use $4{\times}4$ Sudoku not as a challenge of reasoning capability, but as a controlled, transparent environment to empirically validate theoretical error bounds and stability dials $(L_z, n, R)$. The domain's simplicity allows precise measurement of value instability $\Delta_V$ and policy divergence $\Delta_\pi$ under controlled depth mismatch.

\textbf{Setup.}
We use 4$\times$4 Sudoku (1--4 empties) with a deterministic feasibility checker, horizon $T{=}16$, and discount $\gamma{=}0.99$.
The action space has 97 actions (write token at position, plus STOP); state-dependent masking restricts invalid edits.
Contraction ($L_z{=}0.9$) is enforced via operator-norm clamping on $z\!\to\!z$ layers.
We denote the projection-off setting by $R{=}0$ (a sentinel meaning $\Pi_R$ is the identity map); for $R>0$, $\Pi_R$ is Euclidean projection onto the radius-$R$ ball.

\textbf{Terminology.} We distinguish $z\!\to\!z$ contraction enforcement (stable, used throughout) from value-head spectral normalization (disabled due to instability; see Appendix~\ref{app:latent_collapse_diagnostic}).

\subsection{Isolation Study: Contraction vs.\ Projection}
\label{sec:isolation_study}

To test whether inner contraction provides stability under depth mismatch, we evaluate the trained checkpoints at mismatched unroll depths: $n_{\mathrm{train}}{=}2$ vs.\ $n_2\in\{4,8,16\}$ (up to $8\times$ deeper).
We measure value instability $\Delta_V$, policy KL $\Delta_\pi$, and argmax agreement on B0 (100 initial states) and B1 (one-step successors), aggregated over seeds 41, 42, 43 with Wilson 95\% CI for agreement.

\textbf{Key result.}
At $8\times$ depth mismatch on B0 (initial states), contraction is associated with lower $\Delta_V$ ($0.156$ vs.\ $0.038$ in our 3-seed runs) and lower $\Delta_\pi$ ($0.0063$ vs.\ $0.0002$). \textbf{Caveat:} With only 3 seeds, these differences are descriptive; we do not claim statistical significance.

\begin{figure}[t]
\centering
\includegraphics[width=0.95\columnwidth]{figures/fig_exp1_unroll_sensitivity_main.pdf}
\caption{Unroll sensitivity on B0 (initial states). $n_{\mathrm{train}}{=}2$; evaluated at $n_2\in\{4,8,16\}$. ``No Contraction'' vs.\ ``Contraction'' ($L_z{=}0.9$); the \emph{only} difference is \texttt{enable\_contraction}. Both conditions use projection $R{=}10$ (active 100\%); see Appendix~\ref{app:exp2_sweep} for projection ablation. Value-head spectral norm disabled. Seeds: 41, 42, 43. Left: $\Delta_V$ (value instability, log scale). Center: $\Delta_\pi$ (policy KL, log scale). Right: Argmax Agreement. Error bars: mean$\pm$std ($\Delta_V$, $\Delta_\pi$); Wilson 95\% CI (argmax).}
\label{fig:exp1_unroll_sensitivity}
\end{figure}

\textbf{Important caveat:} Both conditions use projection $R{=}10$, active 100\% and providing substantial stabilization (Appendix~\ref{app:exp2_sweep} shows projection alone provides $6$--$10\times$ improvement in $\Delta_V$). The contraction effect reported here is a marginal improvement \emph{on top of} projection-induced stability.

\textbf{Interpretation.}
During depth-mismatch evaluation on B0, enabling contraction (vs.\ disabling it) reduces drift; at $R{=}0$ on B0 this persists, ruling out clipping as the sole explanation.
On B1 successor states, the $R{=}0$ $\Delta_V$ improvement does not hold (Appendix~\ref{app:exp1_b1}).

\begin{figure}[!b]
\centering
\includegraphics[width=\linewidth]{figures/feasibility_6to8empties_success_vs_steps.pdf}
\caption{Harder 4$\times$4 Sudoku (6--8 empties): UPI-TRM reaches 48--57\% success; tuned standard baselines (PPO, A2C, DQN; 11 configurations, Appendix~\ref{app:baseline_tuning}) and Random achieve 0\%. No-contraction ($R{=}10$) outperforms contraction (projection off). 3 seeds, 20k steps.}
\label{fig:hard_suite}
\end{figure}

\subsection{Feasibility from checker feedback (toy suite)}
\label{sec:feasibility_toy}

We test whether UPI--TRM can learn from checker feedback in a minimal controlled domain. An episode is \emph{successful} if the agent produces a feasible solution (zero constraint violations under the feasibility checker) within horizon $T{=}16$. The \emph{Random} baseline samples uniformly over the masked (valid) action set at each step; in the ultra-easy 1--4-empty regime this already achieves 52\% success by chance, so results here should be read as a feasibility and stability diagnostic rather than a competitive Sudoku evaluation. On harder $4{\times}4$ puzzles with 6--8 empties, random success drops to 0\% (Appendix~\ref{app:harder_sudoku_detailed}).

\begin{table}[t]
\caption{Toy-suite feasibility (4$\times$4 Sudoku, 1--4 empties): success rate (feasible within $T{=}16$) after 5000 training steps. Mean $\pm$ std over 3 seeds. Greedy argmax + action masking; unroll depth $n_\mathrm{eval}{=}2$.}
\label{tab:upi_success_only}
\centering
\small
\begin{tabular*}{\columnwidth}{@{\extracolsep{\fill}}lc@{}}
\toprule
Method & Success (\%) \\
\midrule
UPI--TRM (persistent-$z$, no contraction, $R{=}10$) & \textbf{93.3 $\pm$ 2.3} \\
UPI--TRM (episodic-$z$, no contraction, $R{=}10$) & \textbf{93.3 $\pm$ 2.3} \\
UPI--TRM (episodic-$z$, contraction, proj.\ off) & \textbf{90.7 $\pm$ 5.0} \\
Random (masked uniform) & 52.0 \\
\bottomrule
\end{tabular*}
\end{table}

UPI-TRM variants achieve 90--93\% feasibility (Table~\ref{tab:upi_success_only}), substantially exceeding the 52\% random baseline.
The contraction variant ($90.7\%\pm 5.0\%$) is slightly below no-contraction ($93.3\%$); this is expected since contraction is designed as a \emph{stability dial} for depth-mismatch robustness, not a performance booster.
For controlled projection comparisons, see Appendix~\ref{app:exp3_projection_ablation}.

\subsection{Scaling to Harder Puzzles}
\label{sec:harder_puzzles}

On 4$\times$4 Sudoku with 6--8 empty cells (harder suite, $T{=}16$, 20k steps), random uniform sampling achieves 0\% success (search space too large for chance completion). Figure~\ref{fig:hard_suite} shows UPI-TRM variants reach 48--57\% success, demonstrating effectiveness beyond the trivial regime. We use the easier 1--4 empty suite (random baseline 52\%) for controlled ablations due to faster training, but the harder suite provides a more convincing benchmark.

\textbf{Standard RL baselines.}
We evaluated PPO, A2C, and DQN with the same TRM backbone, tuning hyperparameters across 11 configurations covering exploration (high entropy, slow $\varepsilon$-decay), network depth (inner unroll $n{=}8$), learning dynamics, and reward shaping (Appendix~\ref{app:baseline_tuning}).
All achieved \textbf{0\% success} on this hard suite (550 total puzzles evaluated across configs, 0 solved; 95\% CI $[0\%, 0.67\%]$).
The best configuration (A2C with high entropy, $\mathrm{entropy\_coef}{=}0.2$) improved mean score from 9.0 to 11.66/16 but never solved a single puzzle---a gap of 4 cells that standard RL did not bridge in our tuning budget.
In our implementation and tuning budget (11 configs; Appendix~\ref{app:baseline_tuning}), PPO/A2C/DQN did not solve any puzzles on this suite, whereas UPI--TRM reached 48--57\% success. This suggests an advantage of the UPI--TRM update + evaluator design in this setting, but does not rule out success from alternative algorithms, larger sweeps, or longer training.

\subsection{Scaling to 9$\times$9 Sudoku}
\label{sec:9x9_experiments}

To test generalization to a significantly harder domain, we evaluate UPI-TRM against standard RL baselines on 9$\times$9 Sudoku. The 9$\times$9 grid has 81 cells and 729 possible actions (81 positions $\times$ 9 digits), compared to 16 cells and ${\sim}64$ actions for 4$\times$4, with substantially more complex row/column/box constraint interactions.

\textbf{Setup.}
All methods use the same TRM backbone, constraint-aware action masking (which reduces valid actions to ${\sim}250$ per state by masking digits already present in the same row, column, or $3{\times}3$ box), and feasibility-based reward: $\mathrm{score} = \mathrm{filled\_cells} - 2.0 \times \mathrm{violations} - 5.0 \times \mathrm{zero\_candidates}$.
Training: 50k steps, 3 seeds, horizon $T{=}81$, discount $\gamma{=}0.99$.
Initial puzzle score is ${\sim}26.8$ (given clues only); maximum score is 81 (fully solved).

\textbf{Key result: UPI-TRM is the only method that solves any 9$\times$9 puzzles.}

\begin{table}[t]
\caption{9$\times$9 Sudoku results (50k steps, 3 seeds). UPI-TRM is the only method that solves any puzzles. Score improvement is relative to initial puzzle score (${\sim}26.8$).}
\label{tab:9x9_results}
\centering
\small
\begin{tabular}{@{}lccc@{}}
\toprule
Algorithm & Success Rate & Mean Score & Improvement \\
\midrule
\textbf{UPI--TRM} & \textbf{4.0\% $\pm$ 4.0\%} & \textbf{53.3 $\pm$ 1.1} & \textbf{+26.5} \\
A2C & 0.0\% $\pm$ 0.0\% & 31.9 $\pm$ 0.4 & +5.1 \\
DQN & 0.0\% $\pm$ 0.0\% & 29.5 $\pm$ 0.3 & +2.7 \\
PPO & 0.0\% $\pm$ 0.0\% & 28.2 $\pm$ 1.5 & +2.2 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.95\columnwidth]{figures/fig_9x9_success_vs_steps.pdf}
\caption{9$\times$9 Sudoku: success rate vs.\ training steps. UPI-TRM reaches 4--8\% success across seeds while all baselines (PPO, A2C, DQN) remain at 0\%. Shaded regions show $\pm$1 std across 3 seeds.}
\label{fig:9x9_success}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.95\columnwidth]{figures/fig_9x9_mean_score_vs_steps.pdf}
\caption{9$\times$9 Sudoku: mean score vs.\ training steps. UPI-TRM achieves ${\sim}53$ (filling ${\sim}27$ additional cells correctly) while baselines plateau at ${\sim}28$--$32$ (filling only ${\sim}2$--$5$ cells). Initial score ${\sim}26.8$; maximum 81.}
\label{fig:9x9_score}
\end{figure}

Table~\ref{tab:9x9_results} and Figures~\ref{fig:9x9_success}--\ref{fig:9x9_score} show the results.
UPI-TRM solves 4\% of puzzles on average (2 out of 3 seeds solve at least one puzzle), with mean score improvement of +26.5 points.
All baselines achieve 0\% success despite using the same backbone and constraint-aware masking; their score improvements (+2 to +5) indicate they learn to fill a few obvious cells but cannot perform the multi-step constraint propagation needed to complete the puzzle.

\textbf{Why UPI-TRM succeeds.}
The TRM's recursive latent reasoning enables multi-step lookahead essential for Sudoku, where each cell placement affects future possibilities.
With persistent latent state (\texttt{episodic\_latent: false}), the model accumulates constraint structure across edit steps.
Standard RL methods struggle with the 81-step credit assignment problem: they cannot attribute success/failure to early moves in the long sequence of edits.

\subsection{Projection-Free Contraction Dial}
\label{sec:exp4_main}

The projection dominance study (Appendix~\ref{app:exp2_sweep}) shows that the spectral-norm target $L_z^*$ is not an effective dial when projection is active: achieved post-projection Lipschitz collapses across settings.
However, with projection disabled ($R{=}0$), inference-time scaling of the $z\!\to\!z$ weights yields a measurable range of achieved $L^{\mathrm{pre}}_z$ that correlates \emph{monotonically} with depth-mismatch stability.

Figure~\ref{fig:exp4_scatter_main} shows the correlation: lower $L^{\mathrm{pre}}_z$ yields higher argmax agreement (B0: $\rho{=}{-}0.87$; B1: $\rho{=}{-}0.92$).
This confirms that when projection does not dominate, the $z\!\to\!z$ contraction modulus serves as a meaningful stability dial.

\begin{figure}[t]
\centering
\includegraphics[width=0.95\columnwidth]{figures/fig_exp4_dial_scatter.pdf}
\caption{Projection-free contraction dial: $L^{\mathrm{pre}}_z$ vs.\ argmax agreement ($n_2{=}8$, i.e., $4\times$ depth mismatch). Projection disabled ($R{=}0$), value-head spectral norm disabled. Strong negative correlation: lower achieved $L^{\mathrm{pre}}_z$ yields higher argmax agreement, validating the contraction dial when projection is inactive.}
\label{fig:exp4_scatter_main}
\end{figure}

\textbf{Practitioner takeaway.}
Projection ($R > 0$) is the \emph{primary} stability mechanism in our experiments, providing 6--10$\times$ $\Delta_V$ improvement.
Contraction provides \emph{marginal additional} stability when projection is active.
When projection is disabled, contraction becomes the sole stability dial.
\textbf{Recommendation:} Use projection for robust training; disable projection at inference to expose the contraction dial for fine-tuning depth-mismatch tolerance.
Detailed tables are in Appendix~\ref{app:exp4_projection_free_dial_v2}.

\subsection{Discussion}
\label{sec:discussion}

\textbf{Bounds as structural guidance.}
Our theoretical bounds provide \emph{qualitative guidance}---identifying which dials matter---rather than tight quantitative predictions.
Empirically operationalizing the bounds (e.g., correlating $K$-step TD error with success) remains future work.
Contraction is a \emph{stability dial} for depth-mismatch sensitivity, not a performance booster; the modest success drop ($90.7\%$ vs.\ $93.3\%$) is consistent with this framing.
Practitioners should tune $(L_z, n)$ jointly to balance stability and performance.

\textbf{Limitations and future work.}
Our experiments use \textbf{3 seeds} on 4$\times$4 and 9$\times$9 Sudoku; results are descriptive, not statistically significant.
\textbf{Key gap:} The theoretical bounds (Proposition~\ref{prop:error_bounds}, Theorem~\ref{thm:monotone_with_error}) are not directly validated because (i) $L_V$ is uncontrolled, (ii) centering is approximate, (iii) sup-norm residuals are not measured.
We view the experiments as \emph{feasibility demonstrations} and the bounds as \emph{structural guidance}; quantitative validation requires future work with controlled assumptions.
The 1--4 empty suite is diagnostic (Random achieves 52\%); the 6--8 empty suite (Figure~\ref{fig:hard_suite}, Appendix~\ref{app:controlled_2x2}) and the 9$\times$9 scale-up (Section~\ref{sec:9x9_experiments}) provide meaningful regimes where baselines achieve 0\%.
Extending to other TRM benchmarks (e.g., ARC-AGI subsets) with more extensive hyperparameter sweeps remains future work.
See Appendix~\ref{app:discussion_details} for detailed experiment reconciliation and practical recommendations.
}

\longonly{
\section{Preliminary Experiments}
\label{sec:experiments}
\label{sec:isolation_study}  % Alias for cross-references from appendix

We report preliminary results on a toy domain to validate that the theoretical dials correlate with empirical behavior.
Comprehensive empirical validation is deferred to future work.

\textbf{Domain: 4$\times$4 Sudoku.}
We use a simplified 4$\times$4 Sudoku with 2$\times$2 blocks.
Let $v(x,y)$ be the number of violated Sudoku \emph{unit constraints} (rows, columns, and $2\times 2$ blocks) for plan $y$ on instance $x$,
where each unit contributes $1$ if it is not a permutation of $\{1,2,3,4\}$
(equivalently, since entries are restricted to $\{1,2,3,4\}$ or blank, the unit contains a repeated digit or an unfilled cell).
There are $4$ rows, $4$ columns, and $4$ blocks, so $0 \le v(x,y) \le 12$.
We define a nonnegative checker score $c(x,y) = C_{\max} - v(x,y)$ with $C_{\max}=12$,
so $c(x,y)=12$ for a valid solution and smaller values indicate more violations.
Edit actions set a single empty cell to a digit in $\{1,2,3,4\}$.

We use $\gamma = 0.99$, $K = 5$, and vary $L_z$ and $n$.


\textbf{Implementation sensitivity of contraction.}
While our theory assumes an $L_z$-contractive latent update map, enforcing this property in practice depends on the chosen norm-control mechanism.
In our implementation, naively applying PyTorch's power-iteration spectral normalization to the inner recursion can be numerically unstable (Appendix~\ref{app:sn_instability}).
We therefore use a simpler and more robust alternative for the $z\!\to\!z$ reasoning layers: explicit operator-norm clamping (rescaling weights when $\|W\|_{\mathrm{op}}$ exceeds a threshold) combined with per-layer output scaling to obtain a strict contraction.
This achieves stable $\hat L_z=O(1)$ in our diagnostics and avoids the numerical blow-ups observed with spectral normalization (Appendix~\ref{app:sn_instability}). Empirically, however, enforcing $L_z<1$ can be overly restrictive (as seen in our post-fix ablation results), and should be treated as a stability/expressivity dial rather than a guarantee of improved success.

\textbf{Correlation between Parameters and Stability.}
We measured the variance of policy updates (gradient norms) and surrogate improvement $\widehat{L}_\pi(\pi_{\mathrm{new}}) - \eta(\pi)$ across training.
Directionally consistent with theory: (i) smaller $L_z$ (stronger contraction) yielded lower gradient variance; (ii) larger $n$ reduced empirical Bellman residuals; (iii) smaller $\alpha$ produced more stable (though slower) improvement.
These observations provide preliminary validation that the theoretical dials translate to practice, though we do not claim tight quantitative agreement.

\begin{figure}[t]
\centering
\includegraphics[width=0.95\columnwidth]{figures/fig_exp1_unroll_sensitivity_main.pdf}
\caption{Unroll sensitivity on B0 (initial states). $n_{\mathrm{train}}{=}2$; evaluated at $n_2\in\{4,8,16\}$. ``No Contraction'' vs.\ ``Contraction'' ($L_z{=}0.9$). Both conditions: value-head spectral norm disabled, projection $R{=}10$. Seeds: 41, 42, 43. Left: $\Delta_V$ (value instability, log scale). Center: $\Delta_\pi$ (policy KL, log scale). Right: Argmax Agreement. Error bars: mean$\pm$std ($\Delta_V$, $\Delta_\pi$); Wilson 95\% CI (argmax).}
\label{fig:exp1_unroll_sensitivity}
\end{figure}

\textbf{Completed scale-up: 9$\times$9 Sudoku.}
We scaled UPI--TRM to 9$\times$9 Sudoku (81 cells, 729 actions), a significantly harder domain than 4$\times$4. UPI--TRM is the only method that solves any puzzles (4\% success vs.\ 0\% for PPO/A2C/DQN), with mean score improvement of +26.5 vs.\ +2--5 for baselines. See Section~\ref{sec:9x9_experiments} for details.

\textbf{Proposed additional experiments.}
For future work, we plan:
\begin{itemize}
\item \textbf{Benchmarks:} Maze navigation, ARC-AGI subsets, and other constraint satisfaction tasks.
\item \textbf{Baselines:} (i) Supervised TRM with deep supervision; (ii) ``PPO-on-top'': frozen supervised TRM backbone with PPO on edit policy; (iii) UPI--TRM ablations without contraction enforcement or with $K=1$; (iv) Non-recursive baseline: direct MLP on $(x,y)$ without inner recursion.
\item \textbf{Diagnostics:} Empirical Bellman residuals of $U_n$ on held-out $K$-step rollouts; correlations between unrolled values and Monte Carlo returns; empirical $\hat L_z$, $\hat C_z$, $\widehat{C}_{\mathrm{drift}}(n)$ throughout training; ablation of episodic-$z$ vs.\ persistent-$z$; $\KL(\pi_{\mathrm{new}} \| \pi_\phi)$ after distillation.
\end{itemize}}

\shortonly{
\section{Related Work}
\label{sec:related_work}

\textbf{TRM/HRM-style reasoning.}
Tiny Recursive Models~\citep{TRM2025} and Hierarchical Recursive Models~\citep{HRM2025} use iterative latent refinement for verifiable reasoning; follow-ups address training dynamics and test-time adaptation~\citep{Qasim2025CGAR,McGovern2025TRMAdapt}.
We formalize TRM's edit loop as an RL problem with checker feedback, providing a theoretical lens on how architectural choices affect policy optimization.

\textbf{Learning to search and plan-editing RL.}
Our plan-space MDP framing is related to structured prediction as sequential decision making~\citep{Daume2009SEARN,Ross2011DAgger,Chang2015LOLS}, but uses checker rewards rather than expert rollouts.
Model-free RL for Sudoku and constraint satisfaction shows poor reliability~\citep{Mehta2021SudokuRL,Klein2022SudokuRL}; we provide a lens on evaluator design and how inner evaluation overcomes these.

\textbf{RL with verifiable rewards.}
Recent work on RLVR uses automatic verifiers (exact-match, unit tests) rather than learned preference models~\citep{Wang2025OneShotRLVR,Su2025RewardBridge,DeepSeekR1}.
Whereas most RLVR targets token-level LLM post-training, we study verifiable rewards in a plan-editing MDP with explicit evaluator analysis.
Our bounds on value error and conservative improvement complement RLVR by identifying how inner-loop contraction controls evaluation accuracy.

\textbf{Sequence models and in-context learning.}
Algorithm Distillation~\citep{laskin2022context}, Decision Transformer~\citep{chen2021decision}, and Trajectory Transformer~\citep{janner2021trajectory} treat RL as sequence modeling, controlling compute via context length or model depth.
TRMs differ by exposing an explicit inner-loop iterative computation with depth $n$ variable at inference; our analysis quantifies truncation bias from this iterative evaluator.

\textbf{Contraction and conservative PI.}
We build on CPI and trust-region ideas~\citep{KakadeLangford2002,Schulman2015TRPO}, extending to settings where the value oracle is parameterized by architectural dials. Unlike safe PI work~\citep{Pirotta2013SafePI,Metelli2021SafePI,Eshwar2025MonotoneCPI} using constrained optimization, our Theorem~\ref{thm:monotone_with_error} derives the $O(\alpha\,\varepsilon_{A,\mathrm{cand}})$ term from statewise-centered advantages, and Proposition~\ref{prop:error_bounds} couples improvement to TRM dials $(L_z, n)$ via the $L_z^n$ bias.

\textbf{Deep equilibrium models.}
Our contraction analysis connects to DEQs~\citep{Bai2019DEQ}; we identify a sufficient (not necessary) contractive regime where theoretical guarantees hold.}

\longonly{
\section{Related Work}
\label{sec:related_work}

TRM/HRM-style recursive reasoning models~\citep{HRM2025,TRM2025} and follow-ups on training and adaptation~\citep{Qasim2025CGAR,McGovern2025TRMAdapt} motivate our setting.
Our plan-space framing is related to structured prediction as sequential decision making~\citep{Daume2009SEARN,Ross2011DAgger,Chang2015LOLS}, while differing in that we learn from checker rewards rather than expert rollouts.
Our use of automated task checkers also connects to reinforcement learning with verifiable rewards (RLVR), where rewards are computed by verifiers (e.g., exact-match, rule-based checks, or unit tests) rather than learned preference models~\citep{Wang2025OneShotRLVR,Su2025RewardBridge,DeepSeekR1}.
Whereas most RLVR work targets token-level post-training of LLMs, we study verifiable reward in a \emph{plan-editing} MDP and analyze how evaluator design (contraction and finite unrolling) controls evaluation error and conservative improvement.
Model-free RL has also been explored for Sudoku and related deterministic constraint-satisfaction problems, with results indicating poor reliability and weak generalization as difficulty increases~\citep{Mehta2021SudokuRL,Klein2022SudokuRL}.
Technically, we connect contraction of an inner evaluator to approximate dynamic programming error decompositions, contrasting with unrolled DP architectures in environment state space~\citep{Tamar2016VIN,Rozada2025BellNet}.
For safe improvement, we build on conservative policy iteration and trust-region ideas~\citep{KakadeLangford2002,Schulman2015TRPO,Achiam2017CPO}, and we interpret the persistent-$z$ extension through a POMDP-style ``value of memory'' lens~\citep{Kaelbling1998POMDP}.
Recent work on safe policy iteration~\citep{Pirotta2013SafePI,Metelli2021SafePI} establishes monotonic improvement guarantees under function approximation via constrained optimization formulations; \cite{Eshwar2025MonotoneCPI} extends these ideas beyond the tabular case. Our Theorem~\ref{thm:monotone_with_error} differs in two respects: (i) the $O(\alpha\,\varepsilon_{A,\mathrm{cand}})$ term arises from statewise-centered advantage estimation rather than from trust-region constraints, and (ii) the value decomposition (Proposition~\ref{prop:error_bounds}) explicitly couples policy-improvement to TRM evaluator dials $(L_z, n)$ via the finite-unrolling bias $L_z^n$, bridging architectural design choices to improvement guarantees.

\textbf{Deep equilibrium models.}
Our inner-loop contraction analysis connects to Deep Equilibrium Models~\citep{Bai2019DEQ}, which similarly analyze fixed-point convergence in deep networks, though in a supervised rather than RL setting. Note that the original TRM work emphasizes that fixed-point convergence is not required; our contraction assumption identifies a sufficient (not necessary) regime where theoretical guarantees hold.

\textbf{In-context learning and algorithm distillation.} Algorithm Distillation (AD)~\citep{laskin2022context} trains transformers to emulate RL algorithms by modeling learning histories across episodes, enabling in-context policy improvement without parameter updates. While AD distills a pre-trained RL algorithm (e.g., PPO) into a sequence model, our work analyzes RL training of a recursive evaluator from checker rewards within a single task. Both frameworks treat computation depth (AD: context length; TRM: unroll depth $n$) as a hyperparameter affecting performance; unifying these perspectives---e.g., using TRMs as in-context plan editors---is promising future work.}

\section{Conclusion}
\label{sec:conclusion}

We cast TRM-style plan editing as RL in a plan-space MDP with checker feedback, and analyze an episodic-$z$ variant under an inner-loop contraction assumption.
Our main results (i)~decompose value error into an architectural Bellman residual term plus a finite-unrolling bias decaying as $L_z^n$, and (ii)~give a conservative mixture-improvement bound in which centered evaluation error contributes as $O(\alpha\,\varepsilon_{A,\mathrm{cand}})$.
These bounds expose actionable TRM evaluator dials $(L_z,n)$ and clarify how standard algorithmic knobs $(K,\alpha)$ interact with TRM-specific evaluation error, providing structural guidance for training TRM-like models from checker rewards.

Experiments on 4$\times$4 Sudoku illustrate that UPI--TRM can learn from checker feedback and that contraction/projection settings modulate depth-mismatch stability; on 9$\times$9 Sudoku (a significantly harder domain with 729 actions), UPI--TRM is the only method that solves any puzzles (4\% success vs.\ 0\% for PPO/A2C/DQN baselines), demonstrating that the recursive reasoning mechanism provides a meaningful advantage for multi-step constraint satisfaction.
Practitioners can use contraction ($L_z < 1$) and unroll depth ($n$) as stability dials when evaluation-time compute differs from training, while projection provides additional robustness when active.
\shortonly{Limitations and additional experimental details appear in Appendix~\ref{app:discussion_details}.}

\longonly{\textbf{Limitations.}}
\longonly{%
Our analysis uses contraction and Lipschitz assumptions that are only approximately enforced; sup-norm residuals are approximated via $L^2$ losses; guarantees apply to conceptual two-phase GPI with mixtures, not fully interleaved training with distillation.
The persistent-$z$ analysis relies on a slow-drift condition that provides local guidance rather than global guarantees for discrete tasks.
The $O(\alpha \varepsilon_A)$ tightening in Theorem~\ref{thm:monotone_with_error} requires an exactly computed baseline, limiting applicability to discrete action spaces.
Our isolation study (Section~\ref{sec:isolation_study}) provides initial empirical validation: contraction enforcement reduces mismatch drift by $4.1\times$ on value estimates and $33\times$ on policy KL at $8\times$ depth (Figure~\ref{fig:exp1_unroll_sensitivity}), with the $R{=}0$ ablation confirming that stability improvement from contraction persists when projection is disabled on B0 initial states (Appendix~\ref{app:exp1_tables}; the $R{=}0$ claim does \emph{not} hold for $\Delta_V$ on B1 successor states---see Appendix~\ref{app:exp1_b1}).
However, projection at small $R$ can be active 100\%, masking contraction measurements and itself acting as a major stabilizer ($6$--$10\times$ $\Delta_V$ improvement; see Appendix~\ref{app:exp2_sweep}).
We additionally verified that stable training is possible with projection disabled (Appendix~\ref{app:exp3_projection_ablation}), so projection's role is primarily at evaluation-time mismatch, not training stability.
The spectral-norm-based target $L_z^*$ does not function as a meaningful dial in the tested regime (Appendix~\ref{app:exp2_sweep}); when projection is active it can mask contraction effects.
However, when projection is disabled ($R{=}0$), inference-time scaling of the $z\!\to\!z$ recursion yields a measurable range of achieved $L^{\mathrm{pre}}_z$ that correlates monotonically with depth-mismatch stability (Appendix~\ref{app:exp4_projection_free_dial_v2}).
We validated on 9$\times$9 Sudoku (Section~\ref{sec:9x9_experiments}), where UPI--TRM is the only method that solves any puzzles (4\% vs.\ 0\% for baselines); further validation on additional domains (e.g., ARC-AGI) remains future work.
Our theoretical bounds use CPI-style conservative mixture updates (mixture weight $\alpha$) as a clean analysis instantiation that yields monotonicity-style statements.
PPO-style clipped objectives are widely used in practice and often more effective, but our current theory does not directly model PPO's implicit trust region via ratio clipping.
Extending the evaluator-error lens to PPO---for instance via KL-regularized surrogate interpretations---is natural future work.
Our experimental validation uses 3 seeds on 4$\times$4 and 9$\times$9 Sudoku; results are descriptive, not statistically significant.
Standard RL baselines (PPO, A2C, DQN) achieved 0\% success on both hard 4$\times$4 (6--8 empties) and 9$\times$9 Sudoku even after hyperparameter tuning (Appendix~\ref{app:baseline_tuning}), suggesting an advantage of the UPI--TRM design for multi-step constraint satisfaction.}

\longonly{
\textbf{Future directions.}
Comprehensive empirical evaluation on TRM benchmarks; strengthening the POMDP-based analysis; deriving KL trust-region bounds with explicit advantage error; formalizing learned plan embeddings with smoothness regularization; combining with TRM test-time adaptation; and providing rigorous distillation guarantees.}
\bibliography{trm_rl}
\bibliographystyle{icml2026}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% APPENDIX / SUPPLEMENTARY MATERIAL
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\clearpage
\appendix
\crefalias{section}{appendix}
\onecolumn

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Supplementary Material}
\label{app:supplementary}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Short-only definitions for appendix cross-references
\shortonly{%
% These definitions are only needed in short mode since they appear in longonly blocks in the main text.
\subsection*{Key Definitions (for proofs)}

% Projection onto ball:
\begin{equation}
\Pi_R(z) =
\begin{cases}
z, & \|z\|_2 \le R,\\
R\,z/\|z\|_2, & \|z\|_2 > R.
\end{cases}
\label{eq:projection}
\end{equation}

% Proposition (Banach fixed point):
\begin{proposition}[Fixed point and finite-unrolling bias]
\label{prop:banach}
Under Assumption~\ref{ass:Lip}, for each $(x,y)$ the map $T_{x,y}(z)=\tilde f_\theta(z,y,x)$ has a unique fixed point $z^*(x,y)$, with:
\begin{equation}
\norm{z^{(t)} - z^*(x,y)} \le L_z^{\,t}\,\norm{z^{(0)} - z^*(x,y)}.
\label{eq:linear_convergence}
\end{equation}
For initialization $z^{(0)} = z_{\mathrm{init}}(x,y)$, define:
\begin{equation}
C_z:=\sup_{(x,y)}\bigl\|\tilde f_\theta(z_{\mathrm{init}},y,x)-z_{\mathrm{init}}\bigr\|.
\label{eq:Cz_def}
\end{equation}
Then $\|z^{(n)}-z^*(x,y)\| \le \frac{L_z^n}{1-L_z}\,C_z$.
\end{proposition}

% Lipschitz value head:
\begin{equation}
\bigl|V_\psi(z,x)-V_\psi(z',x)\bigr| \le L_V\,\|z-z'\|.
\label{eq:LV_def}
\end{equation}

% Bounded rewards:
\begin{assumption}[Bounded rewards]
\label{ass:bounded}
Shaped rewards satisfy $|r(s,a,s')|\le R_{\max}$ for all transitions.
\end{assumption}

% Bounded value head:
\begin{assumption}[Bounded value head]
\label{ass:bounded_value_head}
There exists $V_{\max}<\infty$ such that $|V_\psi(z,x)| \le V_{\max}$ for all $z\in\mathcal{Z}_{\mathrm{inv}}$ and $x \in \mathrm{supp}(\mathcal{D})$.
\end{assumption}

% CPI bound for mixture updates:
For $\pi_{\mathrm{new}} = (1-\alpha)\pi + \alpha\pi_{\mathrm{cand}}$:
\begin{equation}
\eta(\pi_{\mathrm{new}}) \ge L_\pi(\pi_{\mathrm{new}}) - \frac{2\varepsilon_{\mathrm{CPI}}\,\gamma}{(1-\gamma)^2}\,\alpha^2,
\label{eq:cpi_bound}
\end{equation}
where $\varepsilon_{\mathrm{CPI}} = \max_s|\E_{a\sim\pi_{\mathrm{cand}}}A^\pi(s,a)|$.

% Mixture equation:
\begin{equation}
\pi_{\mathrm{new}} = (1-\alpha)\pi + \alpha\pi_{\mathrm{cand}}, \quad \alpha\in[0,1].
\label{eq:pi_mixture}
\end{equation}
}

\section{Persistent-$z$ Extension (Slow Drift)}
\label{app:persistent_latent}

\subsection{Slow-Drift Dynamics and Persistent-$z$}
\label{sec:twotimescale_persistent}

In the persistent-$z$ variant, the fixed point $z^*(x,y)$ drifts as the plan $y_t$ is edited.
We formalize a slow-drift assumption that captures when the latent can approximately track the moving fixed point.

Let $e:\YY\to\mathcal{E}_y$ be a (possibly learned) embedding of plans, with norm $\|y-y'\|_{\mathcal{E}} := \|e(y)-e(y')\|_{\mathcal{E}_y}$.

\begin{assumption}[Slow drift of fixed points]
\label{ass:twotimescale}
There exist $L_{z^*} \ge 0$, $\Delta y_{\max} \ge 0$, and $E_0 < \infty$ such that:
\begin{enumerate}
\item \textbf{Lipschitz fixed-point:} For all $(x,y),(x,y')$:
$\bigl\|z^*(x,y) - z^*(x,y')\bigr\| \le L_{z^*}\,\|y-y'\|_{\mathcal{E}}$.
\item \textbf{Bounded plan change:} Along trajectories under any policy $\tilde{\pi}$ in the considered policy class $\tilde{\Pi}$:
$\|y_{t+1}-y_t\|_{\mathcal{E}} \le \Delta y_{\max}$.
\item \textbf{Bounded initial error:} For initial states: $\bigl\|z_0 - z^*(x,y_0)\bigr\|\le E_0$.
\end{enumerate}
\end{assumption}

\begin{remark}[Implicit dependence of $L_{z^*}$]
\label{rem:Lzstar_implicit}
Condition (1) of Assumption~\ref{ass:twotimescale} requires the fixed-point map $y \mapsto z^*(x,y)$ to be Lipschitz.
If the fixed point lies strictly inside the projection ball ($\|z^*(x,y)\|_2 < R$), then by Lemma~\ref{lem:interior_proj} the unprojected fixed-point equation $f_\theta(z^*(x,y),y,x)=z^*(x,y)$ holds.
This allows us to apply the implicit function theorem to $z^* = f_\theta(z^*,y,x)$ rather than the projected map $\tilde f_\theta=\Pi_R\circ f_\theta$.

\textbf{Why projection does not affect the derivative at $z^*$:}
Since $\|z^*\|_2<R$, the projection $\Pi_R$ is the identity on an open ball around $z^*=f_\theta(z^*,y,x)$, and on that ball $\Pi_R$ is $C^\infty$ with Jacobian equal to the identity matrix $I$.
Hence the Jacobian of the projected map satisfies $\partial_z\tilde f_\theta(z^*,y,x)=\partial_z f_\theta(z^*,y,x)$, and we may differentiate the unprojected fixed-point equation $z^*=f_\theta(z^*,y,x)$ directly.

\textbf{Regularity qualifier:} We assume that, for each fixed $x$, the map $(z,y)\mapsto f_\theta(z,y,x)$ is $C^1$ in a neighborhood of $(z^*,y)$ so that the classical implicit function theorem applies.
For piecewise-linear networks (e.g., ReLU MLPs), this holds almost everywhere; the same calculation applies on each differentiable region.

By the implicit function theorem applied to $z^* = f_\theta(z^*, y, x)$, differentiating both sides with respect to $y$ yields
\begin{equation*}
\frac{\partial z^*}{\partial y} = \bigl(I - \partial_z f_\theta(z^*,y,x)\bigr)^{-1}
\,\partial_y f_\theta(z^*,y,x).
\end{equation*}
Since $\|\partial_z f_\theta(z^*,y,x)\|_{\mathrm{op}} \le L_z < 1$ (by Assumption~\ref{ass:Lip}, noting that $\partial_z\tilde f_\theta=\partial_z f_\theta$ at $z^*$), the inverse $(I-\partial_z f_\theta)^{-1}$ exists and has operator norm bounded by $(1-L_z)^{-1}$.
Thus:
\begin{equation}
L_{z^*} \le \frac{\|\partial_y f_\theta\|_{\mathrm{op}}}{1 - L_z},
\label{eq:Lzstar_bound}
\end{equation}
where $\|\partial_y f_\theta\|_{\mathrm{op}}$ is the operator norm of the Jacobian with respect to $y$, interpreted with respect to the continuous plan embedding $e(y)$ when $\YY$ is discrete, maximized over $(x,y,z^*)$.
For discrete $\YY$ (e.g., Sudoku grids), this bound applies in the learned embedding $e(y)$; see Remark~\ref{rem:reasoning_manifold}.
In practice, we monitor $\widehat{L}_{z^*}$ via finite differences in the embedding: for sampled $(x,y,y')$ pairs differing by one edit, compute $\|z^*(x,y) - z^*(x,y')\| / \|e(y) - e(y')\|$.
\end{remark}

\begin{remark}[On learned embeddings for discrete tasks]
\label{rem:reasoning_manifold}
For combinatorial tasks (Sudoku, ARC), a single cell change in Hamming distance can move from near-solved to unsatisfiable, making $L_{z^*}$ potentially huge in raw symbolic distance.
Assumption~\ref{ass:twotimescale} should be read as holding in a \emph{learned embedding} $e(y)$ that captures ``reasoning difficulty'' rather than raw symbolic distance.

Concretely, one can parameterize $e$ as a small neural network trained to predict $z^*$ or checker scores, regularized to be locally smooth.
The drift constant $\Cdrift(n)$ then quantifies how well the latent tracks a locally stable fixed point in this embedding.
\textbf{This is a design objective rather than a formal guarantee for discrete domains.}
We posit that successful training \emph{constructs} such a smooth manifold; if the network fails to learn a smooth embedding, the drift bound degenerates and training may diverge.
For tasks where $L_{z^*}$ is large even in learned embeddings, the bound becomes vacuous and the episodic-$z$ analysis (which has unconditional stability guarantees) is preferable.
In practice, we monitor $\hat L_{z^*}$ via finite differences as described in Remark~\ref{rem:Lzstar_implicit}, and $\widehat{\Delta y}_{\max}$ along trajectories.
The bound~\eqref{eq:Lzstar_bound} provides structural guidance: if $\|\partial_y f_\theta\|$ is large or $L_z$ is close to $1$, the drift bound degenerates regardless of the embedding.
\end{remark}

\begin{remark}[Scope of the slow-drift assumption]
\label{rem:slowdrift_scope}
Assumption~\ref{ass:twotimescale} is stated globally but in practice need only hold on the closure of the union of reachable plan sets $\bigcup_{\pi \in \Pi} \{y : (x,y) \in \mathcal{R}_\pi\}$.
For Sudoku, this is the set of partially-filled grids reachable by cell edits from initial configurations.
\end{remark}

Define tracking error $e_t = \bigl\|z_t - z^*(x,y_t)\bigr\|$.

\begin{lemma}[Drift bound for persistent-$z$]
\label{lem:drift_bound}
Under Assumptions~\ref{ass:Lip} and~\ref{ass:twotimescale}, with $\kappa_n := L_z^n$:
\begin{equation}
e_{t+1} \le \kappa_n\,e_t + L_{z^*}\,\Delta y_{\max},
\label{eq:tracking_recursion}
\end{equation}
and for the used latent $z_t^{(n)} = (\Pi_R \circ f_\theta)^{\circ n}(z_t,y_t,x)$:
\begin{equation}
\sup_t \bigl\|z_t^{(n)} - z^*(x,y_t)\bigr\| \le \Cdrift(n),
\end{equation}
where
\begin{equation}
\begin{split}
\Cdrift(n)
&= \kappa_n\,\max\!\Bigl(E_0,\,\frac{L_{z^*}\,\Delta y_{\max}}{1-\kappa_n}\Bigr) \\
&\le \kappa_n E_0 + \frac{\kappa_n L_{z^*}\,\Delta y_{\max}}{1-\kappa_n}.
\end{split}
\label{eq:Cdrift_def}
\end{equation}
The bound is tight for the linear recursion~\eqref{eq:tracking_recursion}.
For intuition, the looser but additive upper bound on the right-hand side of~\eqref{eq:Cdrift_def} cleanly separates an initial-error decay term from a steady-state tracking term.
For $L_z<1$ fixed, $\Cdrift(n) \to 0$ as $n\to\infty$.
\end{lemma}

The proof (Appendix~\ref{app:proofs}) follows from the contraction of $f_\theta^{\circ n}$ in $z$ and Lipschitz dependence of $z^*$ on $y$.
This is a worst-case $L_\infty$ bound that assumes error can accumulate at every step; it may be loose in practice if the model effectively ``solves'' hard steps.
We use it for structural guidance.

\begin{remark}[When the drift bound is meaningful]
\label{rem:drift_meaningful}
For $L_z = 0.8$ and $n = 5$, we have $\kappa_5 \approx 0.33$, giving denominator $1 - \kappa_5 \approx 0.67$.
If $L_{z^*} \Delta y_{\max}$ is moderate (e.g., $\le 1$ in the learned embedding), then using the additive upper bound in~\eqref{eq:Cdrift_def} gives $\Cdrift(5) \lesssim 0.33 E_0 + 0.5$.
Increasing to $n = 10$ gives $\kappa_{10} \approx 0.11$ and $\Cdrift(10) \approx 0.11 E_0 + 0.12$.
For tasks where $L_{z^*}$ is large even in learned embeddings, the bound degenerates and episodic-$z$ analysis is preferable.
\end{remark}


\section{Diagnostics and Practical Proxies}
\label{app:diagnostics}

Table~\ref{tab:diagnostics} shows the theoretical bound quantities and practical diagnostics/proxies typically logged during training.

\begin{table}[t]
\caption{Theory quantities (used in bounds) and practical diagnostics/proxies typically logged during training.}
\label{tab:diagnostics}
\centering
\small
\begin{tabular}{@{}lll@{}}
\toprule
Quantity & Appears in & Practical proxy / diagnostic \\
\midrule
$L_z^{\mathrm{pre}}$ & Assump.~\ref{ass:Lip} & Finite-diff Lipschitz of $f_\theta$ \emph{before} projection \\
$L_z^{\mathrm{post}}$ & Assump.~\ref{ass:Lip} & Finite-diff Lipschitz of $\tilde f_\theta{=}\Pi_R\circ f_\theta$ \emph{after} projection \\
projection\_active & App.~\ref{app:exp2_sweep} & Fraction of samples where $\|f_\theta(z)\|>R$ (clipped) \\
$C_z$ & Assump.~\ref{ass:Lip} & Empirical $\|\tilde f_\theta(z_{\mathrm{init}},y,x)-z_{\mathrm{init}}\|$ on batches \\
$L_V$ & Assump.~\ref{ass:bounded_value_head} & Spectral norm bound on $z\!\to\!V$ layers; finite-diff check in $z$ \\
$\|U_n-\mathcal{T}^\pi_K U_n\|$ & Prop.~\ref{prop:error_bounds} & Empirical $K$-step TD error / residual on held-out rollouts \\
$\varepsilon_{\mathrm{cent}}$ & Thm.~\ref{thm:monotone_with_error} & $\sup_s|\E_{a\sim\pi}\widehat A(s,a)|$ (exactly $0$ if baseline is exact) \\
$\varepsilon_{A,\mathrm{cand}}$ & Thm.~\ref{thm:monotone_with_error} & Empirical bias of $\E_{a\sim\pi_{\mathrm{cand}}}[\widehat A]$ on replay states \\
$\KL(\pi_{\mathrm{new}}\|\pi_\phi)$ & App.~\ref{app:implementation} & Distillation projection gap measured on replay states \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Component Diagnostic: Spectral Normalization Instability and a Robust Alternative}
\label{app:sn_instability}
The contraction assumption (Assumption~\ref{ass:Lip}) is a property of the latent update map $z\mapsto \tilde f_\theta(z,y,x)$.
In practice, we attempted to enforce it by applying power-iteration spectral normalization to linear layers in the inner recursion and then applying a strict scalar contraction factor.
However, on our TRM inner module this spectral normalization step can be numerically unstable.

\textbf{Protocol.}
We estimate a local Lipschitz proxy $\hat L_z$ by finite differences:
sample a small perturbation $\delta$ with $\|\delta\|_2=\varepsilon$ and compute
$\|\tilde f_\theta(z+\delta,y,x)-\tilde f_\theta(z,y,x)\|_2/\varepsilon$, averaging over random draws.
We compare six variants: (i) OFF (no normalization, no scaling), (ii) SN-only, (iii) SCALE-only (only explicit per-layer output scaling),
(iv) SN+SCALE, (v) CLAMP-only (operator-norm clamping without scaling), and (vi) CLAMP+SCALE.

\begin{table}[t]
\caption{Representative local Lipschitz proxy $\hat L_z$ for the inner latent update map under different ``contraction enforcement'' components (4$\times$4 Sudoku batch; $\varepsilon=10^{-3}$). Power-iteration spectral normalization (SN) can cause the proxy to explode in our inner recursion, whereas operator-norm clamping (CLAMP) remains stable.}
\label{tab:sn_instability}
\centering
\small
\begin{tabular}{@{}lcc@{}}
\toprule
Variant & $\hat L_z$ (proxy) & Status \\
\midrule
OFF & $\approx 0.86$ & stable \\
SN-only & $\approx 2.7\times 10^4$ & exploded \\
SCALE-only & $\approx 0.85$ & stable \\
SN+SCALE & $\approx 4.5\times 10^4$ & exploded \\
CLAMP-only & $\approx 0.6$ & stable \\
CLAMP+SCALE & $\approx 0.6$ & stable \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Takeaway.}
These results indicate that \emph{naively applying} power-iteration spectral normalization to this architecture is insufficient for reliable contraction control of the inner recursion.
In contrast, explicit operator-norm clamping is stable in this diagnostic and provides a practical path toward enforcing Assumption~\ref{ass:Lip} without the observed numerical blow-ups.

\subsection{Latent Collapse Diagnostic: Isolating $z\!\to\!z$ Contraction from Value-Head Normalization}
\label{app:latent_collapse_diagnostic}

A natural hypothesis for why $z\!\to\!z$ contraction enforcement can hurt success rates is \emph{latent collapse}: contraction might drive all latent representations toward zero, destroying discriminative information.
To test this, we ran a controlled diagnostic isolating the effect of $z\!\to\!z$ contraction from value-head normalization (which also affects $\mathrm{Var}(V)$ but is architecturally separate).

\textbf{Protocol.}
We use a $2\times 2$ factorial design on the 4$\times$4 Sudoku dataset (1--4 empties, single seed, unroll depth $n{=}16$, batch size 128, projection radius $R{=}10$):
\begin{itemize}
\item \textbf{Axis 1 ($z\!\to\!z$ contraction):} OFF (baseline) vs.\ ON (target $L_z=0.9$).
\item \textbf{Axis 2 (value-head normalization):} OFF (baseline) vs.\ ON (target $L_V=1.0$).
\end{itemize}
This yields four conditions: A (both OFF), B ($z\!\to\!z$ ON, value-head OFF), C ($z\!\to\!z$ OFF, value-head ON), D (both ON).
We measure pre-projection latent norms $\|z^{(n)}_{\text{pre}}\|$ and total variance $\mathrm{Var}(z^{(n)}_{\text{pre}})$, pairwise cosine similarity, value variance $\mathrm{Var}(V)$, and post-projection norms $\|z^{(n)}_{\text{post}}\|$ where $z^{(n)}_{\text{post}}=\Pi_R(z^{(n)}_{\text{pre}})$.

\begin{table}[t]
\caption{Latent collapse diagnostic at $n{=}16$ unroll depth.
Comparing B vs.\ A isolates $z\!\to\!z$ contraction effect; comparing C vs.\ A isolates value-head normalization effect.
Pre-projection metrics are measured before the ball projection $\Pi_R$; post-projection norms are $\|z^{(n)}_{\text{post}}\|=\|\Pi_R(z^{(n)}_{\text{pre}})\|$.}
\label{tab:latent_collapse}
\centering
\small
\begin{tabular}{@{}lccccccc@{}}
\toprule
Cond. & $z\!\to\!z$ & V-head & $\|z^{(n)}_{\text{pre}}\|$ & $\mathrm{Var}(z^{(n)}_{\text{pre}})$ & CosSim & $\mathrm{Var}(V)$ & $\|z^{(n)}_{\text{post}}\|$ \\
\midrule
A & OFF & OFF & 32.0 & 814.0 & 0.196 & 0.016 & 10.0 \\
B & ON & OFF & 32.0 & 608.7 & 0.403 & 0.016 & 10.0 \\
C & OFF & ON & 32.0 & 814.0 & 0.200 & 2493 & 10.0 \\
D & ON & ON & 32.0 & 608.7 & 0.396 & 2412 & 10.0 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Key observations.}
\begin{itemize}
\item \textbf{$z\!\to\!z$ contraction causes modest clustering, not collapse-to-zero.}
Comparing B vs.\ A: total variance decreases by $\sim$25\% (ratio 0.75) and pairwise cosine similarity increases by $\sim$0.21.
However, pre-projection norms remain $\|z^{(n)}_{\text{pre}}\|\approx 32$, well above the projection radius $R{=}10$.
This indicates the latents become modestly more clustered in direction, but do \emph{not} collapse toward zero.
\item \textbf{Huge $\mathrm{Var}(V)$ differences come from value-head normalization, not $z\!\to\!z$ contraction.}
Comparing C vs.\ A: $\mathrm{Var}(V)$ increases by $\sim$160{,}000$\times$ (from 0.016 to 2493) when value-head normalization is enabled, while B vs.\ A shows minimal change (ratio $\approx$1.04).
Earlier observations of exploding $\mathrm{Var}(V)$ with ``contraction ON'' were confounded by value-head normalization.
\item \textbf{Projection saturation explains constant post-projection norms.}
All conditions have pre-projection $\|z^{(n)}_{\text{pre}}\|\approx 32 \gg R{=}10$, so the ball projection $\Pi_R$ is always active, clipping to $\|z^{(n)}_{\text{post}}\|=10$.
\end{itemize}

\textbf{Interpretation.}
This diagnostic provides evidence \emph{against} the hypothesis that $z\!\to\!z$ contraction causes performance degradation via latent collapse-to-zero.
The observed 25\% variance reduction and increased cosine similarity are modest and do not explain the substantial success-rate differences seen in ablations.
The mechanism by which contraction enforcement hurts task success (when it does) remains unclear; one possibility is reduced expressivity of latent dynamics rather than representational collapse.

\textbf{Training dynamics confirmation.}
In a follow-up 2$\times$2 training ablation (200 steps) that toggles $z\!\to\!z$ contraction and value-head normalization independently, we observe: (i) $z\!\to\!z$ contraction alone (B) remains stable with targets $\approx -1.7$; (ii) value-head normalization alone (C) induces severe target clamp-hitting ($\pm 20$); (iii) both combined (D) collapses to the positive clamp (target${}=20$ by step 60, $V(s)=20$ by step 100).
This confirms that the instability previously attributed to ``enable\_contraction'' is driven by the value-head normalization component (spectral normalization + $L_V$ scaling), not by $z\!\to\!z$ contraction itself, in this diagnostic setting.
Consistent with this, in short runs on the 4$\times$4 suite (1--4 empties), enabling $z\!\to\!z$ contraction while disabling value-head normalization remains stable but achieves low success (${\approx}10\%$ at 1k steps), suggesting that contraction can be overly restrictive even when it does not induce collapse.

\textbf{Caveat:} This diagnostic uses a single seed on the 4$\times$4 dataset (1--4 empties); it provides evidence against collapse but is not a universal statement.

\longonly{
\subsection{Baseline Comparisons}
\label{app:baseline_comparison}

On 4$\times$4 Sudoku (1--4 empties), UPI--TRM reaches $90$--$93\%$ success across variants.
``Success'' means producing a feasible solution (zero violations) within horizon $T{=}16$.
The Random baseline samples uniformly over valid (masked) actions; its high 52\% success reflects the tiny search space in this ultra-easy regime.
On 6--8 empties, random success drops to 0\% (Figure~\ref{fig:feasibility_6to8empties_appendix}).
Results are descriptive given limited seeds.
A random policy achieves $\approx 52\%$ on this suite; baselines plateau below random.
Our intent is to isolate checker-only learning dynamics, not to claim state-of-the-art Sudoku solving.
See Section~\ref{sec:experiments} in the long version for full results and discussion.

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/trivial_baselines_vs_no_contraction_success_vs_steps.pdf}
\caption{4$\times$4 Sudoku (1--4 empties): success rate (feasible within $T{=}16$) vs.\ training steps. \textbf{UPI--TRM} variants (episodic/persistent, with/without contraction) vs.\ standard RL baselines (PPO/A2C/DQN). Random samples uniformly over valid actions and achieves 52\% via chance in this tiny search space; in our runs (3 seeds, 5000 steps), the RL baselines plateau below random. Results are descriptive.}
\label{fig:trivial_baselines_vs_no_contraction}
\end{figure}}

\shortonly{
\subsection{Feasibility Results and Harder Suite}
\label{app:baseline_comparison}
\label{app:harder_sudoku}

See Section~\ref{sec:feasibility_toy} in the main text for toy-suite feasibility results.
The harder 4$\times$4 Sudoku suite (6--8 empties) is detailed in Appendix~\ref{app:harder_sudoku_detailed}.
}

\subsection{Isolation Study: Detailed Tables}
\label{app:exp1_tables}

Tables~\ref{tab:unroll_sensitivity} and~\ref{tab:radius_sweep} provide the full numeric results for the B0 isolation study (Section~\ref{sec:isolation_study}).

\input{tables/table_exp1_unroll_sensitivity.tex}
\input{tables/table_exp1_radius_sweep_main.tex}

\subsection{Isolation Study: B1 Successor States}
\label{app:exp1_b1}

We present the full B1 (one-step successor states from B0) results from the isolation study (Section~\ref{sec:isolation_study}).

\textbf{Unroll sensitivity on B1.}
On successor states, contraction still improves policy KL ($\Delta_\pi$) and action agreement at all depth mismatches, though the improvements are smaller than on B0 initial states (Figure~\ref{fig:exp1_unroll_sensitivity_b1}).

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/fig_exp1_unroll_sensitivity_appendix.pdf}
\caption{Unroll sensitivity on B1 (successor states). Training depth $n_{\mathrm{train}}{=}2$, evaluated at $n_2 \in \{4, 8, 16\}$. ``No Contraction'' vs.\ ``Contraction'' ($L_z{=}0.9$); the \emph{only} difference is \texttt{enable\_contraction}. Both conditions: value-head spectral norm disabled, projection $R{=}10$. Seeds: 41, 42, 43. Left: $\Delta_V$ (value instability, log scale). Center: $\Delta_\pi$ (policy KL, log scale). Right: Argmax Agreement. Contraction improves $\Delta_\pi$ and argmax, but $\Delta_V$ improvement is less pronounced than on B0. Error bars: mean$\pm$std ($\Delta_V$, $\Delta_\pi$); Wilson 95\% CI (argmax).}
\label{fig:exp1_unroll_sensitivity_b1}
\end{figure}

\textbf{Radius sweep on B1.}
On B1 at $R{=}0$, contraction consistently improves latent drift ($\Delta_z$: $30.9 \to 16.6$, i.e., $1.9\times$) and action agreement ($64.0\% \to 90.5\%$), but value instability $\Delta_V$ does \emph{not} improve ($1.13 \to 1.65$)---it actually \emph{increases}.
This suggests that on successor states, value estimates may exhibit higher variance without projection, even under contraction.

\textbf{Important scoping note:} The main $R{=}0$ claim (Section~\ref{sec:isolation_study}) is therefore scoped to B0 initial states only.
On B1 with $R{=}0$, contraction improves $\Delta_z$ and argmax, but \emph{not} $\Delta_V$.

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/fig_exp1_radius_sweep_appendix.pdf}
\caption{Projection radius sweep on B1 (successor states). ``No Contraction'' vs.\ ``Contraction'' ($L_z{=}0.9$) across $R\in\{0, 10, 100\}$, where $R{=}0$ denotes \emph{projection disabled} ($\Pi_R$ set to the identity map). Both conditions: value-head spectral norm disabled. Left: $\Delta_V$. Center: $\Delta_z$. Right: argmax agreement. At $R{=}0$, contraction improves $\Delta_z$ and argmax but \emph{not} $\Delta_V$. Seeds: 41, 42, 43. Error bars: mean$\pm$std ($\Delta_V$, $\Delta_z$); Wilson 95\% CI (argmax).}
\label{fig:exp1_radius_sweep_b1}
\end{figure}

\input{tables/table_exp1_radius_sweep_appendix.tex}

\subsection{Projection Dominance Study}
\label{app:exp2_sweep}

We attempted to validate a ``contraction dial'' by sweeping the target $L_z^*\in\{0.9, 0.95, 0.99\}$.
All models are trained on the same 4$\times$4 Sudoku suite (1--4 empties) with \texttt{disable\_value\_head\_norm: true}, evaluated at $8\times$ depth mismatch on B0 (initial states).

\textbf{Negative result: dial does not control achieved $L_z$.}
Figure~\ref{fig:exp2_dial_does_not_control_Lz} and Table~\ref{tab:exp2_dial_failure} show that the achieved post-projection contraction modulus $\hat L_z^{\mathrm{post}}$ is essentially identical across dial settings ($\approx 0.01$--$0.02$), and critically, the \emph{ordering} of dial values does not produce a monotonic ordering in stability metrics.
The dial has insufficient range to meaningfully control the achieved Lipschitz constant.

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/fig_exp2_dial_does_not_control_Lz.pdf}
\caption{The contraction ``dial'' ($L_z^*$) does not meaningfully control achieved $\hat L_z^{\mathrm{post}}$: all three settings collapse to $\approx 0.01$--$0.02$. The ordering of dial values does not produce monotonic stability. All conditions: value-head spectral norm disabled, projection $R{=}10$. \emph{No monotonic stability-dial claim is supported.}}
\label{fig:exp2_dial_does_not_control_Lz}
\end{figure}

\input{tables/table_exp2_dial_does_not_control_Lz.tex}

\textbf{Positive result: projection is a major stabilizer.}
Separately, Figure~\ref{fig:exp2_projection_stabilizer} and Table~\ref{tab:exp2_projection_stabilizer} show that projection at $R{=}10$ provides substantial stabilization: $\Delta_V$ improves $6$--$10\times$ and argmax agreement improves $\approx 8$--$10$~pp compared to projection disabled ($R{=}0$).
At $R{=}10$, projection is active 100\% of the time, which can mask any contraction-related effects.

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/fig_exp2_projection_is_primary_stabilizer.pdf}
\caption{Projection ($R{=}10$ vs.\ $R{=}0$, where $R{=}0$ denotes \emph{projection disabled} and $\Pi_R$ is set to the identity map) is a major stabilizer: $\Delta_V$ improves $6$--$10\times$; argmax agreement improves $8$--$10$~pp. All conditions: value-head spectral norm disabled, $z\!\to\!z$ contraction ON ($L_z{=}0.9$). When projection is active 100\%, it can dominate stability effects.}
\label{fig:exp2_projection_stabilizer}
\end{figure}

\input{tables/table_exp2_projection_effect.tex}

\textbf{Conclusion.}
\emph{No monotonic stability-dial claim is supported by these data.}
The spectral-norm dial mechanism does not meaningfully vary the achieved contraction modulus in this regime, while projection (when active) acts as a strong stabilizer.
The isolation study findings (contraction vs.\ no-contraction at $R{=}0$) remain valid but should be interpreted as a binary effect, not a tunable dial.

\subsection{Projection-Free Dial Range Test}
\label{app:exp4_projection_free_dial}

The projection dominance study shows that the spectral-norm target $L_z^*$ is not an effective dial when projection is active: the achieved post-projection Lipschitz proxy collapses to the same value across settings, and projection can dominate/mask any contraction-related effects.
Here we test whether a \emph{projection-free} mechanism can induce a wider range of achieved contraction.

\textbf{Method.}
With projection disabled ($R{=}0$) and value-head spectral norm disabled, we apply inference-time scalar scaling to the $z\!\to\!z$ update weights of a single trained checkpoint.
We measure the achieved pre-projection Lipschitz proxy $L^{\mathrm{pre}}_z$ and stability metrics ($\Delta_V$, argmax agreement) at $8\times$ depth mismatch on 100 B0 samples.

\input{tables/table_exp4_projection_free_dial_range_test.tex}

\textbf{Result.}
The achieved $L^{\mathrm{pre}}_z$ span is large ($\approx 0.52$--$0.95$; spread $\approx 0.43$), demonstrating that inference-time scaling \emph{can} vary the Lipschitz proxy when projection is disabled.
However, \emph{monotonic linkage} to $\Delta_V$ or argmax is \textbf{inconclusive} in this range test: $\Delta_V$ does not decrease monotonically with $L^{\mathrm{pre}}_z$, and argmax shows non-monotone behavior.
This range test is a feasibility diagnostic only---it does not establish a tunable stability dial.

\subsection{Training Stability Ablation}
\label{app:exp3_projection_ablation}

We ablate projection at \emph{training time} to test whether the ball projection $\Pi_R$ is necessary for stable training, as opposed to its role in evaluation-time mismatch stability (see projection dominance study above).

\input{tables/table_exp3_projection_ablation.tex}

\textbf{Key findings.}
Training remains stable without projection: all 6 conditions (contraction ON/OFF crossed with $R\in\{10, 100, \text{disabled}\}$) pass the stability gate (no NaN, no gradient collapse), and success rates are comparable ($94$--$98\%$) regardless of whether projection is enabled.
Projection can still dominate mismatch stability at \emph{evaluation time} when active, but it is not required for \emph{training stability}.

\subsection{Projection-Free Contraction Dial (Full Results)}
\label{app:exp4_projection_free_dial_v2}

\shortonly{See Section~\ref{sec:exp4_main} in the main text for the key result and scatter plot.
Table~\ref{tab:exp4_projection_free_dial_v2} below provides the full numeric results at $8\times$ depth mismatch.

\input{tables/table_exp4_projection_free_dial_v2.tex}}

\subsection{Stability--Expressivity Tradeoff (Full Results)}
\label{app:exp5_tradeoff_curve}

\shortonly{This appendix provides an exploratory stability--expressivity analysis.
\textbf{Important:} Success rates here (${\approx}6.7\%$) are \emph{not} comparable to Table~\ref{tab:upi_success_only} (${\approx}93\%$) because they use different evaluation protocols: Exp5 uses \emph{matched compute} ($n_\mathrm{eval}{=}2$, projection disabled $R{=}0$) to isolate the contraction dial effect, while Table~\ref{tab:upi_success_only} uses $n_\mathrm{eval}{=}2$ with mixed projection settings (no-contraction: $R{=}10$; contraction: $R{=}0$). The low matched-compute success in Exp5 reflects the inherent difficulty of solving puzzles with only 2 unroll steps under uniform $R{=}0$, not model deficiency.

Using inference-time scaling (scales $\in \{1.0, 0.85, 0.70, 0.55\}$, projection disabled $R{=}0$, value-head spectral normalization disabled), we observe that stability varies modestly across scale settings while task success remains essentially flat ($\approx 6.7\%$); we do not observe a pronounced tradeoff in this regime.
Table~\ref{tab:exp5-tradeoff} provides the numeric results.

\input{tables/table_exp5_tradeoff_curve.tex}

Figure~\ref{fig:exp5_L_vs_metrics} shows achieved $\hat L^{\mathrm{pre}}_z$ vs.\ both stability and success.

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/fig_exp5_L_vs_metrics.pdf}
\caption{Achieved $\hat L^{\mathrm{pre}}_z$ (x-axis) vs.\ argmax agreement [\%] ($n{=}2 \to 16$, left y-axis, blue) and task success [\%] (right y-axis, green). Scale annotations show $s$. \textbf{Note:} Success rates use matched compute ($n{=}2$, $R{=}0$); Table~\ref{tab:upi_success_only} uses $n{=}2$ with mixed $R$ (see text).}
\label{fig:exp5_L_vs_metrics}
\end{figure}
}

\longonly{
\textbf{Setup.}
We extend the projection-free dial test to directly visualize the stability--expressivity tradeoff.
Using the same inference-time scaling mechanism (scales $\in \{1.0, 0.85, 0.70, 0.55\}$, projection disabled $R{=}0$, value-head spectral norm disabled), we measure both:
(1) mismatch stability (argmax agreement at $8\times$ depth mismatch), and
(2) task success on the 4$\times$4 Sudoku suite (1--4 empties).

\textbf{Evaluation protocol clarification.}
Success rates in this appendix (${\approx}6.7\%$) differ from Table~\ref{tab:upi_success_only} (${\approx}93\%$) due to different evaluation protocols.
Exp5 uses \emph{matched compute}: $n_\mathrm{eval}{=}n_\mathrm{train}{=}2$ with projection disabled ($R{=}0$), chosen to isolate the contraction dial effect at a controlled compute budget.
Table~\ref{tab:upi_success_only} uses $n_\mathrm{eval}{=}2$ with mixed projection settings (no-contraction: $R{=}10$; contraction: $R{=}0$).
The low matched-compute success in Exp5 reflects the inherent difficulty of solving puzzles with only 2 unroll steps under uniform $R{=}0$, not model deficiency.

\textbf{Key result.}
Stability (argmax agreement) varies across scale settings, but task success remains essentially flat ($\approx 6.7\%$--$7.0\%$).
We do not observe a clear tradeoff in this regime; the narrow success range relative to variance does not support claims of a stability--success frontier.

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/fig_exp5_tradeoff_curve.pdf}
\caption{Stability vs.\ task success. X-axis: argmax agreement [\%] ($n_\mathrm{train}{=}2 \to n_\mathrm{eval}{=}16$). Y-axis: task success [\%] on 4$\times$4 suite (1--4 empties; $N{=}100$ episodes; $T{=}16$ horizon). Gray points: individual checkpoints; colored markers: scale means $\pm$std; dashed line connects means ordered by scale. Projection disabled ($R{=}0$). \textbf{Note:} Success uses matched compute ($n{=}2$, $R{=}0$); Table~\ref{tab:upi_success_only} uses $n{=}2$ with mixed $R$.}
\label{fig:exp5_tradeoff_longonly}
\end{figure}

Table~\ref{tab:exp5-tradeoff} provides the full numeric results.

\input{tables/table_exp5_tradeoff_curve.tex}

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/fig_exp5_L_vs_metrics.pdf}
\caption{Achieved $\hat L^{\mathrm{pre}}_z$ (x-axis) vs.\ argmax agreement [\%] ($n{=}2 \to 16$, left y-axis, blue) and task success [\%] (right y-axis, green). Scale annotations show $s$. Same data as Figure~\ref{fig:exp5_tradeoff_longonly}, parameterized by achieved Lipschitz constant.}
\label{fig:exp5_L_vs_metrics_longonly}
\end{figure}
}

\subsection{Harder 4$\times$4 Sudoku (6--8 Empties)}
\label{app:harder_sudoku_detailed}

We extend evaluation to a harder suite with 6--8 empty cells, where the random-policy success rate drops to 0\%.

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/table3_hard_controlled_curves.pdf}
\caption{Harder 4$\times$4 Sudoku (6--8 empties): success rate (feasible within $T{=}16$) vs.\ training steps (20k steps, 3 seeds, greedy argmax + action masking, eval every 100 steps, 50 episodes).
Controlled 2$\times$2 ablation (contraction $\times$ projection): no-contraction with projection ($R{=}10$) reaches ${\sim}57\%$; contraction variants reach ${\sim}37$--$47\%$.
Random (uniform over valid actions) achieves 0\%---the search space is now too large for chance completion.
Results are descriptive (3 seeds).}
\label{fig:feasibility_6to8empties_appendix}
\end{figure}

\longonly{
\textbf{Setup.}
We test whether the $z\!\to\!z$ recursion can serve as a practical \emph{contraction dial} when projection is disabled (or inactive).
All evaluations use projection disabled ($R{=}0$) and value-head normalization OFF.
We apply inference-time scalar scaling to the $z\!\to\!z$ layer weights of a single trained checkpoint, varying the scale in $\{1.0, 0.85, 0.70, 0.55\}$.
The evaluation protocol matches the isolation study: training depth $n_{\mathrm{train}}{=}2$, evaluation depths $n_2 \in \{4, 8, 16\}$ (depth multipliers $2\times$, $4\times$, $8\times$), measured on both B0 (100 initial puzzle states) and B1 (one-step successors).

\textbf{Key result.}
Lower achieved pre-projection Lipschitz proxy $L^{\mathrm{pre}}_z$ correlates monotonically with improved depth-mismatch stability: higher argmax action agreement and lower value instability $\Delta_V$ on both B0 and B1.
Table~\ref{tab:exp4_projection_free_dial_v2} summarizes the results at $8\times$ depth mismatch ($n_{\mathrm{train}}{=}2 \to n_2{=}16$).
This contrasts with the projection dominance study (Appendix~\ref{app:exp2_sweep}), where the spectral-norm target $L_z^*$ did not yield a monotonic ordering---there, projection was active and masked contraction effects.
The critical condition for this dial to be visible is that projection must be disabled or inactive.

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/fig_exp4_dial_scatter.pdf}
\caption{Projection-free dial: $L^{\mathrm{pre}}_z$ vs.\ argmax agreement scatter plot.
Projection disabled ($R{=}0$), value-head spectral norm disabled, evaluated at $n_2{=}8$ ($4\times$ depth mismatch).
Points colored by seed, shaped by scale.
Strong negative correlation (B0: $\rho{=}{-}0.87$; B1: $\rho{=}{-}0.92$) confirms that lower achieved $L^{\mathrm{pre}}_z$ yields higher argmax agreement.}
\label{fig:exp4_dial_scatter}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/fig_exp4_scale_comparison.pdf}
\caption{Projection-free dial: scale effect on $L^{\mathrm{pre}}_z$ and argmax agreement.
Bar chart showing mean$\pm$std across seeds for each scale setting.
Lower scale yields lower $L^{\mathrm{pre}}_z$ and higher argmax on both B0 and B1.}
\label{fig:exp4_scale_comparison}
\end{figure}

\input{tables/table_exp4_projection_free_dial_v2.tex}}

\subsection{Controlled 2$\times$2 (Contraction $\times$ Projection) on Hard Suite}
\label{app:controlled_2x2}

The comparison in Figure~\ref{fig:feasibility_6to8empties_appendix} is confounded: no-contraction variants use projection ($R{=}10$) while the contraction variant uses projection disabled.
To deconfound, we ran a controlled 2$\times$2 factorial (contraction ON/OFF $\times$ projection ON/OFF) on the same hard 4$\times$4 Sudoku suite (6--8 empties, 20k steps, 3 seeds).

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/table3_hard_controlled_2x2_bar.pdf}
\caption{Controlled 2$\times$2 (contraction $\times$ projection) on hard 4$\times$4 Sudoku (6--8 empties, $T{=}16$, 20k steps).
Final success rates: \textbf{No Contraction + $R{=}0$:} 33\% $\pm$ 8\%; \textbf{No Contraction + $R{=}10$:} 57\% $\pm$ 4\%; \textbf{Contraction + $R{=}0$:} 37\% $\pm$ 3\%; \textbf{Contraction + $R{=}10$:} 47\% $\pm$ 8\%.
Projection ($R{=}10$) is the dominant main effect, increasing success by 10--24pp.
Contraction has a modest positive effect at $R{=}0$ but is slightly negative at $R{=}10$, revealing an interaction.
Best configuration: no contraction with projection (57\%).
Results are descriptive (3 seeds).}
\label{fig:controlled_2x2_bar}
\end{figure}

\textbf{Key findings (descriptive, 3 seeds).}
\begin{enumerate}
\item \textbf{Projection is the dominant main effect.} Enabling projection ($R{=}10$) increases success by 10--24pp in these runs, regardless of contraction status.
\item \textbf{Contraction's effect depends on projection.} At $R{=}0$ (projection OFF), contraction yields a small benefit (37\% vs.\ 33\%, +4pp). At $R{=}10$ (projection ON), contraction is slightly negative (47\% vs.\ 57\%, $-$10pp).
\item \textbf{Best configuration.} No contraction with projection ($R{=}10$) achieves the highest success (57\% $\pm$ 4\%) in these 3-seed runs.
\end{enumerate}

This deconfounds the hard-suite narrative: contraction does not uniformly hurt performance; rather, projection is the primary factor.
On hard tasks, practitioners should prioritize enabling projection; whether contraction helps or hurts depends on the projection setting.

\subsection{Discussion Details}
\label{app:discussion_details}

\textbf{Reconciling the stability experiments.}
Our studies address distinct questions:
(1)~The isolation study (Section~\ref{sec:isolation_study}) shows that $z\!\to\!z$ contraction improves \emph{evaluation-time} depth-mismatch stability on initial states;
(2)~The projection dominance ablation (Appendix~\ref{app:exp2_sweep}) reveals that projection can dominate and mask contraction effects when active;
(3)~The training stability ablation (Appendix~\ref{app:exp3_projection_ablation}) confirms projection is not required for \emph{training} stability;
(4)~The projection-free dial test (Section~\ref{sec:exp4_main}) demonstrates that with projection disabled, the $z\!\to\!z$ contraction modulus becomes a working dial with monotonic correlation to stability.
We include an exploratory stability--expressivity tradeoff analysis in Appendix~\ref{app:exp5_tradeoff_curve}; success rates there (${\approx}6.7\%$) use matched compute ($n{=}2$, $R{=}0$) and are not directly comparable to Table~\ref{tab:upi_success_only} (which uses $n{=}2$ but mixed projection settings).
Together, these experiments validate our theoretical decomposition while revealing practical nuances.

\textbf{Practical recommendations.}
Start with projection enabled at moderate $R$ for training stability, then disable projection to expose the contraction dial for inference-time tuning.
Use persistent-$z$ unless edit dynamics cause large jumps.
Treat $(L_z, n)$ as coupled dials: stronger contraction allows fewer unroll steps; weaker contraction requires larger $n$.
Monitor gradient variance and value instability under depth mismatch as diagnostic signals.

\textbf{When contraction is too restrictive.}
On harder tasks (6--8 empties, Appendix~\ref{app:harder_sudoku_detailed}), enforcing $L_z < 1$ yields ${\sim}37\%$ success while unconstrained variants reach ${\sim}48$--$57\%$.
However, this comparison is confounded by projection asymmetry (no-contraction used $R{=}10$; contraction used projection disabled).
A controlled 2$\times$2 (contraction $\times$ projection) experiment on the same hard suite (Figure~\ref{fig:controlled_2x2_bar}, Appendix~\ref{app:controlled_2x2}) reveals that \emph{projection is the dominant main effect}: enabling $R{=}10$ increases success by ${\sim}10$--24pp regardless of contraction.
Contraction's effect depends on projection: at $R{=}0$, contraction yields a small benefit (37\% vs.\ 33\%); at $R{=}10$, contraction is slightly negative (47\% vs.\ 57\%).
The best configuration is no-contraction with projection ($R{=}10$, 57\%).
The contraction assumption is \emph{sufficient} for our error bounds but not necessary for good performance; practitioners should treat $L_z$ as a tunable stability dial, not a hard constraint.

\subsection{Baseline Hyperparameter Tuning Study}
\label{app:baseline_tuning}

A potential concern is whether the 0\% success rate of standard RL baselines (PPO, A2C, DQN) on hard 4$\times$4 Sudoku reflects inadequate hyperparameter tuning rather than a difference in algorithm/architecture design.
To address this, we conducted an extensive hyperparameter sweep: 11 configurations across three algorithms, each trained for 20,000 steps on the 6--8 empties suite (matching UPI-TRM's training budget).

\textbf{Tuning strategies.}
We explored four categories of hyperparameters:
\begin{enumerate}[nosep]
\item \textbf{Exploration:} High entropy coefficients ($\mathrm{entropy\_coef}{=}0.2$ vs.\ default $0.05$), extended $\varepsilon$-decay for DQN ($\mathrm{exploration\_fraction}{=}0.5$).
\item \textbf{Network depth:} Deeper inner unrolling ($n{=}8$ vs.\ default $n{=}2$) to allow more reasoning steps.
\item \textbf{Learning dynamics:} Higher learning rates, more PPO epochs, longer rollout horizons.
\item \textbf{Reward shaping:} Stronger violation penalties ($\mathrm{violation\_weight}{=}5.0$), larger failure penalties ($\mathrm{fail\_reward}{=}{-}32$).
\end{enumerate}

\begin{table}[t]
\caption{Hyperparameter tuning results on hard 4$\times$4 Sudoku (6--8 empties, 20k steps, seed 42).
All 11 configurations achieve 0\% success rate.
Score: mean cells correct out of 16 (initial ${\approx}9.0$).
Best result: A2C high-entropy reaches 11.66/16 but solves zero puzzles.}
\label{tab:baseline_tuning}
\centering
\small
\begin{tabular}{llcc}
\toprule
Algorithm & Configuration & Success & Score \\
\midrule
A2C & high\_entropy ($\mathrm{ent}{=}0.2$) & 0\% & \textbf{11.66} \\
A2C & deep\_unroll ($n{=}8$) & 0\% & 9.36 \\
A2C & high\_lr\_rollout & 0\% & 8.88 \\
A2C & strong\_reward & 0\% & 7.60 \\
\midrule
DQN & slow\_explore & 0\% & 6.86 \\
DQN & deep\_unroll ($n{=}8$) & 0\% & 6.16 \\
DQN & strong\_reward & 0\% & 6.08 \\
DQN & large\_buffer & 0\% & 1.06 \\
\midrule
PPO & high\_entropy & 0\% & 5.34 \\
PPO & deep\_unroll ($n{=}8$) & 0\% & 4.76 \\
PPO & strong\_reward & 0\% & 0.44 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Results} (Table~\ref{tab:baseline_tuning}).
All 11 configurations achieved \textbf{0\% success rate}---none solved a single puzzle out of 50 evaluated per configuration (550 total puzzles, 0 solved).
The 95\% confidence interval for the true success rate is $[0\%, 0.67\%]$, substantially below UPI-TRM's 37--57\%.

\textbf{Partial progress but no solutions.}
The best configuration (A2C with high entropy) improved mean score from 9.0 to 11.66/16, demonstrating that learning occurred.
However, the gap of ${\sim}4$ cells from a solved puzzle proved insurmountable: standard RL can fill obvious cells but cannot perform the multi-step constraint propagation needed for the remaining ambiguous cells.

\textbf{Why tuning fails.}
\begin{itemize}[nosep]
\item \textbf{Combinatorial credit assignment:} With 6--8 empty cells, the agent must place multiple interdependent digits correctly. A single wrong choice cascades into constraint violations that the policy cannot recover from.
\item \textbf{No backtracking:} Standard RL commits to actions sequentially without explicit backtracking. UPI-TRM's value-based planning implicitly evaluates consequences before committing.
\item \textbf{Local vs.\ global reasoning:} Even with $n{=}8$ inner unroll steps, standard baselines perform local pattern matching rather than global constraint satisfaction. UPI-TRM's latent contraction and Bellman backup propagate long-horizon reward signals that enable coordinated multi-cell decisions.
\end{itemize}

\textbf{Conclusion.}
The 0\% baseline performance is robust to hyperparameter tuning.
The gap between standard RL (0\%) and UPI-TRM (37--57\%) in our experiments suggests that the UPI--TRM update + evaluator design provides an advantage in this setting, though we cannot rule out success from alternative algorithms, larger sweeps, or longer training.

\section{Notation Summary}
\label{app:notation}

We summarize notation here for convenience.
Table~\ref{tab:notation} lists additional symbols used in proofs.

\begin{table}[t]
\caption{Additional symbols and notations}
\label{tab:notation}
\begin{center}
\begin{tabular}{ll}
\toprule
Symbol & Meaning \\
\midrule
$U_n(s)$, $\tilde U_n(\tilde s)$ & Unrolled value (episodic-$z$/persistent-$z$) \\
$U_*(s)$ & Ideal fixed-point latent value \\
$V_{\mathrm{memless}}(x,y,z) = U_*(x,y)$ & Memoryless value (ignores $z$) \\
$L_{z^*}$, $\Delta y_{\max}$, $E_0$ & Slow-drift parameters \\
$\varepsilon_A$, $\varepsilon_A^{\mathrm{pers}}$ & Advantage error bounds \\
$\varepsilon_{\mathrm{res}}^*$ & Architectural residual at $U_*$ \\
$\varepsilon_{\mathrm{CPI}}$ & CPI constant \\
$\kappa_n = L_z^n$ & Contraction after $n$ steps \\
$\mathcal{R}_\Pi$ & Union of reachable sets for policy family $\Pi$ \\
$\supnorm{f}{\Pi}$ & Sup-norm over $\mathcal{R}_\Pi$ \\
$B_V(s)$ & Baseline: $\E_{b \sim \pi}[\widehat{Q}_V(s,b)]$ \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\section{Constructive Definition of the Advantage-Evaluation Closure}
\label{app:adv_closure_full}

\shortonly{
\begin{definition}[Reachable set]
\label{def:reachable}
For a policy $\pi$, let $\mathcal{R}_\pi$ be the set of states $s$ such that
$\Pr_\pi(\exists\, t\ge 0:\; s_t=s \mid s_0\sim\rho)>0$, where $\rho$ is the initial-state distribution.
\end{definition}

\begin{definition}[Advantage-evaluation closure]
\label{def:adv_closure}
Fix a current policy $\pi$ and a candidate policy $\pi_{\mathrm{cand}}$.
Let $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}$ be any state set that contains $\mathcal{R}_\pi$ and all one-step successors of states in $\mathcal{R}_\pi$ obtained by actions in $\mathrm{supp}(\pi(\cdot|s))\cup\mathrm{supp}(\pi_{\mathrm{cand}}(\cdot|s))$, closed under forward dynamics of $\pi$.
We write $\|f\|_{\infty,\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}} := \sup_{s\in \mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}} |f(s)|$.
\end{definition}
}

For completeness we give a constructive minimal choice for $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}$ consistent with Definition~\ref{def:adv_closure}.

Fix a current policy $\pi$ and a candidate policy $\pi_{\mathrm{cand}}$.
Define the \emph{one-step deviation} set
\begin{equation}
\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{+}
:=
\mathcal{R}_\pi
\;\cup\;
\bigl\{\,s' : \exists\, s\in\mathcal{R}_\pi,
\exists\, a\in \mathrm{supp}(\pi(\cdot|s))\cup\mathrm{supp}(\pi_{\mathrm{cand}}(\cdot|s))
\text{ with } P(s'\mid s,a)>0\,\bigr\}.
\label{eq:R_plus_def_app}
\end{equation}
Let $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}$ denote the smallest superset of $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{+}$ that is closed under forward dynamics of $\pi$:
\begin{align}
\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}
&:=
\bigcup_{t\ge 0} \mathcal{R}^{(t)},
\qquad
\mathcal{R}^{(0)}=\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{+},
\notag\\
\mathcal{R}^{(t+1)}&=\mathcal{R}^{(t)}\cup\{s' : \exists s\in \mathcal{R}^{(t)},\notag\\
&\qquad a\in \mathrm{supp}(\pi(\cdot|s)),\; P(s'\mid s,a)>0\}.
\label{eq:R_adv_def_app}
\end{align}
We write $\|f\|_{\infty,\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}
:= \sup_{s\in \mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}\cup\{s_{\mathrm{abs}}\}} |f(s)|$.

\section{Shaping Convention and Absorbing-State Normalization}
\label{app:shaping_convention}

We use potential-based shaping with potential $\Phi(s)=c(x,y)$ for $s=(x,y)$ and $\Phi(s_{\mathrm{abs}})=C_{\max}$, and define
\begin{equation}
r(s,a,s')
=
r_0(s,a,s')
+
\gamma\,\Phi(s')-\Phi(s),
\end{equation}
applying shaping on \emph{all} transitions, including the absorbing self-loop.

For the absorbing self-loop, taking $r_0(s_{\mathrm{abs}},a,s_{\mathrm{abs}})=0$ gives
\[
r(s_{\mathrm{abs}},a,s_{\mathrm{abs}})
=
\gamma \Phi(s_{\mathrm{abs}})-\Phi(s_{\mathrm{abs}})
=
(\gamma-1)C_{\max}.
\]
Thus every policy satisfies the fixed shaped boundary value
\[
V^\pi(s_{\mathrm{abs}})
=
\sum_{t=0}^\infty \gamma^t (\gamma-1)C_{\max}
=
-C_{\max}.
\]

If one prefers the standard normalization $V(s_{\mathrm{abs}})=0$, use the shifted potential
$\tilde{\Phi}(s):=\Phi(s)-C_{\max}$.
This adds the constant reward $(1-\gamma)C_{\max}$ to every transition and shifts all returns by $+C_{\max}$ independently of the policy.
Accordingly, performance differences and optimal policies are unchanged.

\section{CPI Derivation for Mixture Policies}
\label{app:cpi_derivation}

We derive the CPI bound~\eqref{eq:cpi_bound} specialized to mixture policies $\pi_{\mathrm{new}} = (1-\alpha)\pi + \alpha\pi_{\mathrm{cand}}$, following the framework of~\citet{KakadeLangford2002}.

\textbf{Setup.}
Define the \emph{advantage function under the greedy policy $\pi_{\mathrm{cand}}$}:
\begin{equation}
\delta(s) := \E_{a \sim \pi_{\mathrm{cand}}(\cdot|s)}[A^\pi(s,a)].
\end{equation}
Note that $|\delta(s)| \le \varepsilon_{\mathrm{CPI}} := \max_s |\delta(s)|$ by definition.

For the mixture policy $\pi_{\mathrm{new}} = (1-\alpha)\pi + \alpha\pi_{\mathrm{cand}}$:
\begin{align}
\E_{a \sim \pi_{\mathrm{new}}(\cdot|s)}[A^\pi(s,a)]
&= (1-\alpha)\underbrace{\E_{a \sim \pi}[A^\pi(s,a)]}_{=0} + \alpha \E_{a \sim \pi_{\mathrm{cand}}}[A^\pi(s,a)] \\
&= \alpha \delta(s).
\end{align}
\textbf{Action-set convention.}
If the available edit actions depend on the plan (and hence on the state $s=(x,y)$), we interpret $\pi(\cdot\mid s)$ and
$\pi_{\mathrm{cand}}(\cdot\mid s)$ as distributions on the same per-state action set $\AAcal(s)$.
Equivalently, we may embed both policies into a fixed superset action space by assigning probability $0$ to invalid actions.
Under either convention, $\|\pi'(\cdot\mid s)-\pi(\cdot\mid s)\|_1\le 2$ holds for all $s$.
\textbf{Performance difference lemma.}
The standard performance difference lemma~\citep{KakadeLangford2002} gives:
\begin{equation}
\eta(\pi_{\mathrm{new}}) - \eta(\pi) = \frac{1}{1-\gamma} \E_{s \sim d_{\pi_{\mathrm{new}}}}[\E_{a \sim \pi_{\mathrm{new}}}[A^\pi(s,a)]] = \frac{\alpha}{1-\gamma} \E_{s \sim d_{\pi_{\mathrm{new}}}}[\delta(s)].
\end{equation}

\textbf{Distribution shift bound.}
We need to relate $\E_{s \sim d_{\pi_{\mathrm{new}}}}[\delta(s)]$ to $\E_{s \sim d_\pi}[\delta(s)]$.
The key is to bound $\|d_{\pi_{\mathrm{new}}} - d_\pi\|_1$.

\begin{lemma}[Occupancy measure coupling]
\label{lem:occupancy_coupling}
For any two policies $\pi'$ and $\pi$ in an MDP with discount $\gamma$:
\begin{equation}
\|d_{\pi'} - d_\pi\|_1 \le \frac{\gamma}{1-\gamma} \E_{s \sim d_{\pi'}}[\|\pi'(\cdot|s) - \pi(\cdot|s)\|_1].
\end{equation}
\end{lemma}

\begin{proof}
Recall the discounted occupancy satisfies the fixed-point equation
\[
d_\pi = (1-\gamma)\rho + \gamma\, d_\pi P_\pi,
\qquad
d_{\pi'} = (1-\gamma)\rho + \gamma\, d_{\pi'} P_{\pi'}.
\]
Subtracting gives
\[
d_{\pi'}-d_\pi
=
\gamma\bigl(d_{\pi'}P_{\pi'}-d_\pi P_\pi\bigr)
=
\gamma\bigl(d_{\pi'}(P_{\pi'}-P_\pi) + (d_{\pi'}-d_\pi)P_\pi\bigr).
\]
Taking $\ell_1$ norms and using that $P_\pi$ is a Markov operator (so it is non-expansive in $\ell_1$),
\[
\|d_{\pi'}-d_\pi\|_1
\le
\gamma\|d_{\pi'}(P_{\pi'}-P_\pi)\|_1
+\gamma\|(d_{\pi'}-d_\pi)P_\pi\|_1
\le
\gamma\|d_{\pi'}(P_{\pi'}-P_\pi)\|_1
+\gamma\|d_{\pi'}-d_\pi\|_1.
\]
Rearranging yields
\[
\|d_{\pi'}-d_\pi\|_1
\le
\frac{\gamma}{1-\gamma}\,\|d_{\pi'}(P_{\pi'}-P_\pi)\|_1.
\]
Finally,
\begin{align}
\|d_{\pi'}(P_{\pi'}-P_\pi)\|_1
&=
\Bigl\|
\sum_s d_{\pi'}(s)\bigl(P_{\pi'}(\cdot|s)-P_\pi(\cdot|s)\bigr)
\Bigr\|_1
\notag\\
&\le
\sum_s d_{\pi'}(s)\,\bigl\|P_{\pi'}(\cdot|s)-P_\pi(\cdot|s)\bigr\|_1
\notag\\
&=
\E_{s\sim d_{\pi'}}\bigl[\|P_{\pi'}(\cdot|s)-P_\pi(\cdot|s)\|_1\bigr]
\notag\\
&=
\E_{s\sim d_{\pi'}}\Bigl[\Bigl\|\sum_a \bigl(\pi'(a|s)-\pi(a|s)\bigr)\,P(\cdot\mid s,a)\Bigr\|_1\Bigr]
\notag\\
&\le
\E_{s\sim d_{\pi'}}\Bigl[\sum_a \bigl|\pi'(a|s)-\pi(a|s)\bigr|\,\underbrace{\|P(\cdot\mid s,a)\|_1}_{=1}\Bigr]
\notag\\
&=
\E_{s\sim d_{\pi'}}\bigl[\|\pi'(\cdot|s)-\pi(\cdot|s)\|_1\bigr],
\end{align}
which gives the claim.
\end{proof}

For our mixture:
\begin{equation}
\|\pi_{\mathrm{new}}(\cdot|s) - \pi(\cdot|s)\|_1
=
\alpha \|\pi_{\mathrm{cand}}(\cdot|s) - \pi(\cdot|s)\|_1
\le 2\alpha,
\end{equation}
since $\|p-q\|_1\le 2$ for any two probability distributions $p,q$ on the same action space.


Thus:
\begin{equation}
\|d_{\pi_{\mathrm{new}}} - d_\pi\|_1 \le \frac{2\gamma\alpha}{1-\gamma}.
\end{equation}

\textbf{Combining the bounds.}
Write:
\begin{align}
\E_{s \sim d_{\pi_{\mathrm{new}}}}[\delta(s)] &= \E_{s \sim d_\pi}[\delta(s)] + \left(\E_{s \sim d_{\pi_{\mathrm{new}}}}[\delta(s)] - \E_{s \sim d_\pi}[\delta(s)]\right).
\end{align}

The second term is bounded by:
\begin{equation}
\left|\E_{s \sim d_{\pi_{\mathrm{new}}}}[\delta(s)] - \E_{s \sim d_\pi}[\delta(s)]\right| \le \|d_{\pi_{\mathrm{new}}} - d_\pi\|_1 \cdot \sup_s|\delta(s)| \le \frac{2\gamma\alpha}{1-\gamma} \varepsilon_{\mathrm{CPI}}.
\end{equation}

Therefore:
\begin{align}
\eta(\pi_{\mathrm{new}}) &= \eta(\pi) + \frac{\alpha}{1-\gamma} \E_{s \sim d_{\pi_{\mathrm{new}}}}[\delta(s)] \\
&\ge \eta(\pi) + \frac{\alpha}{1-\gamma} \E_{s \sim d_\pi}[\delta(s)] - \frac{\alpha}{1-\gamma} \cdot \frac{2\gamma\alpha}{1-\gamma} \varepsilon_{\mathrm{CPI}} \\
&= L_\pi(\pi_{\mathrm{new}}) - \frac{2\gamma\varepsilon_{\mathrm{CPI}}}{(1-\gamma)^2} \alpha^2,
\end{align}
where in the last step we used $L_\pi(\pi_{\mathrm{new}}) = \eta(\pi) + \frac{1}{1-\gamma}\E_{s \sim d_\pi, a \sim \pi_{\mathrm{new}}}[A^\pi(s,a)] = \eta(\pi) + \frac{\alpha}{1-\gamma}\E_{s \sim d_\pi}[\delta(s)]$.

This establishes~\eqref{eq:cpi_bound}.

\section{Proofs}
\label{app:proofs}

\subsection{Lemma~\ref{lem:interior_proj} (Interior projection fixed points)}

\begin{lemma}[Interior projection fixed points]
\label{lem:interior_proj}
Let $\Pi_R:\R^d\to\R^d$ be the Euclidean projection onto the closed ball $\{z:\|z\|_2\le R\}$ as in~\eqref{eq:projection}.
Suppose $z^*=\Pi_R(u)$ for some $u\in\R^d$.
If $\|z^*\|_2 < R$, then $z^* = u$ (i.e., the projection is inactive at $u$).
Consequently, if $z^*$ is a fixed point of $\tilde f_\theta(z,y,x)=\Pi_R(f_\theta(z,y,x))$ and $\|z^*\|_2 < R$, then $f_\theta(z^*,y,x)=z^*$ and $\Pi_R$ is inactive at $f_\theta(z^*,y,x)$.
\end{lemma}

\begin{proof}
By~\eqref{eq:projection}, if $\|u\|_2 \le R$ then $\Pi_R(u)=u$.
Since $z^*=\Pi_R(u)$ lies in the open ball $\|z^*\|_2<R$, we have $z^*\ne R\tfrac{u}{\|u\|_2}$, so the second case of~\eqref{eq:projection} cannot apply; hence $\|u\|_2\le R$ and $\Pi_R(u)=u=z^*$.
For the consequence: if $z^*=\tilde f_\theta(z^*,y,x)=\Pi_R(f_\theta(z^*,y,x))$ and $\|z^*\|_2<R$, applying the first statement with $u=f_\theta(z^*,y,x)$ gives $f_\theta(z^*,y,x)=z^*$.
\end{proof}

\longonly{
\subsection{Proof of Lemma~\ref{lem:Kstep_contraction}}

\begin{proof}
For any $s$:
\begin{align*}
(\mathcal{T}^\pi_K V)(s) &= \E\Bigl[\sum_{k=0}^{K-1}\gamma^k r_k + \gamma^K V(s_K)\Bigr] \\
&= b(s) + \gamma^K (P_\pi^K V)(s),
\end{align*}
where $b(s)$ depends only on rewards and $P_\pi^K$ is the $K$-step transition operator under $\pi$.
Thus:
\[
(\mathcal{T}^\pi_K V)(s) - (\mathcal{T}^\pi_K W)(s) = \gamma^K (P_\pi^K (V-W))(s),
\]
and since $P_\pi^K$ is a Markov operator with $\|P_\pi^K\|_\infty \le 1$:
\[
\|\mathcal{T}^\pi_K V - \mathcal{T}^\pi_K W\|_\infty \le \gamma^K \|V-W\|_\infty.
\]

Since $\gamma^K<1$, the operator $\mathcal{T}^\pi_K$ is a contraction on the Banach space of bounded functions with $\|\cdot\|_\infty$; by Banach's fixed-point theorem it has a unique fixed point.
Since $\mathcal{T}^\pi V^\pi = V^\pi$, iterating gives $\mathcal{T}^\pi_K V^\pi = V^\pi$, so $V^\pi$ is this unique fixed point.


Under our shaping convention, the absorbing self-loop reward is $(\gamma-1)C_{\max}$, hence
$V^\pi(s_{\mathrm{abs}})=-C_{\max}$ for all policies.
Including $s_{\mathrm{abs}}$ in the domain ensures the sup-norm is taken over a well-defined set containing all states reachable during $K$-step rollouts.
By Definition~\ref{def:reachable}, $\mathcal{R}_\pi$ is closed under transitions, so the contraction property holds on this domain.
\end{proof}
}

\subsection{Proof of Proposition~\ref{prop:banach}}

\begin{proof}
Fix $(x,y)$.
By Assumption~\ref{ass:Lip}, $T_{x,y}(z)=\tilde f_\theta(z,y,x)$ maps $\mathcal{Z}_{\mathrm{inv}}$ to itself (forward-invariance) and is a contraction with modulus $L_z<1$.
Since $\mathcal{Z}_{\mathrm{inv}}$ is a closed subset of the complete metric space $(\ZZ,\|\cdot\|)=(\R^d,\|\cdot\|_2)$, it is itself complete.
(Boundedness is used separately to ensure $R=\sup_{z\in\mathcal{Z}_{\mathrm{inv}}}\|z\|<\infty$.)

Banach's theorem yields unique fixed point $z^*(x,y) \in \mathcal{Z}_{\mathrm{inv}}$ and linear convergence~\eqref{eq:linear_convergence}.

For the finite-unrolling bound with $z^{(0)} = z_{\mathrm{init}}(x,y)$:
\[
z^* - z^{(n)} = \sum_{k=n}^{\infty}(z^{(k+1)} - z^{(k)}).
\]
Contraction gives $\|z^{(k+1)} - z^{(k)}\| \le L_z^k \|z^{(1)}-z^{(0)}\|$, so:
\[
\|z^* - z^{(n)}\| \le \|z^{(1)}-z^{(0)}\| \sum_{k=n}^{\infty} L_z^k = \frac{L_z^n}{1-L_z}\|z^{(1)}-z^{(0)}\|.
\]
By definition~\eqref{eq:Cz_def} (using the projected update $\tilde f_\theta=\Pi_R\circ f_\theta$), we have
$\|z^{(1)}-z^{(0)}\| = \|\tilde f_\theta(z_{\mathrm{init}},y,x) - z_{\mathrm{init}}\| \le C_z$.
\end{proof}

\subsection{Proof of Lemma~\ref{lem:drift_bound}}

\begin{proof}
For $T_{x,y_t}^{\circ n}$ a $\kappa_n$-contraction (where $\kappa_n = L_z^n$):
\begin{align*}
\|z_{t+1} - z^*(x,y_t)\| &= \|T_{x,y_t}^{\circ n}(z_t) - T_{x,y_t}^{\circ n}(z^*(x,y_t))\| \le \kappa_n e_t.
\end{align*}
By Assumption~\ref{ass:twotimescale}:
\[
\|z^*(x,y_t) - z^*(x,y_{t+1})\| \le L_{z^*}\|y_{t+1}-y_t\|_{\mathcal{E}} \le L_{z^*}\Delta y_{\max}.
\]
Thus:
\[
e_{t+1} = \|z_{t+1} - z^*(x,y_{t+1})\| \le \|z_{t+1} - z^*(x,y_t)\| + \|z^*(x,y_t) - z^*(x,y_{t+1})\| \le \kappa_n e_t + L_{z^*}\Delta y_{\max}.
\]

Unrolling this recurrence:
\[
e_t \le \kappa_n^t e_0 + L_{z^*}\Delta y_{\max} \sum_{j=0}^{t-1} \kappa_n^j = \kappa_n^t e_0 + \frac{L_{z^*}\Delta y_{\max}(1 - \kappa_n^t)}{1-\kappa_n}.
\]

Using $e_0 \le E_0$ and noting that this expression is maximized either at $t=0$ (giving $E_0$) or as $t \to \infty$ (giving $\frac{L_{z^*}\Delta y_{\max}}{1-\kappa_n}$), we have:
\[
\sup_t e_t \le \max\left(E_0, \frac{L_{z^*}\Delta y_{\max}}{1-\kappa_n}\right) \le E_0 + \frac{L_{z^*}\Delta y_{\max}}{1-\kappa_n}.
\]

For the used latent:
\[
\|z_t^{(n)} - z^*(x,y_t)\| = \|T_{x,y_t}^{\circ n}(z_t) - z^*(x,y_t)\|
= \|T_{x,y_t}^{\circ n}(z_t) - T_{x,y_t}^{\circ n}(z^*(x,y_t))\| \le \kappa_n e_t,
\]
where the second equality uses that $z^*(x,y_t)$ is the fixed point of $T_{x,y_t}$,
hence also of $T_{x,y_t}^{\circ n}$, and the inequality follows from
$\kappa_n$-contractivity of the $n$-fold composition.
Thus:
\[
\sup_t \|z_t^{(n)} - z^*(x,y_t)\|
\le
\kappa_n \sup_t e_t
\le
\kappa_n \max\!\left(E_0,\,\frac{L_{z^*}\Delta y_{\max}}{1-\kappa_n}\right)
=
\Cdrift(n),
\]
and the final ``$\le$'' in~\eqref{eq:Cdrift_def} gives the simpler additive upper bound.
\end{proof}

\subsection{Proof of Proposition~\ref{prop:error_bounds}}
Throughout this proof, interpret $\|\cdot\|_\infty$ as the sup-norm over the advantage-evaluation closure $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}$ (Definition~\ref{def:adv_closure}), which is closed under forward dynamics of $\pi$ and contains $s_{\mathrm{abs}}$.

\begin{proof}
\textbf{Part (i):}
Since $V^\pi$ is the fixed point of $\mathcal{T}^\pi_K$\longonly{ (Lemma~\ref{lem:Kstep_contraction})}:
\begin{align*}
\|V - V^\pi\|_\infty &= \|V - \mathcal{T}^\pi_K V^\pi\|_\infty \\
&\le \|V - \mathcal{T}^\pi_K V\|_\infty + \|\mathcal{T}^\pi_K V - \mathcal{T}^\pi_K V^\pi\|_\infty \\
&\le \|V - \mathcal{T}^\pi_K V\|_\infty + \gamma^K\|V - V^\pi\|_\infty.
\end{align*}
Rearranging: $(1-\gamma^K)\|V-V^\pi\|_\infty \le \|V - \mathcal{T}^\pi_K V\|_\infty$.

\textbf{Part (ii):}
Decompose:
\[
\|U_n - V^\pi\|_\infty \le \|U_* - V^\pi\|_\infty + \|U_n - U_*\|_\infty.
\]
Apply part (i) to $U_*$:
\[
\|U_* - V^\pi\|_\infty \le \frac{1}{1-\gamma^K}\|U_* - \mathcal{T}^\pi_K U_*\|_\infty = \frac{\varepsilon_{\mathrm{res}}^*}{1-\gamma^K}.
\]
By Lipschitzness~\eqref{eq:LV_def} and Proposition~\ref{prop:banach}:
\[
\|U_n - U_*\|_\infty \le L_V \sup_{(x,y)}\|z^{(n)}(x,y) - z^*(x,y)\| \le L_V \frac{L_z^n}{1-L_z} C_z.
\]
Combining the two bounds yields:
\[
\|U_n - V^\pi\|_\infty
\le
\frac{\varepsilon_{\mathrm{res}}^*}{1-\gamma^K}
+
L_V\,\frac{L_z^n}{1-L_z}\,C_z.
\]
\end{proof}

\longonly{
\subsection{Proof of Lemma~\ref{lem:residual_transfer}}
Throughout this proof, interpret $\|\cdot\|_\infty$ as the sup-norm over the advantage-evaluation closure $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}$ (Definition~\ref{def:adv_closure}), which is closed under forward dynamics of $\pi$ and contains $s_{\mathrm{abs}}$.

\begin{proof}
For any $s$:
\begin{align*}
U_*(s) - \mathcal{T}^\pi_K U_*(s) &= (U_* - U_n)(s) + (U_n - \mathcal{T}^\pi_K U_n)(s) \\
&\quad + (\mathcal{T}^\pi_K U_n - \mathcal{T}^\pi_K U_*)(s).
\end{align*}
Taking absolute values and sup-norms:
\begin{align*}
\|U_* - \mathcal{T}^\pi_K U_*\|_\infty &\le \|U_n - U_*\|_\infty + \|U_n - \mathcal{T}^\pi_K U_n\|_\infty \\
&\quad + \gamma^K\|U_n - U_*\|_\infty \\
&= \|U_n - \mathcal{T}^\pi_K U_n\|_\infty + (1+\gamma^K)\|U_n - U_*\|_\infty.
\end{align*}
\end{proof}
}

\subsection{Proof of Lemma~\ref{lem:A_error_app}}

\begin{proof}
For any $(s,a)$,
\[
\widehat Q_V(s,a) - Q^\pi(s,a)
=
\gamma\,\E_{s'\sim P(\cdot\mid s,a)}\bigl[V(s')-V^\pi(s')\bigr].
\]
If $s\in\mathcal{R}_\pi$ and $a\in \mathrm{supp}(\pi(\cdot|s))\cup\mathrm{supp}(\pi_{\mathrm{cand}}(\cdot|s))$, then any such $s'$ lies in $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{+}\subseteq \mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}$ by Definition~\ref{def:adv_closure},
so $|V(s')-V^\pi(s')|\le \delta_V$ and hence
$|\widehat Q_V(s,a)-Q^\pi(s,a)|\le \gamma\delta_V$.

Similarly,
\[
\Bigl|\E_{b\sim\pi(\cdot\mid s)}\widehat Q_V(s,b)-\E_{b\sim\pi(\cdot\mid s)}Q^\pi(s,b)\Bigr|
\le \gamma\delta_V,
\]
since $b\sim\pi(\cdot|s)$ implies $b\in \mathrm{supp}(\pi(\cdot|s))\subseteq \mathrm{supp}(\pi(\cdot|s))\cup\mathrm{supp}(\pi_{\mathrm{cand}}(\cdot|s))$ and hence the next state lies in $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{+}$.
Subtracting these two bounds yields
$|\widehat A_V(s,a)-A^\pi(s,a)|\le 2\gamma\delta_V$.
\end{proof}

\subsection{Proof of Theorem~\ref{thm:monotone_with_error}}

\begin{proof}
The CPI bound~\eqref{eq:cpi_bound} (derived in Appendix~\ref{app:cpi_derivation}) gives:
\[
\eta(\pi_{\mathrm{new}}) \ge L_\pi(\pi_{\mathrm{new}}) - \frac{2\varepsilon_{\mathrm{CPI}}\gamma}{(1-\gamma)^2}\alpha^2.
\]

Now relate the true surrogate $L_\pi(\pi_{\mathrm{new}})$ to the estimated surrogate $\widehat{L}_\pi(\pi_{\mathrm{new}})$:
\[
L_\pi(\pi_{\mathrm{new}}) - \widehat{L}_\pi(\pi_{\mathrm{new}})
=
\frac{1}{1-\gamma}\E_{s\sim d_\pi,\, a\sim\pi_{\mathrm{new}}(\cdot\mid s)}\!\left[A^\pi(s,a) - \widehat{A}(s,a)\right].
\]

For the mixture policy $\pi_{\mathrm{new}}=(1-\alpha)\pi+\alpha\pi_{\mathrm{cand}}$:
\begin{align*}
\E_{a\sim\pi_{\mathrm{new}}(\cdot\mid s)}[A^\pi(s,a) - \widehat{A}(s,a)]
&=
(1-\alpha)\E_{a\sim\pi(\cdot\mid s)}[A^\pi(s,a) - \widehat{A}(s,a)] \\
&\quad + \alpha\E_{a\sim\pi_{\mathrm{cand}}(\cdot\mid s)}[A^\pi(s,a) - \widehat{A}(s,a)].
\end{align*}

By definition of advantage, $\E_{a\sim\pi(\cdot\mid s)}[A^\pi(s,a)]=0$.
By the centering assumption~\eqref{eq:centering_condition}, $\E_{a\sim\pi(\cdot\mid s)}[\widehat A(s,a)]=0$.
Hence the $(1-\alpha)$ term vanishes, and we obtain
\[
\Bigl|\E_{a\sim\pi_{\mathrm{new}}(\cdot\mid s)}[A^\pi(s,a) - \widehat{A}(s,a)]\Bigr|
=
\alpha\Bigl|\E_{a\sim\pi_{\mathrm{cand}}(\cdot\mid s)}[A^\pi(s,a) - \widehat{A}(s,a)]\Bigr|
\le \alpha\,\varepsilon_{A,\mathrm{cand}}.
\]

\textbf{Why this is a tightening:}
Note that by Jensen's inequality and Assumption~\ref{ass:Ahat_error_bound},
\[
\varepsilon_{A,\mathrm{cand}} 
= \sup_{s}\bigl|\E_{a\sim\pi_{\mathrm{cand}}}[\widehat{A}(s,a)-A^\pi(s,a)]\bigr|
\le \sup_{s}\E_{a\sim\pi_{\mathrm{cand}}}\bigl[|\widehat{A}(s,a)-A^\pi(s,a)|\bigr]
\le \varepsilon_A.
\]
Thus the evaluation error contribution $\alpha\varepsilon_{A,\mathrm{cand}}/(1-\gamma)$ is strictly smaller than the naive bound $\varepsilon_A/(1-\gamma)$ by a factor of $\alpha$ (plus the potential improvement from $\varepsilon_{A,\mathrm{cand}} \le \varepsilon_A$).

Taking expectation over $s\sim d_\pi$ yields
\[
L_\pi(\pi_{\mathrm{new}}) \ge \widehat{L}_\pi(\pi_{\mathrm{new}}) - \frac{\alpha}{1-\gamma}\varepsilon_{A,\mathrm{cand}}.
\]

Combining with the CPI bound gives~\eqref{eq:main_theorem}.
\end{proof}

\section{Implementation Details}
\label{app:implementation}

\shortonly{
\subsection{Algorithm Pseudocode}
\label{app:algorithm_pseudocode}

Algorithm~\ref{alg:upi} presents the full UPI--TRM update step in persistent-$z$ form.
For the episodic-$z$ variant, skip line~10 (``Carry latent'') and reinitialize $z_k \gets z_{\mathrm{init}}(x,y_k)$ at the start of each loop iteration.

\begin{algorithm}[h]
\small
\caption{UPI--TRM: single interaction + update step}
\label{alg:upi}
\begin{algorithmic}[1]
\STATE \textbf{Input:} $x, y_0, z_0, \gamma, n, K, \alpha, \tau, R$
\STATE Initialize $\tilde{s}_0 \gets (x,y_0,z_0)$; $\mathit{done} \gets \mathrm{false}$
\FOR{$k = 0$ to $K-1$}
  \IF{$\mathit{done}$}
    \STATE $r_k \gets (\gamma-1)C_{\max}$; $\tilde{s}_{k+1} \gets s_{\mathrm{abs}}$ \hfill \emph{// Absorbing self-loop}
  \ELSE
    \STATE $z_k^{(n)} \gets (\Pi_R \circ f_\theta)^{\circ n}(z_k,y_k,x)$ \hfill \emph{// $n$ inner steps}
    \STATE Sample $a_k \sim \pi_{\phi}(a \mid y_k,z_k^{(n)},x)$
    \STATE $y_{k+1} \gets \mathrm{edit}(y_k,a_k;x)$; receive $r_k$
    \STATE $z_{k+1} \gets z_k^{(n)}$ \hfill \emph{// Carry latent (skip for episodic-$z$)}
    \STATE $\tilde{s}_{k+1} \gets (x,y_{k+1},z_{k+1})$
    \IF{terminated}
     \STATE $\mathit{done} \gets \mathrm{true}$; $\tilde{s}_{k+1} \gets s_{\mathrm{abs}}$
    \ENDIF
  \ENDIF
\ENDFOR
\STATE Compute target $G^{(K)}$ via~\eqref{eq:GK_def}
\STATE Update $(\theta,\psi)$ by minimizing $\mathcal{L}_{\mathrm{val}}$ (bootstrapped MSE)
\STATE Form advantages $\widehat A$ via centered estimator (see below)
\STATE Policy gradient step $\to$ candidate $\pi_{\mathrm{cand}}$
\STATE $\pi_{\mathrm{new}} \gets (1-\alpha)\pi_{\phi} + \alpha\pi_{\mathrm{cand}}$
\STATE Distill: $\pi_\phi \gets \arg\min_\phi \KL(\pi_{\mathrm{new}} \| \pi_\phi)$
\STATE $\bar\psi \gets \tau \bar\psi + (1-\tau)\psi$ \hfill \emph{// Target network update}
\end{algorithmic}
\end{algorithm}
}

\textbf{Replay and mini-batches.}
We maintain a replay buffer and sample mini-batches for value and policy updates.
Latents in the buffer computed under older $\theta$ are stale; we recompute $z^{(n)}$ for sampled states or use slowly moving target encoder $f_{\bar\theta}$.

\textbf{Advantage estimation.}
While theory uses the centered estimator~\eqref{eq:Ahat_def_app}, we may use GAE/$(\lambda)$-return variants for variance reduction.
\textbf{Important:} the $O(\alpha\varepsilon_A)$ reduction in Theorem~\ref{thm:monotone_with_error} relies on \emph{exact} statewise centering $\E_{a\sim\pi}[\widehat{A}(s,a)]=0$, which holds deterministically only when the baseline is computed exactly as $B_V(s)=\E_{b\sim\pi(\cdot|s)}[\widehat Q_V(s,b)]$ (e.g., by summation over a discrete action space).
When we instead use a learned value baseline (as in standard GAE), centering is only approximate and the deterministic bound degrades according to the centering defect $\varepsilon_{\mathrm{cent}}$; the naive $O(\varepsilon_A/(1-\gamma))$ penalty is recovered only as a worst-case upper bound when $\varepsilon_{\mathrm{cent}}$ and $\varepsilon_{A,\mathrm{cand}}$ are both controlled by $\varepsilon_A$.

\textbf{Operator-norm control and contraction details.}
We enforce contraction on the inner $z\!\to\!z$ recursion via explicit operator-norm clamping (periodic rescaling of linear weights using power-iteration estimates) followed by per-layer output scaling / an explicit scalar contraction factor to target $L_z<1$.
We estimate $\hat L_z$ on mini-batches via finite differences:
\[
\hat L_z = \max_i \frac{\|f_\theta(z_i,y_i,x_i) - f_\theta(z_i',y_i,x_i)\|}{\|z_i - z_i'\|}.
\]

\textbf{Enforcing forward-invariance.}
To ensure $z_{\mathrm{init}}(x,y) \in \mathcal{Z}_{\mathrm{inv}}$ and that iterates remain in $\mathcal{Z}_{\mathrm{inv}}$:
\begin{itemize}
\item Use zero initialization $z_{\mathrm{init}}(x,y) = 0$ with $\mathcal{Z}_{\mathrm{inv}} = \{z : \|z\| \le R\}$.
\item After each update, apply Euclidean projection onto the ball: $z \gets \Pi_R(z)$ with $\Pi_R$ defined in~\eqref{eq:projection}.
\item Under the Euclidean norm, this projection is $1$-Lipschitz (non-expansive) and therefore preserves the contraction modulus.
\item Equivalently, define $\tilde f_\theta := \Pi_R \circ f_\theta$ and unroll $\tilde f_\theta^{\circ n}$, i.e.,
$z^{(t+1)}=\Pi_R(f_\theta(z^{(t)},y,x))$ for $t=0,\dots,n-1$.
\end{itemize}

\textbf{Monitoring drift.}
For persistent-$z$, we track:
\[
\widehat{C}_{\mathrm{drift}}(n) = \max_t \|z_t^{(n)} - z_{\mathrm{init}}(x,y_t)\|.
\]
This upper-bounds latent movement relative to fresh initialization.

\textbf{Distillation.}
When distilling $\pi_{\mathrm{new}}$ to $\pi_\phi$, we minimize:
\[
\mathcal{L}_{\mathrm{distill}}(\phi) = \E_{\tilde{s}\sim\text{buffer}}[\KL(\pi_{\mathrm{new}}(\cdot\mid\tilde{s}) \| \pi_\phi(\cdot\mid\tilde{s}))].
\]
This is a heuristic not covered by theory; we monitor $\KL(\pi_{\mathrm{new}} \| \pi_\phi)$ on replay states (sampled from the buffer during training) to verify the projection is accurate.

\section{Mapping to Original TRM Architecture}
\label{app:trm_mapping}

We provide an explicit mapping between our formalism and the original TRM implementation~\citep{TRM2025}.

\begin{center}
\begin{tabular}{lll}
\toprule
Our Formalism & TRM Implementation & Notes \\
\midrule
$z \in \ZZ$ & Hidden state & Latent reasoning state \\
$y \in \YY$ & Output grid/sequence & Candidate solution \\
$f_\theta$ & Recurrent update & Single update step \\
$n$ (inner steps) & Recursion depth & Controlled by ACT head \\
$z_{\mathrm{init}}(x,y)$ & Initial hidden & Zero or learned \\
$\pi_\phi(a|y,z,x)$ & Edit head & Produces next output \\
$\mathrm{edit}(y,a;x)$ & Output update & Deterministic \\
\midrule
Episodic-$z$ & Not in original & Our simplification \\
Persistent-$z$ & Original TRM & $z$ carried across edits \\
Deep supervision & Training signal & Replaced by RL reward \\
ACT halting & Early stopping & Can integrate with our $n$ \\
\bottomrule
\end{tabular}
\end{center}

The key differences:
\begin{enumerate}
\item \textbf{Training signal}: Original TRM uses supervised loss with deep supervision; we use checker-based rewards.
\item \textbf{Episodic-$z$ vs.\ persistent-$z$}: We analyze both; original TRM is persistent-$z$.
\item \textbf{Halting}: Original TRM learns when to stop via ACT; our analysis fixes $n$ for tractability.
\end{enumerate}
\section{Advantage estimation and bounds}
\label{app:adv_bounds}

\begin{align}
\widehat Q_V(s,a) &= \E_{s'}[r(s,a,s') + \gamma V(s')], \\
\widehat A_V(s,a) &= \widehat Q_V(s,a) -
\E_{b\sim\pi(\cdot|s)}[\widehat Q_V(s,b)].
\label{eq:Ahat_def_app}
\end{align}

\begin{remark}[Centering and baseline computation]
\label{rem:baseline_app}
The $O(\alpha\,\varepsilon_{A,\mathrm{cand}})$ reduction in Theorem~\ref{thm:monotone_with_error}
relies on the \emph{statewise centering} property
\[
\E_{a\sim\pi(\cdot\mid s)}[\widehat A(s,a)] = 0 \qquad \text{for all } s.
\]
This holds identically when $\widehat A$ is defined as in~\eqref{eq:Ahat_def_app} with the baseline computed exactly as
$B_V(s)=\E_{b\sim\pi(\cdot\mid s)}[\widehat Q_V(s,b)]$ (e.g., by summation over a finite action space).

If the baseline is approximated, centering may fail. We quantify this by the \emph{centering defect}
\[
\varepsilon_{\mathrm{cent}}
:=
\sup_{s\in\mathcal{R}_\pi}\left|\E_{a\sim\pi(\cdot\mid s)}[\widehat A(s,a)]\right|.
\]
Theorem~\ref{thm:monotone_with_error} corresponds to the ideal case $\varepsilon_{\mathrm{cent}}=0$.
\end{remark}

\begin{lemma}[Value to one-step advantage error]
\label{lem:A_error_app}
Let $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}$ be the advantage-evaluation closure from Definition~\ref{def:adv_closure},
and let $\delta_V := \sup_{s\in \mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}} |V(s)-V^\pi(s)|$.
Then for all $(s,a)$ with $s\in\mathcal{R}_\pi$ and $a\in \mathrm{supp}(\pi(\cdot|s))\cup\mathrm{supp}(\pi_{\mathrm{cand}}(\cdot|s))$,
\[
|\widehat A_V(s,a) - A^\pi(s,a)| \le 2\gamma\,\delta_V.
\]
\end{lemma}

The factor $2\gamma$ is tight worst-case (error appears in both $Q$ and baseline).
In practice, errors in $\widehat Q_V(s,a)$ and the baseline $\E_\pi[\widehat Q_V]$ often have the same sign and partially cancel.
\begin{remark}[Extensions to $K$-step and GAE-style advantages]
\label{rem:A_error_kstep_app}
Lemma~\ref{lem:A_error_app} is stated for the \emph{one-step} estimator~\eqref{eq:Ahat_def_app}, hence the factor $2\gamma$.
If instead one defines a $K$-step action-value estimate
\begin{equation*}
\widehat Q_V^{(K)}(s,a) :=
\E\biggl[\sum_{t=0}^{K-1}\gamma^t r_t + \gamma^K V(s_K) \biggm| s_0=s, a_0=a, \pi\biggr],
\end{equation*}
and the corresponding centered advantage
$\widehat A_V^{(K)}(s,a):=\widehat Q_V^{(K)}(s,a)-\E_{b\sim\pi(\cdot|s)}[\widehat Q_V^{(K)}(s,b)]$
(with the baseline computed exactly), then the same argument yields
\[
|\widehat A_V^{(K)}(s,a)-A^\pi(s,a)| \le 2\gamma^K\,\delta_V.
\]
In practice we may use $\lambda$-return / GAE-style estimators (weighted mixtures of multi-step returns), which admit analogous bounds with a coefficient depending on $(\gamma,\lambda)$; throughout, we absorb such estimator-dependent constants into the uniform advantage error parameter $\varepsilon_A$ in Assumption~\ref{ass:Ahat_error_bound}.
\end{remark}

\begin{corollary}[Advantage error in terms of dials]
\label{cor:epsA_dials_app}
Under Assumptions~\ref{ass:Lip} and~\eqref{eq:LV_def}, combining Proposition~\ref{prop:error_bounds} with Lemma~\ref{lem:A_error_app} yields:
\begin{align}
\varepsilon_A
&\le
\underbrace{\frac{2\gamma\,\varepsilon_{\mathrm{res}}^*}{1-\gamma^K}}_{\text{architectural}}
+
\underbrace{2\gamma\,L_V\,\frac{L_z^{\,n}}{1-L_z}\,C_z}_{\text{unrolling}}.
\label{eq:epsA_dials_app}
\end{align}
Here $\varepsilon_A$ bounds the advantage error on $\mathcal{R}_\pi$ for actions
$a\in \mathrm{supp}(\pi(\cdot|s))\cup\mathrm{supp}(\pi_{\mathrm{cand}}(\cdot|s))$,
and the intermediate value-error quantity in Lemma~\ref{lem:A_error_app} is taken over the advantage-evaluation closure $\mathcal{R}_{\pi,\pi_{\mathrm{cand}}}^{\mathrm{adv}}$ (Definition~\ref{def:adv_closure}).
\end{corollary}
\end{document}
